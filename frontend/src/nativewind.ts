import { cssInterop } from 'nativewind';
import { LinearGradient } from 'expo-linear-gradient';

// Enable `className` on common third-party components.
cssInterop(LinearGradient, { className: 'style' });

