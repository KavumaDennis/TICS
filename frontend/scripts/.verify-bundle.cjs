"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// stub:firebase/firestore
var require_firestore = __commonJS({
  "stub:firebase/firestore"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:firebase/app
var require_app = __commonJS({
  "stub:firebase/app"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:@react-native-async-storage/async-storage
var require_async_storage = __commonJS({
  "stub:@react-native-async-storage/async-storage"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:react-native
var require_react_native = __commonJS({
  "stub:react-native"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:firebase/auth
var require_auth = __commonJS({
  "stub:firebase/auth"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:firebase/functions
var require_functions = __commonJS({
  "stub:firebase/functions"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// stub:firebase/storage
var require_storage = __commonJS({
  "stub:firebase/storage"(exports2, module2) {
    module2.exports = new Proxy({}, { get: () => ({}), apply: () => ({}) });
  }
});

// assets/images/destination-fallback.jpg
var require_destination_fallback = __commonJS({
  "assets/images/destination-fallback.jpg"(exports2, module2) {
    module2.exports = "data:image/jpeg;base64,";
  }
});

// src/modules/explore/services/discovery/providers/types.ts
function buildTicsId(source, rawId) {
  return `tics:${source}:${String(rawId)}`;
}
function isValidCoordinate(lat, lng) {
  return typeof lat === "number" && typeof lng === "number" && Number.isFinite(lat) && Number.isFinite(lng) && lat >= -90 && lat <= 90 && lng >= -180 && lng <= 180;
}
function isValidUserLocation(lat, lng) {
  if (!isValidCoordinate(lat, lng)) return false;
  if (lat === 0 && lng === 0) return false;
  return true;
}

// src/modules/explore/services/discovery/providers/OpenStreetMapProvider.ts
var OVERPASS_ENDPOINTS = [
  {
    url: "https://overpass-api.de/api/interpreter",
    coverage: "global",
    purpose: "primary \u2014 fastest when healthy"
  },
  {
    url: "https://maps.mail.ru/osm/tools/overpass/api/interpreter",
    coverage: "global",
    purpose: "mirror 1 \u2014 slowest but most consistently reachable; requires a long budget"
  },
  {
    url: "https://overpass.kumi.systems/api/interpreter",
    coverage: "global",
    purpose: "mirror 2"
  },
  {
    url: "https://overpass.private.coffee/api/interpreter",
    coverage: "global",
    purpose: "mirror 3"
  }
];
var OVERPASS_URLS = OVERPASS_ENDPOINTS.map((e) => e.url);
var MAX_RADIUS_KM = 300;
var LOCAL_BUDGET_MS = 32e3;
var PER_REQUEST_CAP_MS = 28e3;
var MAX_RETRIES = 1;
var BACKOFF_BASE_MS = 600;
var MIN_REQUEST_GAP_MS = 300;
var CACHE_TTL_MS = 30 * 60 * 1e3;
var STALE_TTL_MS = 24 * 60 * 60 * 1e3;
var EMPTY_RESULT_TTL_MS = 5 * 60 * 1e3;
var DEFAULT_LIMIT = 60;
var ENDPOINT_COOLDOWN_MS = 3 * 60 * 1e3;
var FAILURES_TO_DEGRADE = 1;
var FAILURES_TO_FAIL = 3;
var endpointStates = /* @__PURE__ */ new Map();
function endpointState(url) {
  let s = endpointStates.get(url);
  if (!s) {
    s = { health: "healthy", consecutiveFailures: 0, lastFailureAt: 0 };
    endpointStates.set(url, s);
  }
  if (s.health !== "healthy" && s.lastFailureAt > 0 && Date.now() - s.lastFailureAt > ENDPOINT_COOLDOWN_MS) {
    s.health = "healthy";
    s.consecutiveFailures = 0;
  }
  return s;
}
function recordEndpointSuccess(url) {
  const s = endpointState(url);
  s.health = "healthy";
  s.consecutiveFailures = 0;
}
function recordEndpointFailure(url) {
  const s = endpointState(url);
  s.consecutiveFailures += 1;
  s.lastFailureAt = Date.now();
  if (s.consecutiveFailures >= FAILURES_TO_FAIL) s.health = "failed";
  else if (s.consecutiveFailures >= FAILURES_TO_DEGRADE) s.health = "degraded";
}
function orderedEndpoints() {
  const rank = (h) => h === "healthy" ? 0 : h === "degraded" ? 1 : 2;
  return [...OVERPASS_URLS].sort(
    (a, b) => rank(endpointState(a).health) - rank(endpointState(b).health)
  );
}
function logEndpointHealth() {
  const primary = endpointState(OVERPASS_URLS[0]);
  const secondary = OVERPASS_URLS[1] ? endpointState(OVERPASS_URLS[1]) : null;
  console.log(
    `[OpenStreetMapProvider]
endpoint health:
primary=${primary.health}
${secondary ? `secondary=${secondary.health}` : ""}`.trim()
  );
}
var TOURISM_TO_TYPE = {
  attraction: "tourist_attraction",
  museum: "museum",
  viewpoint: "viewpoint",
  gallery: "art_gallery",
  hotel: "lodging",
  hostel: "lodging",
  guest_house: "lodging",
  camp_site: "campground",
  caravan_site: "campground",
  information: "point_of_interest",
  theme_park: "theme_park",
  zoo: "zoo",
  aquarium: "aquarium",
  picnic_site: "park"
};
var NATURAL_TO_TYPE = {
  beach: "beach",
  peak: "mountain",
  volcano: "mountain",
  waterfall: "waterfall",
  cave_entrance: "mountain",
  hot_spring: "natural_feature",
  bay: "beach",
  lake: "lake",
  wood: "forest",
  protected_area: "national_park",
  water: "lake",
  spring: "natural_feature",
  hill: "mountain"
};
var HISTORIC_TO_TYPE = {
  monument: "monument",
  memorial: "monument",
  castle: "castle",
  ruins: "archaeological_site",
  archaeological_site: "archaeological_site",
  fort: "castle",
  building: "historic_site",
  church: "place_of_worship",
  cathedral: "place_of_worship",
  monastery: "historic_site"
};
var LEISURE_TO_TYPE = {
  park: "park",
  nature_reserve: "nature_reserve",
  marina: "marina",
  garden: "park",
  dog_park: "park",
  playground: "park",
  water_park: "water_park",
  golf_course: "entertainment"
};
var AMENITY_TO_TYPE = {
  restaurant: "restaurant",
  cafe: "cafe",
  bar: "bar",
  pub: "bar",
  fast_food: "restaurant",
  cinema: "movie_theater",
  theatre: "performing_arts_theater",
  nightclub: "night_club",
  bus_station: "bus_station",
  casino: "casino",
  market: "market"
};
var CATEGORY_TO_TAG_PREDICATE = {
  attractions: '["tourism"]',
  restaurants: '["amenity"~"restaurant|cafe|fast_food|bar|pub"]',
  hotels: '["tourism"~"hotel|hostel|guest_house|apartment"]',
  nature: '["natural"]',
  culture: '["historic"]',
  entertainment: '["amenity"~"cinema|theatre|nightclub"]',
  landmarks: '["tourism"~"attraction|viewpoint"]',
  beaches: '["natural"="beach"]',
  museums: '["tourism"="museum"]',
  parks: '["leisure"="park"]'
};
var cache = /* @__PURE__ */ new Map();
function cacheKey(lat, lng, radiusKm, category) {
  const latBucket = Math.round(lat * 2) / 2;
  const lngBucket = Math.round(lng * 2) / 2;
  return `osm:${latBucket}:${lngBucket}:${radiusKm}:${category || "all"}`;
}
function readCache(lat, lng, radiusKm, category) {
  const key = cacheKey(lat, lng, radiusKm, category);
  const entry = cache.get(key);
  if (!entry) return { value: null, isStale: false };
  const age = Date.now() - entry.fetchedAt;
  return {
    value: entry.value,
    isStale: age > (entry.ttlMs ?? CACHE_TTL_MS)
  };
}
function writeCache(lat, lng, radiusKm, category, value, ttlMs) {
  const key = cacheKey(lat, lng, radiusKm, category);
  if (cache.size > 400) {
    const oldest = cache.keys().next().value;
    if (oldest) cache.delete(oldest);
  }
  cache.set(key, { value, fetchedAt: Date.now(), ttlMs });
}
var lastRequestAt = 0;
var joinStats = { inFlightHits: 0 };
var lastOutcome = {
  status: "IDLE",
  durationMs: 0
};
function getLastOsmOutcome() {
  return lastOutcome;
}
function getOsmJoinStats() {
  return joinStats.inFlightHits;
}
function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}
async function fetchOnce(url, body, timeoutMs, parentSignal) {
  const now = Date.now();
  const elapsed = now - lastRequestAt;
  if (elapsed < MIN_REQUEST_GAP_MS) {
    await sleep(MIN_REQUEST_GAP_MS - elapsed);
  }
  lastRequestAt = Date.now();
  const ctrl = new AbortController();
  const onAbort = () => ctrl.abort();
  parentSignal.addEventListener("abort", onAbort, { once: true });
  const timer = setTimeout(() => ctrl.abort(), timeoutMs);
  try {
    return await fetch(url, {
      method: "POST",
      headers: {
        "Content-Type": "application/x-www-form-urlencoded",
        // Some carrier proxies / middleboxes return 406 when no Accept header
        // is present on a POST with a urlencoded body. Be explicit.
        "Accept": "application/json",
        // REQUIRED: Overpass API usage policy asks for an identifiable UA, and
        // LIVE VERIFICATION showed that requests WITHOUT one are rejected with
        // HTTP 406 (bot detection) by overpass-api.de. Do not remove.
        "User-Agent": "TICS/1.0 (React Native; https://tics.app)"
      },
      body: `data=${encodeURIComponent(body)}`,
      signal: ctrl.signal
    });
  } finally {
    clearTimeout(timer);
    parentSignal.removeEventListener("abort", onAbort);
  }
}
async function fetchOnceGet(url, body, timeoutMs, parentSignal) {
  const ctrl = new AbortController();
  const onAbort = () => ctrl.abort();
  parentSignal.addEventListener("abort", onAbort, { once: true });
  const timer = setTimeout(() => ctrl.abort(), timeoutMs);
  try {
    return await fetch(`${url}?data=${encodeURIComponent(body)}`, {
      method: "GET",
      headers: { "Accept": "application/json" },
      signal: ctrl.signal
    });
  } finally {
    clearTimeout(timer);
    parentSignal.removeEventListener("abort", onAbort);
  }
}
var OVERPASS_MIN_GAP_MS = 1200;
var overpassChain = Promise.resolve();
var lastOverpassRequestAt = 0;
async function acquireOverpassSlot() {
  const wait = Math.max(
    0,
    lastOverpassRequestAt + OVERPASS_MIN_GAP_MS - Date.now()
  );
  const chained = overpassChain.then(async () => {
    if (wait > 0) await new Promise((r) => setTimeout(r, wait));
  });
  overpassChain = chained.catch(() => void 0);
  await chained;
  lastOverpassRequestAt = Date.now();
  return () => {
    lastOverpassRequestAt = Date.now();
  };
}
async function fetchOverpass(query2, options) {
  const budgetMs = options?.budgetMs ?? LOCAL_BUDGET_MS;
  const startedAt = Date.now();
  const parentCtrl = new AbortController();
  const deadlineTimer = setTimeout(() => parentCtrl.abort(), budgetMs);
  let lastError = null;
  let getFallbackUsed = false;
  try {
    for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
      for (const url of orderedEndpoints()) {
        const remaining = budgetMs - (Date.now() - startedAt);
        if (remaining <= 200 || parentCtrl.signal.aborted) break;
        const attemptStart = Date.now();
        try {
          const release = await acquireOverpassSlot();
          let response;
          try {
            response = await fetchOnce(
              url,
              query2,
              Math.min(PER_REQUEST_CAP_MS, remaining),
              parentCtrl.signal
            );
            if ((response.status === 406 || response.status >= 500) && !parentCtrl.signal.aborted && !getFallbackUsed) {
              getFallbackUsed = true;
              console.log(
                `[OpenStreetMapProvider] endpoint=${shortHost(url)} POST status=${response.status} \u2014 trying GET fallback (once per run)`
              );
              response = await fetchOnceGet(
                url,
                query2,
                Math.min(PER_REQUEST_CAP_MS, budgetMs - (Date.now() - startedAt)),
                parentCtrl.signal
              );
            }
          } finally {
            release();
          }
          if (!response.ok) {
            lastError = new Error(`Overpass HTTP ${response.status}`);
            recordEndpointFailure(url);
            console.log(
              `[OSM DEBUG]
endpoint: ${shortHost(url)}
method: POST(+GET fallback)
status: ${response.status}
query length: ${query2.length}
timeout: ${Math.min(PER_REQUEST_CAP_MS, remaining)}ms
durationMs: ${Date.now() - attemptStart}
parsed element count: 0
error: HTTP ${response.status}`
            );
            continue;
          }
          const data = await response.json();
          recordEndpointSuccess(url);
          const elements = Array.isArray(data?.elements) ? data.elements : [];
          console.log(
            `[OSM DEBUG]
endpoint: ${shortHost(url)}
method: POST
status: 200
query length: ${query2.length}
durationMs: ${Date.now() - attemptStart}
response size: ${JSON.stringify(data).length}
parsed element count: ${elements.length}
error: none`
          );
          return elements;
        } catch (err) {
          lastError = err;
          recordEndpointFailure(url);
          const kind = isAbort(err) ? "AbortError" : "NetworkError";
          console.log(
            `[OpenStreetMapProvider] endpoint=${shortHost(url)} attempt=${attempt + 1} status=${kind} durationMs=${Date.now() - attemptStart} resultCount=0`
          );
          if (parentCtrl.signal.aborted) break;
        }
      }
      if (parentCtrl.signal.aborted) break;
      const wait = BACKOFF_BASE_MS * Math.pow(2, attempt);
      if (budgetMs - (Date.now() - startedAt) <= wait) break;
      await sleep(wait);
    }
    lastOutcome = {
      status: parentCtrl.signal.aborted ? "TIMEOUT" : "FAILED",
      durationMs: Date.now() - startedAt
    };
    console.warn(
      `[OpenStreetMapProvider] ${lastOutcome.status} after ${lastOutcome.durationMs}ms (budget ${budgetMs}ms):`,
      lastError instanceof Error ? lastError.message : lastError ?? "no response"
    );
    logEndpointHealth();
    return [];
  } finally {
    clearTimeout(deadlineTimer);
    if (!parentCtrl.signal.aborted && Date.now() - startedAt >= budgetMs) {
      parentCtrl.abort();
    }
  }
}
function shortHost(url) {
  try {
    return new URL(url).host;
  } catch {
    return url;
  }
}
function isAbort(err) {
  return Boolean(err) && typeof err === "object" && err.name === "AbortError";
}
function readStaleCache(lat, lng, radiusKm, category) {
  const key = cacheKey(lat, lng, radiusKm, category);
  const entry = cache.get(key);
  if (!entry) return null;
  if (Date.now() - entry.fetchedAt > STALE_TTL_MS) return null;
  return entry.value;
}
function toBBox(lat, lng, radiusKm) {
  const latDeg = radiusKm / 111;
  const lngDeg = radiusKm / (111 * Math.max(Math.cos(lat * Math.PI / 180), 0.2));
  const s = lat - latDeg;
  const w = lng - lngDeg;
  const n = lat + latDeg;
  const e = lng + lngDeg;
  return `${s.toFixed(5)},${w.toFixed(5)},${n.toFixed(5)},${e.toFixed(5)}`;
}
function travelUnionBlock(bbox, predicate) {
  if (predicate) {
    return `
    (
      node${predicate}(${bbox});
      way${predicate}(${bbox});
      relation${predicate}(${bbox});
    );`;
  }
  return `
    (
      node["tourism"](${bbox});
      way["tourism"](${bbox});
      relation["tourism"](${bbox});
      node["natural"](${bbox});
      way["natural"](${bbox});
      node["historic"](${bbox});
      way["historic"](${bbox});
      relation["historic"](${bbox});
      node["leisure"~"^(park|nature_reserve|marina|garden)$"](${bbox});
      way["leisure"~"^(park|nature_reserve|marina|garden)$"](${bbox});
      node["amenity"="restaurant"](${bbox});
      node["amenity"="cafe"](${bbox});
      node["amenity"="bar"](${bbox});
      node["amenity"="pub"](${bbox});
      node["aeroway"="aerodrome"](${bbox});
      node["railway"="station"](${bbox});
      node["tourism"="hotel"](${bbox});
    );`;
}
function buildNearbyQuery(lat, lng, radiusKm, category) {
  const bbox = toBBox(lat, lng, radiusKm);
  const predicate = category && CATEGORY_TO_TAG_PREDICATE[category] ? CATEGORY_TO_TAG_PREDICATE[category] : void 0;
  return `[out:json][timeout:30];${travelUnionBlock(bbox, predicate)}out center ${DEFAULT_LIMIT};`;
}
function buildSearchQuery(query2, country) {
  const esc = query2.replace(/"/g, "");
  const countryFilter = country ? `["addr:country"="${country}"]` : "";
  return `[out:json][timeout:25];
  (
    node["name"~"${esc}",i]${countryFilter};
    way["name"~"${esc}",i]${countryFilter};
    relation["name"~"${esc}",i]${countryFilter};
  );
  out center 25;`;
}
function buildByIdQuery(elmType, id) {
  return `[out:json][timeout:20];${elmType}(${id});out center;`;
}
function haversine(lat1, lng1, lat2, lng2) {
  const R = 6371;
  const dLat = (lat2 - lat1) * Math.PI / 180;
  const dLng = (lng2 - lng1) * Math.PI / 180;
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLng / 2) * Math.sin(dLng / 2);
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
function resolveType(tags) {
  if (tags.tourism && TOURISM_TO_TYPE[tags.tourism]) return TOURISM_TO_TYPE[tags.tourism];
  if (tags.natural && NATURAL_TO_TYPE[tags.natural]) return NATURAL_TO_TYPE[tags.natural];
  if (tags.historic && HISTORIC_TO_TYPE[tags.historic]) return HISTORIC_TO_TYPE[tags.historic];
  if (tags.leisure && LEISURE_TO_TYPE[tags.leisure]) return LEISURE_TO_TYPE[tags.leisure];
  if (tags.amenity && AMENITY_TO_TYPE[tags.amenity]) return AMENITY_TO_TYPE[tags.amenity];
  if (tags.aeroway === "aerodrome") return "airport";
  if (tags.railway === "station") return "train_station";
  return null;
}
function travelCategoryFromType(type) {
  if (["beach", "waterfall", "lake", "mountain", "nature_reserve", "forest", "national_park", "natural_feature", "viewpoint", "island"].includes(type)) return "Nature";
  if (["museum", "art_gallery", "castle", "monument", "historic_site", "archaeological_site", "place_of_worship"].includes(type)) return "Culture";
  if (["zoo", "aquarium", "theme_park", "amusement_park", "entertainment", "movie_theater", "night_club"].includes(type)) return "Entertainment";
  if (["restaurant", "cafe", "bar"].includes(type)) return "Food";
  if (["lodging", "hotel"].includes(type)) return "Accommodation";
  if (["airport", "train_station", "bus_station"].includes(type)) return "Transport";
  if (["park"].includes(type)) return "Parks & Outdoors";
  return "Attraction";
}
function collectTags(tags, type) {
  const out = /* @__PURE__ */ new Set([type]);
  const keys = ["tourism", "natural", "historic", "leisure", "amenity", "aeroway", "railway", "landuse", "waterway", "boundary", "man_made"];
  for (const k of keys) if (tags[k]) out.add(tags[k]);
  out.add(travelCategoryFromType(type));
  return Array.from(out);
}
function computeQuality(tags) {
  let q = 0.45;
  if (tags.website || tags["contact:website"]) q += 0.15;
  if (tags.opening_hours) q += 0.1;
  if (tags.wikipedia) q += 0.1;
  if (tags.wikidata) q += 0.1;
  if (tags.phone || tags["contact:phone"]) q += 0.05;
  if (tags.description) q += 0.05;
  return Math.min(1, Math.max(0, q));
}
function elementToDestination(el, centerLat, centerLng) {
  const tags = el.tags || {};
  const name = tags.name?.trim();
  if (!name) return null;
  const lat = el.lat ?? el.center?.lat;
  const lon = el.lon ?? el.center?.lon;
  if (typeof lat !== "number" || typeof lon !== "number") return null;
  if (!isValidCoordinate(lat, lon)) return null;
  const type = resolveType(tags);
  if (!type) return null;
  return {
    id: buildTicsId("openstreetmap", `${el.type}:${el.id}`),
    sourceId: `${el.type}/${el.id}`,
    name,
    latitude: lat,
    longitude: lon,
    description: tags.description || void 0,
    categories: [travelCategoryFromType(type)],
    tags: collectTags(tags, type),
    type,
    website: tags.website || tags["contact:website"] || void 0,
    phone: tags.phone || tags["contact:phone"] || void 0,
    openingHours: tags.opening_hours || void 0,
    imageUrl: void 0,
    rating: void 0,
    reviewCount: void 0,
    distanceKm: haversine(centerLat, centerLng, lat, lon),
    isOpen: void 0,
    source: "openstreetmap",
    sourceConfidence: 0.6,
    qualityScore: computeQuality(tags),
    metadata: {
      osmType: el.type,
      osmId: el.id,
      wikipedia: tags.wikipedia,
      wikidata: tags.wikidata
    }
  };
}
function normalizeName(name) {
  return name.toLowerCase().replace(/^(the|a|an)\s+/, "").replace(/[^a-z0-9]+/g, "");
}
function dedupe(dests) {
  const seen = /* @__PURE__ */ new Map();
  for (const d of dests) {
    const key = `${normalizeName(d.name)}_${d.latitude.toFixed(2)}_${d.longitude.toFixed(2)}`;
    const existing = seen.get(key);
    if (!existing || (d.qualityScore || 0) > (existing.qualityScore || 0)) {
      seen.set(key, d);
    }
  }
  return Array.from(seen.values());
}
function applyLimit(dests, limit2) {
  const n = limit2 && limit2 > 0 ? limit2 : DEFAULT_LIMIT;
  return dests.slice(0, n);
}
var inFlight = /* @__PURE__ */ new Map();
async function refreshNearby(lat, lng, radiusKm, category, limit2, budgetMs = LOCAL_BUDGET_MS) {
  const key = cacheKey(lat, lng, radiusKm, category);
  const existing = inFlight.get(key);
  if (existing) {
    joinStats.inFlightHits += 1;
    console.log("[OpenStreetMapProvider] in-flight HIT \u2014 joining existing request");
    return existing;
  }
  const promise = (async () => {
    const startedAt = Date.now();
    try {
      const query2 = buildNearbyQuery(lat, lng, radiusKm, category);
      const elements = await fetchOverpass(query2, { budgetMs });
      if (elements.length === 0) {
        if (getLastOsmOutcome().status === "IDLE") {
          lastOutcome = { status: "SUCCESS_EMPTY", durationMs: Date.now() - startedAt };
        }
      } else {
        lastOutcome = { status: "SUCCESS_WITH_DATA", durationMs: Date.now() - startedAt };
      }
      const dests = elements.map((el) => elementToDestination(el, lat, lng)).filter((d) => d !== null);
      const unique = dedupe(dests).sort(
        (a, b) => (a.distanceKm ?? 1e9) - (b.distanceKm ?? 1e9)
      );
      writeCache(
        lat,
        lng,
        radiusKm,
        category,
        unique,
        unique.length === 0 ? EMPTY_RESULT_TTL_MS : void 0
      );
      console.log(
        `[OpenStreetMapProvider] Query: nearby | Center: ${lat.toFixed(4)},${lng.toFixed(4)} | Radius: ${radiusKm}km | Results: ${unique.length} | durationMs=${Date.now() - startedAt}`
      );
      return applyLimit(unique, limit2);
    } catch (err) {
      console.warn(
        "[OpenStreetMapProvider] ERROR nearby fetch failed:",
        err instanceof Error ? err.message : err
      );
      lastOutcome = { status: "FAILED", durationMs: Date.now() - startedAt };
      const stale = readStaleCache(lat, lng, radiusKm, category);
      if (stale && stale.length > 0) {
        console.log(`[OpenStreetMapProvider] serving STALE cache (${stale.length}) after failure`);
        return applyLimit(stale, limit2);
      }
      return [];
    }
  })();
  inFlight.set(key, promise);
  try {
    return await promise;
  } finally {
    inFlight.delete(key);
  }
}
var OpenStreetMapProvider = {
  name: "openstreetmap",
  kind: "primary",
  isAvailable() {
    return true;
  },
  async searchNearby({
    lat,
    lng,
    radiusKm = 50,
    limit: limit2,
    category,
    budgetMs
  }) {
    if (!isValidUserLocation(lat, lng)) {
      console.warn(
        `[OpenStreetMapProvider] rejected invalid user location lat=${lat} lng=${lng} \u2014 skipping OSM query`
      );
      return [];
    }
    const radius = Math.min(Math.max(radiusKm || 50, 1), MAX_RADIUS_KM);
    const cached = readCache(lat, lng, radius, category);
    if (cached.value) {
      if (cached.isStale && cached.value.length === 0) {
        console.log("[OpenStreetMapProvider] cache STALE-EMPTY \u2014 refetching live");
        return refreshNearby(lat, lng, radius, category, limit2, budgetMs ?? LOCAL_BUDGET_MS);
      }
      if (cached.isStale) {
        console.log(`[OpenStreetMapProvider] cache STALE \u2014 serving stale and refreshing (${cached.value.length})`);
        void refreshNearby(lat, lng, radius, category, limit2, budgetMs ?? LOCAL_BUDGET_MS);
      } else {
        console.log(`[OpenStreetMapProvider] cache HIT (${cached.value.length})`);
      }
      return applyLimit(cached.value, limit2);
    }
    console.log("[OpenStreetMapProvider] cache MISS \u2014 fetching");
    return refreshNearby(lat, lng, radius, category, limit2, budgetMs ?? LOCAL_BUDGET_MS);
  },
  async searchByQuery(query2, options = {}) {
    if (!query2 || query2.trim().length < 2) return [];
    try {
      const ql = buildSearchQuery(query2.trim(), options.country);
      const elements = await fetchOverpass(ql);
      const dests = elements.map((el) => elementToDestination(el, 0, 0)).filter((d) => d !== null);
      const unique = dedupe(dests);
      console.log(`[OpenStreetMapProvider] Query: search "${query2}" | Results: ${unique.length}`);
      return applyLimit(unique, options.limit || 20);
    } catch (err) {
      console.warn("[OpenStreetMapProvider] ERROR search failed:", err);
      return [];
    }
  },
  async getById(id) {
    const parts = id.split(":");
    const elmType = parts[parts.length - 2];
    const rawId = parts[parts.length - 1];
    if (elmType !== "node" && elmType !== "way" && elmType !== "relation" || !rawId) {
      return null;
    }
    try {
      const elements = await fetchOverpass(buildByIdQuery(elmType, Number(rawId)));
      const el = elements[0];
      if (!el) return null;
      return elementToDestination(el, el.lat ?? el.center?.lat ?? 0, el.lon ?? el.center?.lon ?? 0);
    } catch {
      return null;
    }
  }
};
function clearOpenStreetMapCache() {
  cache.clear();
  inFlight.clear();
}

// src/modules/explore/services/ExploreService.ts
var import_firestore2 = __toESM(require_firestore());

// src/firebase/firebaseApp.ts
var import_app = __toESM(require_app());
var import_async_storage = __toESM(require_async_storage());
var import_react_native = __toESM(require_react_native());
var import_auth = __toESM(require_auth());
var import_firestore = __toESM(require_firestore());
var import_functions = __toESM(require_functions());
var import_storage = __toESM(require_storage());
var firebaseConfig = {
  apiKey: process.env.EXPO_PUBLIC_FIREBASE_API_KEY,
  authDomain: process.env.EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.EXPO_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: process.env.EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.EXPO_PUBLIC_FIREBASE_APP_ID
};
console.log("Firebase config:", {
  apiKey: process.env.EXPO_PUBLIC_FIREBASE_API_KEY,
  projectId: process.env.EXPO_PUBLIC_FIREBASE_PROJECT_ID,
  appId: process.env.EXPO_PUBLIC_FIREBASE_APP_ID
});
function assertFirebaseConfig() {
  const missing = Object.entries(firebaseConfig).filter(([, v]) => !v).map(([k]) => k);
  if (missing.length) {
    throw new Error(
      `[TICS] Missing Firebase config env vars: ${missing.join(", ")}
Copy .env.example \u2192 .env and fill in all EXPO_PUBLIC_FIREBASE_* values.`
    );
  }
}
var _app = null;
function getFirebaseApp() {
  if (_app) return _app;
  if ((0, import_app.getApps)().length) {
    _app = (0, import_app.getApps)()[0];
    return _app;
  }
  assertFirebaseConfig();
  console.log("Firebase Config", firebaseConfig);
  _app = (0, import_app.initializeApp)(firebaseConfig);
  return _app;
}
function getFirebaseFirestore() {
  return (0, import_firestore.getFirestore)(getFirebaseApp());
}

// src/modules/explore/constants/index.ts
var FIRESTORE_COLLECTIONS = {
  EXPLORE_CATEGORIES: "exploreCategories",
  DESTINATIONS: "destinations",
  EVENTS: "events",
  RECOMMENDATIONS: "recommendations",
  TRENDING: "trending",
  SAVED_PLACES: "savedPlaces",
  SEARCH_HISTORY: "searchHistory",
  JOURNEY_FEED: "journeyFeed",
  NEARBY_PLACES: "nearbyPlaces",
  EXPLORE_ANALYTICS: "exploreAnalytics"
};
var CACHE_TTL = {
  CATEGORIES: 24 * 60 * 60 * 1e3,
  // 24 hours
  TRENDING: 6 * 60 * 60 * 1e3,
  // 6 hours
  DESTINATIONS: 12 * 60 * 60 * 1e3,
  // 12 hours
  EVENTS: 6 * 60 * 60 * 1e3,
  // 6 hours
  RECENTLY_VIEWED: 7 * 24 * 60 * 60 * 1e3,
  // 7 days
  SEARCH_HISTORY: 30 * 24 * 60 * 60 * 1e3,
  // 30 days
  JOURNEY_FEED: 1 * 60 * 60 * 1e3,
  // 1 hour
  NEARBY: 30 * 60 * 1e3
  // 30 minutes
};
var PAGINATION = {
  DEFAULT_PAGE_SIZE: 10,
  MAX_PAGE_SIZE: 50,
  TRENDING_LIMIT: 20,
  POPULAR_LIMIT: 20,
  FEATURED_LIMIT: 10,
  RECENTLY_VIEWED_LIMIT: 20,
  NEARBY_LIMIT: 30,
  FEED_LIMIT: 20,
  SEARCH_LIMIT: 10,
  AUTOCOMPLETE_LIMIT: 5
};

// src/modules/explore/services/discovery/EventCache.ts
var eventCache = /* @__PURE__ */ new Map();
var MAX_CACHE_SIZE = 200;
var EventCache = {
  set(event) {
    if (eventCache.size >= MAX_CACHE_SIZE) {
      const firstKey = eventCache.keys().next().value;
      if (firstKey) eventCache.delete(firstKey);
    }
    eventCache.set(event.id, event);
  },
  setMany(events) {
    for (const event of events) {
      this.set(event);
    }
  },
  get(id) {
    return eventCache.get(id);
  },
  clear() {
    eventCache.clear();
  }
};

// src/modules/explore/utils/withTimeout.ts
function withTimeout(promise, ms, label) {
  const startedAt = Date.now();
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => {
      console.warn(
        `[OSM TIMEOUT TRACE]
caller: ${label}
timeout configured: ${ms}ms
timeout actually applied: ${ms}ms
elapsed: ${Date.now() - startedAt}ms
abort signal: none (promise detached, keeps running)
abort reason: section budget exceeded \u2014 underlying OSM promise continues`
      );
      const err = new Error(`[${label}] Timed out after ${ms}ms`);
      err.name = "TimeoutError";
      reject(err);
    }, ms);
    promise.then(
      (value) => {
        clearTimeout(timer);
        resolve(value);
      },
      (err) => {
        clearTimeout(timer);
        reject(err);
      }
    );
  });
}
async function withTimeoutFallback(promise, ms, label, fallback) {
  try {
    return await withTimeout(promise, ms, label);
  } catch (err) {
    if (err?.name === "TimeoutError") {
      console.warn(`[withTimeout] ${label} timed out after ${ms}ms \u2014 using fallback`);
    }
    return fallback;
  }
}
var PROVIDER_TIMEOUTS = {
  GOOGLE_PLACES: 5e3,
  // Overpass LIVE MEASUREMENT: 2s (healthy primary) to ~16-28s (failover to
  // the slow mirror). 12s aborted healthy requests; sized to the provider's
  // LOCAL_BUDGET_MS so the two never disagree.
  OPEN_STREET_MAP: 32e3,
  TICKETMASTER: 5e3,
  // 5 seconds
  EVENTBRITE: 5e3,
  // 5 seconds
  WEATHER: 3e3,
  // 3 seconds
  GEMINI: 5e3,
  // 5 seconds
  FIRESTORE: 5e3
  // 5 seconds
};

// src/modules/explore/services/discovery/EventsProvider.ts
var TICKETMASTER_API_BASE = "https://app.ticketmaster.com/discovery/v2/events";
var EVENT_CATEGORIES = [
  "Music",
  "Festivals",
  "Food Festivals",
  "Sports",
  "Comedy",
  "Theatre",
  "Family",
  "Business",
  "Community",
  "Art",
  "Culture",
  "Networking"
];
function getTicketmasterKey() {
  return process.env.EXPO_PUBLIC_TICKETMASTER_API_KEY || "";
}
function haversineDistance(from, to) {
  const R = 6371;
  const dLat = toRad(to.lat - from.lat);
  const dLon = toRad(to.lng - from.lng);
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(toRad(from.lat)) * Math.cos(toRad(to.lat)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function toRad(deg) {
  return deg * (Math.PI / 180);
}
function mapCategory(name) {
  const lower = (name || "").toLowerCase();
  if (lower.includes("music") || lower.includes("concert")) return "Music";
  if (lower.includes("festival") || lower.includes("food")) return "Festivals";
  if (lower.includes("sport")) return "Sports";
  if (lower.includes("comedy")) return "Comedy";
  if (lower.includes("theatre") || lower.includes("theater") || lower.includes("drama")) return "Theatre";
  if (lower.includes("family") || lower.includes("kids")) return "Family";
  if (lower.includes("business") || lower.includes("conference") || lower.includes("networking")) return "Business";
  if (lower.includes("community")) return "Community";
  if (lower.includes("art") || lower.includes("exhibition")) return "Art";
  if (lower.includes("culture") || lower.includes("heritage")) return "Culture";
  return "Entertainment";
}
async function fetchFromTicketmaster(lat, lng, radius, startDate, endDate) {
  const key = getTicketmasterKey();
  if (!key) return [];
  const effectiveRadius = Math.max(radius, 500);
  const attemptFetch = async (signal) => {
    const params = new URLSearchParams({
      apikey: key,
      latlong: `${lat},${lng}`,
      radius: String(effectiveRadius),
      unit: "km",
      size: "50",
      sort: "date,asc"
    });
    if (startDate) params.set("startDateTime", startDate);
    if (endDate) params.set("endDateTime", endDate);
    const response = await fetch(`${TICKETMASTER_API_BASE}.json?${params}`, {
      headers: { "Accept": "application/json" },
      signal
    });
    if (!response.ok) {
      console.warn(`[EventsProvider] Ticketmaster HTTP ${response.status}`);
      return [];
    }
    const data = await response.json();
    return data._embedded?.events || [];
  };
  for (let attempt = 1; attempt <= 2; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), PROVIDER_TIMEOUTS.TICKETMASTER);
      const events = await attemptFetch(controller.signal);
      clearTimeout(timeoutId);
      if (attempt > 1) {
        console.log(`[EventsProvider] Ticketmaster succeeded on retry attempt ${attempt}`);
      }
      console.log(`[EventsProvider] Ticketmaster query: latlong=${lat},${lng} radius=${effectiveRadius}km`);
      console.log(`[EventsProvider] Ticketmaster returned ${events.length} raw events`);
      return events;
    } catch (err) {
      const isAbort2 = err?.name === "AbortError" || err?.code === 20;
      if (isAbort2 && attempt === 1) {
        console.warn(`[EventsProvider] Ticketmaster timeout on attempt ${attempt}, retrying...`);
        continue;
      }
      if (isAbort2) {
        console.error(`[EventsProvider] Ticketmaster timeout after ${attempt} attempts \u2014 giving up.`);
      } else {
        console.error("[EventsProvider] Ticketmaster error:", err);
      }
      return [];
    }
  }
  return [];
}
async function fetchFromTicketmasterGlobal(startDate, endDate) {
  const key = getTicketmasterKey();
  if (!key) return [];
  const attemptFetch = async (signal, keyword) => {
    const params = new URLSearchParams({
      apikey: key,
      size: "50",
      sort: "date,asc"
    });
    if (keyword) params.set("keyword", keyword);
    if (startDate) params.set("startDateTime", startDate);
    if (endDate) params.set("endDateTime", endDate);
    const response = await fetch(`${TICKETMASTER_API_BASE}.json?${params}`, {
      headers: { "Accept": "application/json" },
      signal
    });
    if (!response.ok) {
      console.warn(`[EventsProvider] Ticketmaster global HTTP ${response.status}`);
      return [];
    }
    const data = await response.json();
    return data._embedded?.events || [];
  };
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), PROVIDER_TIMEOUTS.TICKETMASTER);
  try {
    const events = await attemptFetch(controller.signal, "festival");
    clearTimeout(timeoutId);
    console.log(`[EventsProvider] Ticketmaster global query (keyword=festival) returned ${events.length} raw events`);
    if (events.length >= 5) {
      return events;
    }
    console.log(`[EventsProvider] Too few global festival events (${events.length}), trying broader query...`);
    const allEvents = await attemptFetch(controller.signal, void 0);
    console.log(`[EventsProvider] Ticketmaster global query (no keyword) returned ${allEvents.length} raw events`);
    return allEvents.length > events.length ? allEvents : events;
  } catch (err) {
    clearTimeout(timeoutId);
    const isAbort2 = err?.name === "AbortError" || err?.code === 20;
    if (isAbort2) {
      console.warn("[EventsProvider] Ticketmaster global timeout");
    } else {
      console.error("[EventsProvider] Ticketmaster global error:", err);
    }
    return [];
  }
}
function transformTicketmasterEvent(e) {
  const venue = e._embedded?.venues?.[0];
  const attraction = e._embedded?.attractions?.[0];
  const coords = {
    lat: parseFloat(venue?.location?.latitude || "0"),
    lng: parseFloat(venue?.location?.longitude || "0")
  };
  const classification = e.classifications?.[0];
  const priceRange = e.priceRanges?.[0];
  if (!coords.lat || !coords.lng) return null;
  return {
    id: `tm_${e.id}`,
    title: e.name,
    slug: e.name.toLowerCase().replace(/[^a-z0-9]+/g, "-"),
    description: e.description || "",
    shortDescription: classification?.genre?.name || "",
    category: mapCategory(classification?.segment?.name || ""),
    subcategory: classification?.genre?.name || "",
    country: venue?.country?.name || "",
    city: venue?.city?.name || "",
    coordinates: coords,
    venue: {
      name: venue?.name || "",
      address: venue?.address?.line1 || "",
      coordinates: coords,
      capacity: 0,
      website: e.url,
      phone: ""
    },
    images: attraction?.images?.length ? [{ url: attraction.images[0].url, caption: "", credit: "" }] : [{ url: "https://images.unsplash.com/photo-1492684223066-81342ee5ff30?w=400", caption: "", credit: "" }],
    startDate: new Date(e.dates.start.dateTime || e.dates.start.localDate),
    endDate: e.dates.end ? new Date(e.dates.end.dateTime || e.dates.end.localDate) : new Date(e.dates.start.dateTime || e.dates.start.localDate),
    ticketUrl: e.url,
    ticketPrice: priceRange ? `${priceRange.currency} ${priceRange.min} - ${priceRange.max}` : "",
    organizer: classification?.segment?.name || "",
    website: e.url,
    popularity: 50,
    rating: 4.2,
    destinationId: "",
    relatedEventIds: [],
    tags: [classification?.segment?.name || "", classification?.genre?.name || ""].filter(Boolean),
    featured: false,
    trending: false,
    active: true,
    createdAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date()
  };
}
var EventsProvider = {
  /**
   * Fetch events from Ticketmaster Discovery API.
   *
   * @param lat - User's latitude
   * @param lng - User's longitude
   * @param radiusKm - Search radius in km
   * @param dateRange - Optional date range filter
   * @param global - If true, fetch global events (ignores lat/lng)
   * @returns Sorted, scored events
   */
  async getEvents(lat, lng, radiusKm = 100, dateRange, global2 = false) {
    const useLocation = !global2 && typeof lat === "number" && typeof lng === "number";
    const userLocation = useLocation ? { lat, lng } : null;
    console.log(`[EventsProvider] getEvents: global=${global2}, useLocation=${useLocation}, lat=${lat}, lng=${lng}, radius=${radiusKm}km`);
    const rawEvents = global2 ? await fetchFromTicketmasterGlobal(dateRange?.start, dateRange?.end) : await fetchFromTicketmaster(lat, lng, radiusKm, dateRange?.start, dateRange?.end);
    const ticketmasterEvents = rawEvents.map(transformTicketmasterEvent).filter((e) => e !== null);
    console.log(`[EventsProvider] Transformed ${ticketmasterEvents.length} events`);
    if (ticketmasterEvents.length === 0) {
      console.warn(`[EventsProvider] \u26A0 No events returned from Ticketmaster! API key: ${getTicketmasterKey() ? "PRESENT" : "MISSING"}`);
      return {
        events: [],
        provider: "ticketmaster",
        fetchedAt: Date.now()
      };
    }
    const scored = ticketmasterEvents.map((event) => {
      let distanceScore = 50;
      if (userLocation) {
        const distance = haversineDistance(userLocation, event.coordinates);
        distanceScore = Math.max(0, 100 - Math.round(distance / 2));
      }
      const timeUntilEvent = event.startDate.getTime() - Date.now();
      const urgencyScore = timeUntilEvent > 0 ? Math.max(0, 100 - Math.round(timeUntilEvent / (1e3 * 60 * 60 * 24))) : 0;
      const eventScore = Math.round(
        distanceScore * 0.3 + 50 * 0.25 + 42 * 0.25 + urgencyScore * 0.2
      );
      return { ...event, popularity: eventScore };
    }).sort((a, b) => b.popularity - a.popularity);
    const countryCounts = {};
    for (const e of scored) {
      const c = e.country || "Unknown";
      countryCounts[c] = (countryCounts[c] || 0) + 1;
    }
    console.log(`[EventsProvider] Events by country:`, countryCounts);
    console.log(`[EventsProvider] Fetched ${scored.length} events from Ticketmaster`);
    if (scored.length > 0) {
      console.log(`[EventsProvider] Sample events:`, scored.slice(0, 5).map((e) => `${e.title} (${e.city}, ${e.country})`));
    }
    EventCache.setMany(scored.slice(0, 20));
    return {
      events: scored.slice(0, 20),
      provider: "ticketmaster",
      fetchedAt: Date.now()
    };
  },
  /**
   * Get supported event categories.
   */
  getSupportedCategories() {
    return EVENT_CATEGORIES;
  }
};

// node_modules/@tanstack/query-core/build/modern/subscribable.js
var Subscribable = class {
  constructor() {
    this.listeners = /* @__PURE__ */ new Set();
    this.subscribe = this.subscribe.bind(this);
  }
  subscribe(listener) {
    this.listeners.add(listener);
    this.onSubscribe();
    return () => {
      this.listeners.delete(listener);
      this.onUnsubscribe();
    };
  }
  hasListeners() {
    return this.listeners.size > 0;
  }
  onSubscribe() {
  }
  onUnsubscribe() {
  }
};

// node_modules/@tanstack/query-core/build/modern/focusManager.js
var FocusManager = class extends Subscribable {
  #focused;
  #cleanup;
  #setup;
  constructor() {
    super();
    this.#setup = (onFocus) => {
      if (typeof window !== "undefined" && window.addEventListener) {
        const listener = () => onFocus();
        window.addEventListener("visibilitychange", listener, false);
        return () => {
          window.removeEventListener("visibilitychange", listener);
        };
      }
      return;
    };
  }
  onSubscribe() {
    if (!this.#cleanup) {
      this.setEventListener(this.#setup);
    }
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      this.#cleanup?.();
      this.#cleanup = void 0;
    }
  }
  setEventListener(setup) {
    this.#setup = setup;
    this.#cleanup?.();
    this.#cleanup = setup((focused) => {
      if (typeof focused === "boolean") {
        this.setFocused(focused);
      } else {
        this.onFocus();
      }
    });
  }
  setFocused(focused) {
    const changed = this.#focused !== focused;
    if (changed) {
      this.#focused = focused;
      this.onFocus();
    }
  }
  onFocus() {
    const isFocused = this.isFocused();
    this.listeners.forEach((listener) => {
      listener(isFocused);
    });
  }
  isFocused() {
    if (typeof this.#focused === "boolean") {
      return this.#focused;
    }
    return globalThis.document?.visibilityState !== "hidden";
  }
};
var focusManager = new FocusManager();

// node_modules/@tanstack/query-core/build/modern/timeoutManager.js
var defaultTimeoutProvider = {
  // We need the wrapper function syntax below instead of direct references to
  // global setTimeout etc.
  //
  // BAD: `setTimeout: setTimeout`
  // GOOD: `setTimeout: (cb, delay) => setTimeout(cb, delay)`
  //
  // If we use direct references here, then anything that wants to spy on or
  // replace the global setTimeout (like tests) won't work since we'll already
  // have a hard reference to the original implementation at the time when this
  // file was imported.
  setTimeout: (callback, delay) => setTimeout(callback, delay),
  clearTimeout: (timeoutId) => clearTimeout(timeoutId),
  setInterval: (callback, delay) => setInterval(callback, delay),
  clearInterval: (intervalId) => clearInterval(intervalId)
};
var TimeoutManager = class {
  // We cannot have TimeoutManager<T> as we must instantiate it with a concrete
  // type at app boot; and if we leave that type, then any new timer provider
  // would need to support the default provider's concrete timer ID, which is
  // infeasible across environments.
  //
  // We settle for type safety for the TimeoutProvider type, and accept that
  // this class is unsafe internally to allow for extension.
  #provider = defaultTimeoutProvider;
  #providerCalled = false;
  setTimeoutProvider(provider) {
    if (process.env.NODE_ENV !== "production") {
      if (this.#providerCalled && provider !== this.#provider) {
        console.error(
          `[timeoutManager]: Switching provider after calls to previous provider might result in unexpected behavior.`,
          { previous: this.#provider, provider }
        );
      }
    }
    this.#provider = provider;
    if (process.env.NODE_ENV !== "production") {
      this.#providerCalled = false;
    }
  }
  setTimeout(callback, delay) {
    if (process.env.NODE_ENV !== "production") {
      this.#providerCalled = true;
    }
    return this.#provider.setTimeout(callback, delay);
  }
  clearTimeout(timeoutId) {
    this.#provider.clearTimeout(timeoutId);
  }
  setInterval(callback, delay) {
    if (process.env.NODE_ENV !== "production") {
      this.#providerCalled = true;
    }
    return this.#provider.setInterval(callback, delay);
  }
  clearInterval(intervalId) {
    this.#provider.clearInterval(intervalId);
  }
};
var timeoutManager = new TimeoutManager();
function systemSetTimeoutZero(callback) {
  setTimeout(callback, 0);
}

// node_modules/@tanstack/query-core/build/modern/utils.js
var isServer = typeof window === "undefined" || "Deno" in globalThis;
function noop() {
}
function functionalUpdate(updater, input) {
  return typeof updater === "function" ? updater(input) : updater;
}
function isValidTimeout(value) {
  return typeof value === "number" && value >= 0 && value !== Infinity;
}
function timeUntilStale(updatedAt, staleTime) {
  return Math.max(updatedAt + (staleTime || 0) - Date.now(), 0);
}
function resolveStaleTime(staleTime, query2) {
  return typeof staleTime === "function" ? staleTime(query2) : staleTime;
}
function resolveQueryBoolean(option, query2) {
  return typeof option === "function" ? option(query2) : option;
}
function matchQuery(filters, query2) {
  const {
    type = "all",
    exact,
    fetchStatus,
    predicate,
    queryKey,
    stale
  } = filters;
  if (queryKey) {
    if (exact) {
      if (query2.queryHash !== hashQueryKeyByOptions(queryKey, query2.options)) {
        return false;
      }
    } else if (!partialMatchKey(query2.queryKey, queryKey)) {
      return false;
    }
  }
  if (type !== "all") {
    const isActive = query2.isActive();
    if (type === "active" && !isActive) {
      return false;
    }
    if (type === "inactive" && isActive) {
      return false;
    }
  }
  if (typeof stale === "boolean" && query2.isStale() !== stale) {
    return false;
  }
  if (fetchStatus && fetchStatus !== query2.state.fetchStatus) {
    return false;
  }
  if (predicate && !predicate(query2)) {
    return false;
  }
  return true;
}
function matchMutation(filters, mutation) {
  const { exact, status, predicate, mutationKey } = filters;
  if (mutationKey) {
    if (!mutation.options.mutationKey) {
      return false;
    }
    if (exact) {
      if (hashKey(mutation.options.mutationKey) !== hashKey(mutationKey)) {
        return false;
      }
    } else if (!partialMatchKey(mutation.options.mutationKey, mutationKey)) {
      return false;
    }
  }
  if (status && mutation.state.status !== status) {
    return false;
  }
  if (predicate && !predicate(mutation)) {
    return false;
  }
  return true;
}
function hashQueryKeyByOptions(queryKey, options) {
  const hashFn = options?.queryKeyHashFn || hashKey;
  return hashFn(queryKey);
}
function hashKey(queryKey) {
  return JSON.stringify(
    queryKey,
    (_, val) => isPlainObject(val) ? Object.keys(val).sort().reduce((result, key) => {
      result[key] = val[key];
      return result;
    }, {}) : val
  );
}
function partialMatchKey(a, b) {
  if (a === b) {
    return true;
  }
  if (typeof a !== typeof b) {
    return false;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    if (Array.isArray(a) && Array.isArray(b)) {
      for (let i = 0; i < b.length; i++) {
        if (!partialMatchKey(a[i], b[i])) {
          return false;
        }
      }
      return true;
    }
    const bKeys = Object.keys(b);
    for (const key of bKeys) {
      if (!partialMatchKey(a[key], b[key])) {
        return false;
      }
    }
    return true;
  }
  return false;
}
var hasOwn = Object.prototype.hasOwnProperty;
function replaceEqualDeep(a, b, depth = 0) {
  if (a === b) {
    return a;
  }
  if (depth > 500) return b;
  const array = isPlainArray(a) && isPlainArray(b);
  if (!array && !(isPlainObject(a) && isPlainObject(b))) return b;
  const aItems = array ? a : Object.keys(a);
  const aSize = aItems.length;
  const bItems = array ? b : Object.keys(b);
  const bSize = bItems.length;
  const copy = array ? new Array(bSize) : {};
  let equalItems = 0;
  for (let i = 0; i < bSize; i++) {
    const key = array ? i : bItems[i];
    const aItem = a[key];
    const bItem = b[key];
    if (aItem === bItem) {
      copy[key] = aItem;
      if (array ? i < aSize : hasOwn.call(a, key)) equalItems++;
      continue;
    }
    if (aItem === null || bItem === null || typeof aItem !== "object" || typeof bItem !== "object") {
      copy[key] = bItem;
      continue;
    }
    const v = replaceEqualDeep(aItem, bItem, depth + 1);
    copy[key] = v;
    if (v === aItem) equalItems++;
  }
  return aSize === bSize && equalItems === aSize ? a : copy;
}
function isPlainArray(value) {
  return Array.isArray(value) && value.length === Object.keys(value).length;
}
function isPlainObject(o) {
  if (!hasObjectPrototype(o)) {
    return false;
  }
  const ctor = o.constructor;
  if (ctor === void 0) {
    return true;
  }
  const prot = ctor.prototype;
  if (!hasObjectPrototype(prot)) {
    return false;
  }
  if (!prot.hasOwnProperty("isPrototypeOf")) {
    return false;
  }
  if (Object.getPrototypeOf(o) !== Object.prototype) {
    return false;
  }
  return true;
}
function hasObjectPrototype(o) {
  return Object.prototype.toString.call(o) === "[object Object]";
}
function sleep2(timeout) {
  return new Promise((resolve) => {
    timeoutManager.setTimeout(resolve, timeout);
  });
}
function replaceData(prevData, data, options) {
  if (typeof options.structuralSharing === "function") {
    return options.structuralSharing(prevData, data);
  } else if (options.structuralSharing !== false) {
    if (process.env.NODE_ENV !== "production") {
      try {
        return replaceEqualDeep(prevData, data);
      } catch (error) {
        console.error(
          `Structural sharing requires data to be JSON serializable. To fix this, turn off structuralSharing or return JSON-serializable data from your queryFn. [${options.queryHash}]: ${error}`
        );
        throw error;
      }
    }
    return replaceEqualDeep(prevData, data);
  }
  return data;
}
function addToEnd(items, item, max = 0) {
  const newItems = [...items, item];
  return max && newItems.length > max ? newItems.slice(1) : newItems;
}
function addToStart(items, item, max = 0) {
  const newItems = [item, ...items];
  return max && newItems.length > max ? newItems.slice(0, -1) : newItems;
}
var skipToken = /* @__PURE__ */ Symbol();
function ensureQueryFn(options, fetchOptions) {
  if (process.env.NODE_ENV !== "production") {
    if (options.queryFn === skipToken) {
      console.error(
        `Attempted to invoke queryFn when set to skipToken. This is likely a configuration error. Query hash: '${options.queryHash}'`
      );
    }
  }
  if (!options.queryFn && fetchOptions?.initialPromise) {
    return () => fetchOptions.initialPromise;
  }
  if (!options.queryFn || options.queryFn === skipToken) {
    return () => Promise.reject(new Error(`Missing queryFn: '${options.queryHash}'`));
  }
  return options.queryFn;
}
function addConsumeAwareSignal(object, getSignal, onCancelled) {
  let consumed = false;
  let signal;
  Object.defineProperty(object, "signal", {
    enumerable: true,
    get: () => {
      signal ??= getSignal();
      if (consumed) {
        return signal;
      }
      consumed = true;
      if (signal.aborted) {
        onCancelled();
      } else {
        signal.addEventListener("abort", onCancelled, { once: true });
      }
      return signal;
    }
  });
  return object;
}

// node_modules/@tanstack/query-core/build/modern/environmentManager.js
var environmentManager = /* @__PURE__ */ (() => {
  let isServerFn = () => isServer;
  return {
    /**
     * Returns whether the current runtime should be treated as a server environment.
     */
    isServer() {
      return isServerFn();
    },
    /**
     * Overrides the server check globally.
     */
    setIsServer(isServerValue) {
      isServerFn = isServerValue;
    }
  };
})();

// node_modules/@tanstack/query-core/build/modern/thenable.js
function pendingThenable() {
  let resolve;
  let reject;
  const thenable = new Promise((_resolve, _reject) => {
    resolve = _resolve;
    reject = _reject;
  });
  thenable.status = "pending";
  thenable.catch(() => {
  });
  function finalize(data) {
    Object.assign(thenable, data);
    delete thenable.resolve;
    delete thenable.reject;
  }
  thenable.resolve = (value) => {
    finalize({
      status: "fulfilled",
      value
    });
    resolve(value);
  };
  thenable.reject = (reason) => {
    finalize({
      status: "rejected",
      reason
    });
    reject(reason);
  };
  return thenable;
}

// node_modules/@tanstack/query-core/build/modern/notifyManager.js
var defaultScheduler = systemSetTimeoutZero;
function createNotifyManager() {
  let queue = [];
  let transactions = 0;
  let notifyFn = (callback) => {
    callback();
  };
  let batchNotifyFn = (callback) => {
    callback();
  };
  let scheduleFn = defaultScheduler;
  const schedule = (callback) => {
    if (transactions) {
      queue.push(callback);
    } else {
      scheduleFn(() => {
        notifyFn(callback);
      });
    }
  };
  const flush = () => {
    const originalQueue = queue;
    queue = [];
    if (originalQueue.length) {
      scheduleFn(() => {
        batchNotifyFn(() => {
          originalQueue.forEach((callback) => {
            notifyFn(callback);
          });
        });
      });
    }
  };
  return {
    batch: (callback) => {
      let result;
      transactions++;
      try {
        result = callback();
      } finally {
        transactions--;
        if (!transactions) {
          flush();
        }
      }
      return result;
    },
    /**
     * All calls to the wrapped function will be batched.
     */
    batchCalls: (callback) => {
      return (...args) => {
        schedule(() => {
          callback(...args);
        });
      };
    },
    schedule,
    /**
     * Use this method to set a custom notify function.
     * This can be used to for example wrap notifications with `React.act` while running tests.
     */
    setNotifyFunction: (fn) => {
      notifyFn = fn;
    },
    /**
     * Use this method to set a custom function to batch notifications together into a single tick.
     * By default React Query will use the batch function provided by ReactDOM or React Native.
     */
    setBatchNotifyFunction: (fn) => {
      batchNotifyFn = fn;
    },
    setScheduler: (fn) => {
      scheduleFn = fn;
    }
  };
}
var notifyManager = createNotifyManager();

// node_modules/@tanstack/query-core/build/modern/onlineManager.js
var OnlineManager = class extends Subscribable {
  #online = true;
  #cleanup;
  #setup;
  constructor() {
    super();
    this.#setup = (onOnline) => {
      if (typeof window !== "undefined" && window.addEventListener) {
        const onlineListener = () => onOnline(true);
        const offlineListener = () => onOnline(false);
        window.addEventListener("online", onlineListener, false);
        window.addEventListener("offline", offlineListener, false);
        return () => {
          window.removeEventListener("online", onlineListener);
          window.removeEventListener("offline", offlineListener);
        };
      }
      return;
    };
  }
  onSubscribe() {
    if (!this.#cleanup) {
      this.setEventListener(this.#setup);
    }
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      this.#cleanup?.();
      this.#cleanup = void 0;
    }
  }
  setEventListener(setup) {
    this.#setup = setup;
    this.#cleanup?.();
    this.#cleanup = setup(this.setOnline.bind(this));
  }
  setOnline(online) {
    const changed = this.#online !== online;
    if (changed) {
      this.#online = online;
      this.listeners.forEach((listener) => {
        listener(online);
      });
    }
  }
  isOnline() {
    return this.#online;
  }
};
var onlineManager = new OnlineManager();

// node_modules/@tanstack/query-core/build/modern/retryer.js
function defaultRetryDelay(failureCount) {
  return Math.min(1e3 * 2 ** failureCount, 3e4);
}
function canFetch(networkMode) {
  return (networkMode ?? "online") === "online" ? onlineManager.isOnline() : true;
}
var CancelledError = class extends Error {
  constructor(options) {
    super("CancelledError");
    this.revert = options?.revert;
    this.silent = options?.silent;
  }
};
function createRetryer(config) {
  let isRetryCancelled = false;
  let failureCount = 0;
  let continueFn;
  const thenable = pendingThenable();
  const isResolved = () => thenable.status !== "pending";
  const cancel = (cancelOptions) => {
    if (!isResolved()) {
      const error = new CancelledError(cancelOptions);
      reject(error);
      config.onCancel?.(error);
    }
  };
  const cancelRetry = () => {
    isRetryCancelled = true;
  };
  const continueRetry = () => {
    isRetryCancelled = false;
  };
  const canContinue = () => focusManager.isFocused() && (config.networkMode === "always" || onlineManager.isOnline()) && config.canRun();
  const canStart = () => canFetch(config.networkMode) && config.canRun();
  const resolve = (value) => {
    if (!isResolved()) {
      continueFn?.();
      thenable.resolve(value);
    }
  };
  const reject = (value) => {
    if (!isResolved()) {
      continueFn?.();
      thenable.reject(value);
    }
  };
  const pause = () => {
    return new Promise((continueResolve) => {
      continueFn = (value) => {
        if (isResolved() || canContinue()) {
          continueResolve(value);
        }
      };
      config.onPause?.();
    }).then(() => {
      continueFn = void 0;
      if (!isResolved()) {
        config.onContinue?.();
      }
    });
  };
  const run2 = () => {
    if (isResolved()) {
      return;
    }
    let promiseOrValue;
    const initialPromise = failureCount === 0 ? config.initialPromise : void 0;
    try {
      promiseOrValue = initialPromise ?? config.fn();
    } catch (error) {
      promiseOrValue = Promise.reject(error);
    }
    Promise.resolve(promiseOrValue).then(resolve).catch((error) => {
      if (isResolved()) {
        return;
      }
      const retry = config.retry ?? (environmentManager.isServer() ? 0 : 3);
      const retryDelay = config.retryDelay ?? defaultRetryDelay;
      const delay = typeof retryDelay === "function" ? retryDelay(failureCount, error) : retryDelay;
      const shouldRetry = retry === true || typeof retry === "number" && failureCount < retry || typeof retry === "function" && retry(failureCount, error);
      if (isRetryCancelled || !shouldRetry) {
        reject(error);
        return;
      }
      failureCount++;
      config.onFail?.(failureCount, error);
      sleep2(delay).then(() => {
        return canContinue() ? void 0 : pause();
      }).then(() => {
        if (isRetryCancelled) {
          reject(error);
        } else {
          run2();
        }
      });
    });
  };
  return {
    promise: thenable,
    status: () => thenable.status,
    cancel,
    continue: () => {
      continueFn?.();
      return thenable;
    },
    cancelRetry,
    continueRetry,
    canStart,
    start: () => {
      if (canStart()) {
        run2();
      } else {
        pause().then(run2);
      }
      return thenable;
    }
  };
}

// node_modules/@tanstack/query-core/build/modern/removable.js
var Removable = class {
  #gcTimeout;
  destroy() {
    this.clearGcTimeout();
  }
  scheduleGc() {
    this.clearGcTimeout();
    if (isValidTimeout(this.gcTime)) {
      this.#gcTimeout = timeoutManager.setTimeout(() => {
        this.optionalRemove();
      }, this.gcTime);
    }
  }
  updateGcTime(newGcTime) {
    this.gcTime = Math.max(
      this.gcTime || 0,
      newGcTime ?? (environmentManager.isServer() ? Infinity : 5 * 60 * 1e3)
    );
  }
  clearGcTimeout() {
    if (this.#gcTimeout !== void 0) {
      timeoutManager.clearTimeout(this.#gcTimeout);
      this.#gcTimeout = void 0;
    }
  }
};

// node_modules/@tanstack/query-core/build/modern/infiniteQueryBehavior.js
function infiniteQueryBehavior(pages) {
  return {
    onFetch: (context, query2) => {
      const options = context.options;
      const direction = context.fetchOptions?.meta?.fetchMore?.direction;
      const oldPages = context.state.data?.pages || [];
      const oldPageParams = context.state.data?.pageParams || [];
      let result = { pages: [], pageParams: [] };
      let currentPage = 0;
      const fetchFn = async () => {
        let cancelled = false;
        const addSignalProperty = (object) => {
          addConsumeAwareSignal(
            object,
            () => context.signal,
            () => cancelled = true
          );
        };
        const queryFn = ensureQueryFn(context.options, context.fetchOptions);
        const fetchPage = async (data, param, previous) => {
          if (cancelled) {
            return Promise.reject(context.signal.reason);
          }
          if (param == null && data.pages.length) {
            return Promise.resolve(data);
          }
          const createQueryFnContext = () => {
            const queryFnContext2 = {
              client: context.client,
              queryKey: context.queryKey,
              pageParam: param,
              direction: previous ? "backward" : "forward",
              meta: context.options.meta
            };
            addSignalProperty(queryFnContext2);
            return queryFnContext2;
          };
          const queryFnContext = createQueryFnContext();
          const page = await queryFn(queryFnContext);
          const { maxPages } = context.options;
          const addTo = previous ? addToStart : addToEnd;
          return {
            pages: addTo(data.pages, page, maxPages),
            pageParams: addTo(data.pageParams, param, maxPages)
          };
        };
        if (direction && oldPages.length) {
          const previous = direction === "backward";
          const pageParamFn = previous ? getPreviousPageParam : getNextPageParam;
          const oldData = {
            pages: oldPages,
            pageParams: oldPageParams
          };
          const param = pageParamFn(options, oldData);
          result = await fetchPage(oldData, param, previous);
        } else {
          const remainingPages = pages ?? oldPages.length;
          do {
            const param = currentPage === 0 ? oldPageParams[0] ?? options.initialPageParam : getNextPageParam(options, result);
            if (currentPage > 0 && param == null) {
              break;
            }
            result = await fetchPage(result, param);
            currentPage++;
          } while (currentPage < remainingPages);
        }
        return result;
      };
      if (context.options.persister) {
        context.fetchFn = () => {
          return context.options.persister?.(
            fetchFn,
            {
              client: context.client,
              queryKey: context.queryKey,
              meta: context.options.meta,
              signal: context.signal
            },
            query2
          );
        };
      } else {
        context.fetchFn = fetchFn;
      }
    }
  };
}
function getNextPageParam(options, { pages, pageParams }) {
  const lastIndex = pages.length - 1;
  return pages.length > 0 ? options.getNextPageParam(
    pages[lastIndex],
    pages,
    pageParams[lastIndex],
    pageParams
  ) : void 0;
}
function getPreviousPageParam(options, { pages, pageParams }) {
  return pages.length > 0 ? options.getPreviousPageParam?.(pages[0], pages, pageParams[0], pageParams) : void 0;
}

// node_modules/@tanstack/query-core/build/modern/query.js
var Query = class extends Removable {
  #queryType;
  #initialState;
  #revertState;
  #cache;
  #client;
  #retryer;
  #defaultOptions;
  #abortSignalConsumed;
  constructor(config) {
    super();
    this.#abortSignalConsumed = false;
    this.#defaultOptions = config.defaultOptions;
    this.setOptions(config.options);
    this.observers = [];
    this.#client = config.client;
    this.#cache = this.#client.getQueryCache();
    this.queryKey = config.queryKey;
    this.queryHash = config.queryHash;
    this.#initialState = getDefaultState(this.options);
    this.state = config.state ?? this.#initialState;
    this.scheduleGc();
  }
  get meta() {
    return this.options.meta;
  }
  get queryType() {
    return this.#queryType;
  }
  get promise() {
    return this.#retryer?.promise;
  }
  setOptions(options) {
    this.options = { ...this.#defaultOptions, ...options };
    if (options?._type) {
      this.#queryType = options._type;
    }
    this.updateGcTime(this.options.gcTime);
    if (this.state && this.state.data === void 0) {
      const defaultState = getDefaultState(this.options);
      if (defaultState.data !== void 0) {
        this.setState(
          successState(defaultState.data, defaultState.dataUpdatedAt)
        );
        this.#initialState = defaultState;
      }
    }
  }
  optionalRemove() {
    if (!this.observers.length && this.state.fetchStatus === "idle") {
      this.#cache.remove(this);
    }
  }
  setData(newData, options) {
    const data = replaceData(this.state.data, newData, this.options);
    this.#dispatch({
      data,
      type: "success",
      dataUpdatedAt: options?.updatedAt,
      manual: options?.manual
    });
    return data;
  }
  setState(state) {
    this.#dispatch({ type: "setState", state });
  }
  cancel(options) {
    const promise = this.#retryer?.promise;
    this.#retryer?.cancel(options);
    return promise ? promise.then(noop).catch(noop) : Promise.resolve();
  }
  destroy() {
    super.destroy();
    this.cancel({ silent: true });
  }
  get resetState() {
    return this.#initialState;
  }
  reset() {
    this.destroy();
    this.setState(this.resetState);
  }
  isActive() {
    return this.observers.some(
      (observer) => resolveQueryBoolean(observer.options.enabled, this) !== false
    );
  }
  isDisabled() {
    if (this.getObserversCount() > 0) {
      return !this.isActive();
    }
    return this.options.queryFn === skipToken || !this.isFetched();
  }
  isFetched() {
    return this.state.dataUpdateCount + this.state.errorUpdateCount > 0;
  }
  isStatic() {
    if (this.getObserversCount() > 0) {
      return this.observers.some(
        (observer) => resolveStaleTime(observer.options.staleTime, this) === "static"
      );
    }
    return false;
  }
  isStale() {
    if (this.getObserversCount() > 0) {
      return this.observers.some(
        (observer) => observer.getCurrentResult().isStale
      );
    }
    return this.state.data === void 0 || this.state.isInvalidated;
  }
  isStaleByTime(staleTime = 0) {
    if (this.state.data === void 0) {
      return true;
    }
    if (staleTime === "static") {
      return false;
    }
    if (this.state.isInvalidated) {
      return true;
    }
    return !timeUntilStale(this.state.dataUpdatedAt, staleTime);
  }
  onFocus() {
    const observer = this.observers.find((x) => x.shouldFetchOnWindowFocus());
    observer?.refetch({ cancelRefetch: false });
    this.#retryer?.continue();
  }
  onOnline() {
    const observer = this.observers.find((x) => x.shouldFetchOnReconnect());
    observer?.refetch({ cancelRefetch: false });
    this.#retryer?.continue();
  }
  addObserver(observer) {
    if (!this.observers.includes(observer)) {
      this.observers.push(observer);
      this.clearGcTimeout();
      this.#cache.notify({ type: "observerAdded", query: this, observer });
    }
  }
  removeObserver(observer) {
    if (this.observers.includes(observer)) {
      this.observers = this.observers.filter((x) => x !== observer);
      if (!this.observers.length) {
        if (this.#retryer) {
          if (this.#abortSignalConsumed || this.#isInitialPausedFetch()) {
            this.#retryer.cancel({ revert: true });
          } else {
            this.#retryer.cancelRetry();
          }
        }
        this.scheduleGc();
      }
      this.#cache.notify({ type: "observerRemoved", query: this, observer });
    }
  }
  getObserversCount() {
    return this.observers.length;
  }
  #isInitialPausedFetch() {
    return this.state.fetchStatus === "paused" && this.state.status === "pending";
  }
  invalidate() {
    if (!this.state.isInvalidated) {
      this.#dispatch({ type: "invalidate" });
    }
  }
  async fetch(options, fetchOptions) {
    if (this.state.fetchStatus !== "idle" && // If the promise in the retryer is already rejected, we have to definitely
    // re-start the fetch; there is a chance that the query is still in a
    // pending state when that happens
    this.#retryer?.status() !== "rejected") {
      if (this.state.data !== void 0 && fetchOptions?.cancelRefetch) {
        this.cancel({ silent: true });
      } else if (this.#retryer) {
        this.#retryer.continueRetry();
        return this.#retryer.promise;
      }
    }
    if (options) {
      this.setOptions(options);
    }
    if (!this.options.queryFn) {
      const observer = this.observers.find((x) => x.options.queryFn);
      if (observer) {
        this.setOptions(observer.options);
      }
    }
    if (process.env.NODE_ENV !== "production") {
      if (!Array.isArray(this.options.queryKey)) {
        console.error(
          `As of v4, queryKey needs to be an Array. If you are using a string like 'repoData', please change it to an Array, e.g. ['repoData']`
        );
      }
    }
    const abortController = new AbortController();
    const addSignalProperty = (object) => {
      Object.defineProperty(object, "signal", {
        enumerable: true,
        get: () => {
          this.#abortSignalConsumed = true;
          return abortController.signal;
        }
      });
    };
    const fetchFn = () => {
      const queryFn = ensureQueryFn(this.options, fetchOptions);
      const createQueryFnContext = () => {
        const queryFnContext2 = {
          client: this.#client,
          queryKey: this.queryKey,
          meta: this.meta
        };
        addSignalProperty(queryFnContext2);
        return queryFnContext2;
      };
      const queryFnContext = createQueryFnContext();
      this.#abortSignalConsumed = false;
      if (this.options.persister) {
        return this.options.persister(
          queryFn,
          queryFnContext,
          this
        );
      }
      return queryFn(queryFnContext);
    };
    const createFetchContext = () => {
      const context2 = {
        fetchOptions,
        options: this.options,
        queryKey: this.queryKey,
        client: this.#client,
        state: this.state,
        fetchFn
      };
      addSignalProperty(context2);
      return context2;
    };
    const context = createFetchContext();
    const behavior = this.#queryType === "infinite" ? infiniteQueryBehavior(
      this.options.pages
    ) : this.options.behavior;
    behavior?.onFetch(context, this);
    this.#revertState = this.state;
    if (this.state.fetchStatus === "idle" || this.state.fetchMeta !== context.fetchOptions?.meta) {
      this.#dispatch({ type: "fetch", meta: context.fetchOptions?.meta });
    }
    this.#retryer = createRetryer({
      initialPromise: fetchOptions?.initialPromise,
      fn: context.fetchFn,
      onCancel: (error) => {
        if (error instanceof CancelledError && error.revert) {
          this.setState({
            ...this.#revertState,
            fetchStatus: "idle"
          });
        }
        abortController.abort();
      },
      onFail: (failureCount, error) => {
        this.#dispatch({ type: "failed", failureCount, error });
      },
      onPause: () => {
        this.#dispatch({ type: "pause" });
      },
      onContinue: () => {
        this.#dispatch({ type: "continue" });
      },
      retry: context.options.retry,
      retryDelay: context.options.retryDelay,
      networkMode: context.options.networkMode,
      canRun: () => true
    });
    try {
      const data = await this.#retryer.start();
      if (data === void 0) {
        if (process.env.NODE_ENV !== "production") {
          console.error(
            `Query data cannot be undefined. Please make sure to return a value other than undefined from your query function. Affected query key: ${this.queryHash}`
          );
        }
        throw new Error(`${this.queryHash} data is undefined`);
      }
      this.setData(data);
      this.#cache.config.onSuccess?.(data, this);
      this.#cache.config.onSettled?.(
        data,
        this.state.error,
        this
      );
      return data;
    } catch (error) {
      if (error instanceof CancelledError) {
        if (error.silent) {
          return this.#retryer.promise;
        } else if (error.revert) {
          if (this.state.data === void 0) {
            throw error;
          }
          return this.state.data;
        }
      }
      this.#dispatch({
        type: "error",
        error
      });
      this.#cache.config.onError?.(
        error,
        this
      );
      this.#cache.config.onSettled?.(
        this.state.data,
        error,
        this
      );
      throw error;
    } finally {
      this.scheduleGc();
    }
  }
  #dispatch(action) {
    const reducer = (state) => {
      switch (action.type) {
        case "failed":
          return {
            ...state,
            fetchFailureCount: action.failureCount,
            fetchFailureReason: action.error
          };
        case "pause":
          return {
            ...state,
            fetchStatus: "paused"
          };
        case "continue":
          return {
            ...state,
            fetchStatus: "fetching"
          };
        case "fetch":
          return {
            ...state,
            ...fetchState(state.data, this.options),
            fetchMeta: action.meta ?? null
          };
        case "success":
          const newState = {
            ...state,
            ...successState(action.data, action.dataUpdatedAt),
            dataUpdateCount: state.dataUpdateCount + 1,
            ...!action.manual && {
              fetchStatus: "idle",
              fetchFailureCount: 0,
              fetchFailureReason: null
            }
          };
          this.#revertState = action.manual ? newState : void 0;
          return newState;
        case "error":
          const error = action.error;
          return {
            ...state,
            error,
            errorUpdateCount: state.errorUpdateCount + 1,
            errorUpdatedAt: Date.now(),
            fetchFailureCount: state.fetchFailureCount + 1,
            fetchFailureReason: error,
            fetchStatus: "idle",
            status: "error",
            // flag existing data as invalidated if we get a background error
            // note that "no data" always means stale so we can set unconditionally here
            isInvalidated: true
          };
        case "invalidate":
          return {
            ...state,
            isInvalidated: true
          };
        case "setState":
          return {
            ...state,
            ...action.state
          };
      }
    };
    this.state = reducer(this.state);
    notifyManager.batch(() => {
      this.observers.forEach((observer) => {
        observer.onQueryUpdate();
      });
      this.#cache.notify({ query: this, type: "updated", action });
    });
  }
};
function fetchState(data, options) {
  return {
    fetchFailureCount: 0,
    fetchFailureReason: null,
    fetchStatus: canFetch(options.networkMode) ? "fetching" : "paused",
    ...data === void 0 && {
      error: null,
      status: "pending"
    }
  };
}
function successState(data, dataUpdatedAt) {
  return {
    data,
    dataUpdatedAt: dataUpdatedAt ?? Date.now(),
    error: null,
    isInvalidated: false,
    status: "success"
  };
}
function getDefaultState(options) {
  const data = typeof options.initialData === "function" ? options.initialData() : options.initialData;
  const hasData = data !== void 0;
  const initialDataUpdatedAt = hasData ? typeof options.initialDataUpdatedAt === "function" ? options.initialDataUpdatedAt() : options.initialDataUpdatedAt : 0;
  return {
    data,
    dataUpdateCount: 0,
    dataUpdatedAt: hasData ? initialDataUpdatedAt ?? Date.now() : 0,
    error: null,
    errorUpdateCount: 0,
    errorUpdatedAt: 0,
    fetchFailureCount: 0,
    fetchFailureReason: null,
    fetchMeta: null,
    isInvalidated: false,
    status: hasData ? "success" : "pending",
    fetchStatus: "idle"
  };
}

// node_modules/@tanstack/query-core/build/modern/mutation.js
var Mutation = class extends Removable {
  #client;
  #observers;
  #mutationCache;
  #retryer;
  constructor(config) {
    super();
    this.#client = config.client;
    this.mutationId = config.mutationId;
    this.#mutationCache = config.mutationCache;
    this.#observers = [];
    this.state = config.state || getDefaultState2();
    this.setOptions(config.options);
    this.scheduleGc();
  }
  setOptions(options) {
    this.options = options;
    this.updateGcTime(this.options.gcTime);
  }
  get meta() {
    return this.options.meta;
  }
  addObserver(observer) {
    if (!this.#observers.includes(observer)) {
      this.#observers.push(observer);
      this.clearGcTimeout();
      this.#mutationCache.notify({
        type: "observerAdded",
        mutation: this,
        observer
      });
    }
  }
  removeObserver(observer) {
    this.#observers = this.#observers.filter((x) => x !== observer);
    this.scheduleGc();
    this.#mutationCache.notify({
      type: "observerRemoved",
      mutation: this,
      observer
    });
  }
  optionalRemove() {
    if (!this.#observers.length) {
      if (this.state.status === "pending") {
        this.scheduleGc();
      } else {
        this.#mutationCache.remove(this);
      }
    }
  }
  continue() {
    return this.#retryer?.continue() ?? // continuing a mutation assumes that variables are set, mutation must have been dehydrated before
    this.execute(this.state.variables);
  }
  async execute(variables) {
    const onContinue = () => {
      this.#dispatch({ type: "continue" });
    };
    const mutationFnContext = {
      client: this.#client,
      meta: this.options.meta,
      mutationKey: this.options.mutationKey
    };
    this.#retryer = createRetryer({
      fn: () => {
        if (!this.options.mutationFn) {
          return Promise.reject(new Error("No mutationFn found"));
        }
        return this.options.mutationFn(variables, mutationFnContext);
      },
      onFail: (failureCount, error) => {
        this.#dispatch({ type: "failed", failureCount, error });
      },
      onPause: () => {
        this.#dispatch({ type: "pause" });
      },
      onContinue,
      retry: this.options.retry ?? 0,
      retryDelay: this.options.retryDelay,
      networkMode: this.options.networkMode,
      canRun: () => this.#mutationCache.canRun(this)
    });
    const restored = this.state.status === "pending";
    const isPaused = !this.#retryer.canStart();
    try {
      if (restored) {
        onContinue();
      } else {
        this.#dispatch({ type: "pending", variables, isPaused });
        if (this.#mutationCache.config.onMutate) {
          await this.#mutationCache.config.onMutate(
            variables,
            this,
            mutationFnContext
          );
        }
        const context = await this.options.onMutate?.(
          variables,
          mutationFnContext
        );
        if (context !== this.state.context) {
          this.#dispatch({
            type: "pending",
            context,
            variables,
            isPaused
          });
        }
      }
      const data = await this.#retryer.start();
      await this.#mutationCache.config.onSuccess?.(
        data,
        variables,
        this.state.context,
        this,
        mutationFnContext
      );
      await this.options.onSuccess?.(
        data,
        variables,
        this.state.context,
        mutationFnContext
      );
      await this.#mutationCache.config.onSettled?.(
        data,
        null,
        this.state.variables,
        this.state.context,
        this,
        mutationFnContext
      );
      await this.options.onSettled?.(
        data,
        null,
        variables,
        this.state.context,
        mutationFnContext
      );
      this.#dispatch({ type: "success", data });
      return data;
    } catch (error) {
      try {
        await this.#mutationCache.config.onError?.(
          error,
          variables,
          this.state.context,
          this,
          mutationFnContext
        );
      } catch (e) {
        void Promise.reject(e);
      }
      try {
        await this.options.onError?.(
          error,
          variables,
          this.state.context,
          mutationFnContext
        );
      } catch (e) {
        void Promise.reject(e);
      }
      try {
        await this.#mutationCache.config.onSettled?.(
          void 0,
          error,
          this.state.variables,
          this.state.context,
          this,
          mutationFnContext
        );
      } catch (e) {
        void Promise.reject(e);
      }
      try {
        await this.options.onSettled?.(
          void 0,
          error,
          variables,
          this.state.context,
          mutationFnContext
        );
      } catch (e) {
        void Promise.reject(e);
      }
      this.#dispatch({ type: "error", error });
      throw error;
    } finally {
      this.#mutationCache.runNext(this);
    }
  }
  #dispatch(action) {
    const reducer = (state) => {
      switch (action.type) {
        case "failed":
          return {
            ...state,
            failureCount: action.failureCount,
            failureReason: action.error
          };
        case "pause":
          return {
            ...state,
            isPaused: true
          };
        case "continue":
          return {
            ...state,
            isPaused: false
          };
        case "pending":
          return {
            ...state,
            context: action.context,
            data: void 0,
            failureCount: 0,
            failureReason: null,
            error: null,
            isPaused: action.isPaused,
            status: "pending",
            variables: action.variables,
            submittedAt: Date.now()
          };
        case "success":
          return {
            ...state,
            data: action.data,
            failureCount: 0,
            failureReason: null,
            error: null,
            status: "success",
            isPaused: false
          };
        case "error":
          return {
            ...state,
            data: void 0,
            error: action.error,
            failureCount: state.failureCount + 1,
            failureReason: action.error,
            isPaused: false,
            status: "error"
          };
      }
    };
    this.state = reducer(this.state);
    notifyManager.batch(() => {
      this.#observers.forEach((observer) => {
        observer.onMutationUpdate(action);
      });
      this.#mutationCache.notify({
        mutation: this,
        type: "updated",
        action
      });
    });
  }
};
function getDefaultState2() {
  return {
    context: void 0,
    data: void 0,
    error: null,
    failureCount: 0,
    failureReason: null,
    isPaused: false,
    status: "idle",
    variables: void 0,
    submittedAt: 0
  };
}

// node_modules/@tanstack/query-core/build/modern/mutationCache.js
var MutationCache = class extends Subscribable {
  constructor(config = {}) {
    super();
    this.config = config;
    this.#mutations = /* @__PURE__ */ new Set();
    this.#scopes = /* @__PURE__ */ new Map();
    this.#mutationId = 0;
  }
  #mutations;
  #scopes;
  #mutationId;
  build(client, options, state) {
    const mutation = new Mutation({
      client,
      mutationCache: this,
      mutationId: ++this.#mutationId,
      options: client.defaultMutationOptions(options),
      state
    });
    this.add(mutation);
    return mutation;
  }
  add(mutation) {
    this.#mutations.add(mutation);
    const scope = scopeFor(mutation);
    if (typeof scope === "string") {
      const scopedMutations = this.#scopes.get(scope);
      if (scopedMutations) {
        scopedMutations.push(mutation);
      } else {
        this.#scopes.set(scope, [mutation]);
      }
    }
    this.notify({ type: "added", mutation });
  }
  remove(mutation) {
    if (this.#mutations.delete(mutation)) {
      const scope = scopeFor(mutation);
      if (typeof scope === "string") {
        const scopedMutations = this.#scopes.get(scope);
        if (scopedMutations) {
          if (scopedMutations.length > 1) {
            const index = scopedMutations.indexOf(mutation);
            if (index !== -1) {
              scopedMutations.splice(index, 1);
            }
          } else if (scopedMutations[0] === mutation) {
            this.#scopes.delete(scope);
          }
        }
      }
    }
    this.notify({ type: "removed", mutation });
  }
  canRun(mutation) {
    const scope = scopeFor(mutation);
    if (typeof scope === "string") {
      const mutationsWithSameScope = this.#scopes.get(scope);
      const firstPendingMutation = mutationsWithSameScope?.find(
        (m) => m.state.status === "pending"
      );
      return !firstPendingMutation || firstPendingMutation === mutation;
    } else {
      return true;
    }
  }
  runNext(mutation) {
    const scope = scopeFor(mutation);
    if (typeof scope === "string") {
      const foundMutation = this.#scopes.get(scope)?.find((m) => m !== mutation && m.state.isPaused);
      return foundMutation?.continue() ?? Promise.resolve();
    } else {
      return Promise.resolve();
    }
  }
  clear() {
    notifyManager.batch(() => {
      this.#mutations.forEach((mutation) => {
        this.notify({ type: "removed", mutation });
      });
      this.#mutations.clear();
      this.#scopes.clear();
    });
  }
  getAll() {
    return Array.from(this.#mutations);
  }
  find(filters) {
    const defaultedFilters = { exact: true, ...filters };
    return this.getAll().find(
      (mutation) => matchMutation(defaultedFilters, mutation)
    );
  }
  findAll(filters = {}) {
    return this.getAll().filter((mutation) => matchMutation(filters, mutation));
  }
  notify(event) {
    notifyManager.batch(() => {
      this.listeners.forEach((listener) => {
        listener(event);
      });
    });
  }
  resumePausedMutations() {
    const pausedMutations = this.getAll().filter((x) => x.state.isPaused);
    return notifyManager.batch(
      () => Promise.all(
        pausedMutations.map((mutation) => mutation.continue().catch(noop))
      )
    );
  }
};
function scopeFor(mutation) {
  return mutation.options.scope?.id;
}

// node_modules/@tanstack/query-core/build/modern/queryCache.js
var QueryCache = class extends Subscribable {
  constructor(config = {}) {
    super();
    this.config = config;
    this.#queries = /* @__PURE__ */ new Map();
  }
  #queries;
  build(client, options, state) {
    const queryKey = options.queryKey;
    const queryHash = options.queryHash ?? hashQueryKeyByOptions(queryKey, options);
    let query2 = this.get(queryHash);
    if (!query2) {
      query2 = new Query({
        client,
        queryKey,
        queryHash,
        options: client.defaultQueryOptions(options),
        state,
        defaultOptions: client.getQueryDefaults(queryKey)
      });
      this.add(query2);
    }
    return query2;
  }
  add(query2) {
    if (!this.#queries.has(query2.queryHash)) {
      this.#queries.set(query2.queryHash, query2);
      this.notify({
        type: "added",
        query: query2
      });
    }
  }
  remove(query2) {
    const queryInMap = this.#queries.get(query2.queryHash);
    if (queryInMap) {
      query2.destroy();
      if (queryInMap === query2) {
        this.#queries.delete(query2.queryHash);
      }
      this.notify({ type: "removed", query: query2 });
    }
  }
  clear() {
    notifyManager.batch(() => {
      this.getAll().forEach((query2) => {
        this.remove(query2);
      });
    });
  }
  get(queryHash) {
    return this.#queries.get(queryHash);
  }
  getAll() {
    return [...this.#queries.values()];
  }
  find(filters) {
    const defaultedFilters = { exact: true, ...filters };
    return this.getAll().find(
      (query2) => matchQuery(defaultedFilters, query2)
    );
  }
  findAll(filters = {}) {
    const queries = this.getAll();
    return Object.keys(filters).length > 0 ? queries.filter((query2) => matchQuery(filters, query2)) : queries;
  }
  notify(event) {
    notifyManager.batch(() => {
      this.listeners.forEach((listener) => {
        listener(event);
      });
    });
  }
  onFocus() {
    notifyManager.batch(() => {
      this.getAll().forEach((query2) => {
        query2.onFocus();
      });
    });
  }
  onOnline() {
    notifyManager.batch(() => {
      this.getAll().forEach((query2) => {
        query2.onOnline();
      });
    });
  }
};

// node_modules/@tanstack/query-core/build/modern/queryClient.js
var QueryClient = class {
  #queryCache;
  #mutationCache;
  #defaultOptions;
  #queryDefaults;
  #mutationDefaults;
  #mountCount;
  #unsubscribeFocus;
  #unsubscribeOnline;
  constructor(config = {}) {
    this.#queryCache = config.queryCache || new QueryCache();
    this.#mutationCache = config.mutationCache || new MutationCache();
    this.#defaultOptions = config.defaultOptions || {};
    this.#queryDefaults = /* @__PURE__ */ new Map();
    this.#mutationDefaults = /* @__PURE__ */ new Map();
    this.#mountCount = 0;
  }
  mount() {
    this.#mountCount++;
    if (this.#mountCount !== 1) return;
    this.#unsubscribeFocus = focusManager.subscribe(async (focused) => {
      if (focused) {
        await this.resumePausedMutations();
        this.#queryCache.onFocus();
      }
    });
    this.#unsubscribeOnline = onlineManager.subscribe(async (online) => {
      if (online) {
        await this.resumePausedMutations();
        this.#queryCache.onOnline();
      }
    });
  }
  unmount() {
    this.#mountCount--;
    if (this.#mountCount !== 0) return;
    this.#unsubscribeFocus?.();
    this.#unsubscribeFocus = void 0;
    this.#unsubscribeOnline?.();
    this.#unsubscribeOnline = void 0;
  }
  isFetching(filters) {
    return this.#queryCache.findAll({ ...filters, fetchStatus: "fetching" }).length;
  }
  isMutating(filters) {
    return this.#mutationCache.findAll({ ...filters, status: "pending" }).length;
  }
  /**
   * Imperative (non-reactive) way to retrieve data for a QueryKey.
   * Should only be used in callbacks or functions where reading the latest data is necessary, e.g. for optimistic updates.
   *
   * Hint: Do not use this function inside a component, because it won't receive updates.
   * Use `useQuery` to create a `QueryObserver` that subscribes to changes.
   */
  getQueryData(queryKey) {
    const options = this.defaultQueryOptions({ queryKey });
    return this.#queryCache.get(options.queryHash)?.state.data;
  }
  ensureQueryData(options) {
    const defaultedOptions = this.defaultQueryOptions(options);
    const query2 = this.#queryCache.build(this, defaultedOptions);
    const cachedData = query2.state.data;
    if (cachedData === void 0) {
      return this.fetchQuery(options);
    }
    if (options.revalidateIfStale && query2.isStaleByTime(resolveStaleTime(defaultedOptions.staleTime, query2))) {
      void this.prefetchQuery(defaultedOptions);
    }
    return Promise.resolve(cachedData);
  }
  getQueriesData(filters) {
    return this.#queryCache.findAll(filters).map(({ queryKey, state }) => {
      const data = state.data;
      return [queryKey, data];
    });
  }
  setQueryData(queryKey, updater, options) {
    const defaultedOptions = this.defaultQueryOptions({ queryKey });
    const query2 = this.#queryCache.get(
      defaultedOptions.queryHash
    );
    const prevData = query2?.state.data;
    const data = functionalUpdate(updater, prevData);
    if (data === void 0) {
      return void 0;
    }
    return this.#queryCache.build(this, defaultedOptions).setData(data, { ...options, manual: true });
  }
  setQueriesData(filters, updater, options) {
    return notifyManager.batch(
      () => this.#queryCache.findAll(filters).map(({ queryKey }) => [
        queryKey,
        this.setQueryData(queryKey, updater, options)
      ])
    );
  }
  getQueryState(queryKey) {
    const options = this.defaultQueryOptions({ queryKey });
    return this.#queryCache.get(
      options.queryHash
    )?.state;
  }
  removeQueries(filters) {
    const queryCache = this.#queryCache;
    notifyManager.batch(() => {
      queryCache.findAll(filters).forEach((query2) => {
        queryCache.remove(query2);
      });
    });
  }
  resetQueries(filters, options) {
    const queryCache = this.#queryCache;
    return notifyManager.batch(() => {
      queryCache.findAll(filters).forEach((query2) => {
        query2.reset();
      });
      return this.refetchQueries(
        {
          type: "active",
          ...filters
        },
        options
      );
    });
  }
  cancelQueries(filters, cancelOptions = {}) {
    const defaultedCancelOptions = { revert: true, ...cancelOptions };
    const promises = notifyManager.batch(
      () => this.#queryCache.findAll(filters).map((query2) => query2.cancel(defaultedCancelOptions))
    );
    return Promise.all(promises).then(noop).catch(noop);
  }
  invalidateQueries(filters, options = {}) {
    return notifyManager.batch(() => {
      this.#queryCache.findAll(filters).forEach((query2) => {
        query2.invalidate();
      });
      if (filters?.refetchType === "none") {
        return Promise.resolve();
      }
      return this.refetchQueries(
        {
          ...filters,
          type: filters?.refetchType ?? filters?.type ?? "active"
        },
        options
      );
    });
  }
  refetchQueries(filters, options = {}) {
    const fetchOptions = {
      ...options,
      cancelRefetch: options.cancelRefetch ?? true
    };
    const promises = notifyManager.batch(
      () => this.#queryCache.findAll(filters).filter((query2) => !query2.isDisabled() && !query2.isStatic()).map((query2) => {
        let promise = query2.fetch(void 0, fetchOptions);
        if (!fetchOptions.throwOnError) {
          promise = promise.catch(noop);
        }
        return query2.state.fetchStatus === "paused" ? Promise.resolve() : promise;
      })
    );
    return Promise.all(promises).then(noop);
  }
  fetchQuery(options) {
    const defaultedOptions = this.defaultQueryOptions(options);
    if (defaultedOptions.retry === void 0) {
      defaultedOptions.retry = false;
    }
    const query2 = this.#queryCache.build(this, defaultedOptions);
    return query2.isStaleByTime(
      resolveStaleTime(defaultedOptions.staleTime, query2)
    ) ? query2.fetch(defaultedOptions) : Promise.resolve(query2.state.data);
  }
  prefetchQuery(options) {
    return this.fetchQuery(options).then(noop).catch(noop);
  }
  fetchInfiniteQuery(options) {
    options._type = "infinite";
    return this.fetchQuery(options);
  }
  prefetchInfiniteQuery(options) {
    return this.fetchInfiniteQuery(options).then(noop).catch(noop);
  }
  ensureInfiniteQueryData(options) {
    options._type = "infinite";
    return this.ensureQueryData(options);
  }
  resumePausedMutations() {
    if (onlineManager.isOnline()) {
      return this.#mutationCache.resumePausedMutations();
    }
    return Promise.resolve();
  }
  getQueryCache() {
    return this.#queryCache;
  }
  getMutationCache() {
    return this.#mutationCache;
  }
  getDefaultOptions() {
    return this.#defaultOptions;
  }
  setDefaultOptions(options) {
    this.#defaultOptions = options;
  }
  setQueryDefaults(queryKey, options) {
    this.#queryDefaults.set(hashKey(queryKey), {
      queryKey,
      defaultOptions: options
    });
  }
  getQueryDefaults(queryKey) {
    const defaults = [...this.#queryDefaults.values()];
    const result = {};
    defaults.forEach((queryDefault) => {
      if (partialMatchKey(queryKey, queryDefault.queryKey)) {
        Object.assign(result, queryDefault.defaultOptions);
      }
    });
    return result;
  }
  setMutationDefaults(mutationKey, options) {
    this.#mutationDefaults.set(hashKey(mutationKey), {
      mutationKey,
      defaultOptions: options
    });
  }
  getMutationDefaults(mutationKey) {
    const defaults = [...this.#mutationDefaults.values()];
    const result = {};
    defaults.forEach((queryDefault) => {
      if (partialMatchKey(mutationKey, queryDefault.mutationKey)) {
        Object.assign(result, queryDefault.defaultOptions);
      }
    });
    return result;
  }
  defaultQueryOptions(options) {
    if (options._defaulted) {
      return options;
    }
    const defaultedOptions = {
      ...this.#defaultOptions.queries,
      ...this.getQueryDefaults(options.queryKey),
      ...options,
      _defaulted: true
    };
    if (!defaultedOptions.queryHash) {
      defaultedOptions.queryHash = hashQueryKeyByOptions(
        defaultedOptions.queryKey,
        defaultedOptions
      );
    }
    if (defaultedOptions.refetchOnReconnect === void 0) {
      defaultedOptions.refetchOnReconnect = defaultedOptions.networkMode !== "always";
    }
    if (defaultedOptions.throwOnError === void 0) {
      defaultedOptions.throwOnError = !!defaultedOptions.suspense;
    }
    if (!defaultedOptions.networkMode && defaultedOptions.persister) {
      defaultedOptions.networkMode = "offlineFirst";
    }
    if (defaultedOptions.queryFn === skipToken) {
      defaultedOptions.enabled = false;
    }
    return defaultedOptions;
  }
  defaultMutationOptions(options) {
    if (options?._defaulted) {
      return options;
    }
    return {
      ...this.#defaultOptions.mutations,
      ...options?.mutationKey && this.getMutationDefaults(options.mutationKey),
      ...options,
      _defaulted: true
    };
  }
  clear() {
    this.#queryCache.clear();
    this.#mutationCache.clear();
  }
};

// src/modules/explore/utils/queryClient.ts
var queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1e3,
      // 5 minutes default
      gcTime: 30 * 60 * 1e3,
      // 30 minutes garbage collection
      retry: 1,
      // Single retry for transient failures
      retryDelay: (attemptIndex) => Math.min(1e3 * 2 ** attemptIndex, 3e4),
      refetchOnWindowFocus: false,
      // Don't refetch on window focus (mobile)
      refetchOnReconnect: true,
      // Refetch when network reconnects
      refetchOnMount: true
      // Refetch on mount if stale
    },
    mutations: {
      retry: 0
    }
  }
});

// src/modules/explore/utils/index.ts
var DESTINATION_FALLBACK_IMAGE = "https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=400";
var DESTINATION_FALLBACK_ASSET = require_destination_fallback();
var INVALID_URL_VALUES = /* @__PURE__ */ new Set(["", "undefined", "null", "none", "n/a"]);
function isValidImageUrl(url) {
  if (typeof url !== "string") return false;
  const trimmed = url.trim();
  if (INVALID_URL_VALUES.has(trimmed.toLowerCase())) return false;
  if (/^https?:\/\//i.test(trimmed)) {
    try {
      new URL(trimmed);
      return true;
    } catch {
      return false;
    }
  }
  if (/^gs:\/\//i.test(trimmed)) return true;
  return false;
}
function firebaseStorageToHttps(gsUrl) {
  const match = /^gs:\/\/([^/]+)\/(.+)$/i.exec(gsUrl.trim());
  if (!match) return null;
  const [, bucket, path] = match;
  return `https://firebasestorage.googleapis.com/v0/b/${bucket}/o/${encodeURIComponent(path)}?alt=media`;
}
function resolveDestinationImage(destination) {
  if (!destination || typeof destination !== "object") return null;
  const d = destination;
  const candidates = [
    d.images?.[0]?.url,
    d.images?.[0]?.uri,
    d.images?.find?.((im) => im?.url)?.url,
    Array.isArray(d.photos) ? d.photos[0]?.url : void 0,
    Array.isArray(d.photos) ? d.photos[0] : void 0,
    typeof d.photos === "string" ? d.photos : void 0,
    d.imageUrl,
    d.image,
    d.photoUrl,
    d.photo,
    d.coverImage,
    d.heroImage,
    d.thumbnail
  ];
  for (const raw of candidates) {
    if (!isValidImageUrl(raw)) continue;
    let url = raw.trim();
    if (/^gs:\/\//i.test(url)) {
      const https = firebaseStorageToHttps(url);
      if (!https) continue;
      url = https;
    }
    return url;
  }
  return null;
}
function getDestinationPrimaryImage(destination) {
  const resolved = resolveDestinationImage(destination);
  if (resolved) return resolved;
  return DESTINATION_FALLBACK_IMAGE;
}

// src/modules/explore/services/ExploreService.ts
function db() {
  return getFirebaseFirestore();
}
function firestoreCollection(name) {
  return (0, import_firestore2.collection)(db(), name);
}
function docRef(collectionName, docId) {
  return (0, import_firestore2.doc)(db(), collectionName, docId);
}
async function loadCategories() {
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.EXPLORE_CATEGORIES),
      (0, import_firestore2.limit)(100)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    const categories = snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        ...data,
        createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
        updatedAt: data.updatedAt?.toDate?.() ?? /* @__PURE__ */ new Date()
      };
    }).filter((c) => c.active !== false).sort((a, b) => (a.sortOrder ?? 0) - (b.sortOrder ?? 0));
    return { data: categories, loading: false, error: null };
  } catch (err) {
    console.error("[ExploreService] loadCategories error:", err);
    return { data: null, loading: false, error: "Failed to load categories" };
  }
}
async function getCategoryById(categoryId) {
  try {
    const d = await (0, import_firestore2.getDoc)(docRef(FIRESTORE_COLLECTIONS.EXPLORE_CATEGORIES, categoryId));
    if (!d.exists()) {
      return { data: null, loading: false, error: "Category not found" };
    }
    const data = d.data();
    return {
      data: {
        id: d.id,
        ...data,
        createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
        updatedAt: data.updatedAt?.toDate?.() ?? /* @__PURE__ */ new Date()
      },
      loading: false,
      error: null
    };
  } catch (err) {
    console.error("[ExploreService] getCategoryById error:", err);
    return { data: null, loading: false, error: "Failed to load category" };
  }
}
function normalizeDestinationDoc(id, data) {
  const dest = {
    id,
    ...data,
    createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
    updatedAt: data.updatedAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
    weatherSummary: data.weatherSummary ?? null,
    reviews: data.reviews ?? [],
    travelRequirements: data.travelRequirements ?? [],
    emergencyContacts: data.emergencyContacts ?? []
  };
  const resolved = resolveDestinationImage(data);
  if (resolved) {
    if (!Array.isArray(dest.images) || dest.images.length === 0) {
      dest.images = [{ url: resolved, caption: "", credit: "" }];
    } else if (!dest.images[0]?.url) {
      dest.images[0] = { ...dest.images[0] || {}, url: resolved };
    }
  }
  return dest;
}
async function loadDestinations(options) {
  const pageSize = options?.pageSize ?? PAGINATION.DEFAULT_PAGE_SIZE;
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.DESTINATIONS),
      (0, import_firestore2.orderBy)("__name__"),
      (0, import_firestore2.limit)(pageSize + 30)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    let destinations = snap.docs.map((d) => normalizeDestinationDoc(d.id, d.data()));
    destinations = destinations.filter((d) => d.active !== false);
    if (options?.featured) {
      destinations = destinations.filter((d) => d.featured === true);
    }
    if (options?.trending) {
      destinations = destinations.filter((d) => d.trending === true);
    }
    if (options?.category) {
      const categoryLower = options.category.toLowerCase();
      destinations = destinations.filter((d) => {
        const cats = (d.categories || []).map((c) => c.toLowerCase());
        return cats.some((c) => c.includes(categoryLower) || categoryLower.includes(c));
      });
    }
    destinations.sort((a, b) => (b.popularity ?? 0) - (a.popularity ?? 0));
    const hasMore = destinations.length > pageSize;
    const sliced = destinations.slice(0, pageSize);
    return {
      data: sliced,
      total: sliced.length,
      hasMore,
      lastCursor: hasMore ? sliced[sliced.length - 1]?.id ?? null : null
    };
  } catch (err) {
    console.error("[ExploreService] loadDestinations error:", err);
    return { data: [], total: 0, hasMore: false, lastCursor: null };
  }
}
async function getDestinationById(destinationId) {
  try {
    const d = await (0, import_firestore2.getDoc)(docRef(FIRESTORE_COLLECTIONS.DESTINATIONS, destinationId));
    if (!d.exists()) {
      return { data: null, loading: false, error: "Destination not found" };
    }
    return {
      data: normalizeDestinationDoc(d.id, d.data()),
      loading: false,
      error: null
    };
  } catch (err) {
    console.error("[ExploreService] getDestinationById error:", err);
    return { data: null, loading: false, error: "Failed to load destination" };
  }
}
async function loadTrendingDestinations() {
  const result = await loadDestinations({ trending: true, pageSize: PAGINATION.TRENDING_LIMIT });
  return { data: result.data, loading: false, error: result.data.length ? null : "No trending destinations" };
}
async function loadPopularDestinations() {
  const result = await loadDestinations({ pageSize: PAGINATION.POPULAR_LIMIT });
  return { data: result.data, loading: false, error: result.data.length ? null : "No popular destinations" };
}
async function loadFeaturedDestinations() {
  const result = await loadDestinations({ featured: true, pageSize: PAGINATION.FEATURED_LIMIT });
  return { data: result.data, loading: false, error: result.data.length ? null : "No featured destinations" };
}
async function loadDestinationsByCategory(categorySlug, cursor) {
  return loadDestinations({ category: categorySlug, cursor });
}
async function loadEvents(options) {
  const pageSize = options?.pageSize ?? PAGINATION.DEFAULT_PAGE_SIZE;
  if (options?.global) {
    try {
      console.log("[ExploreService] Fetching global events from Ticketmaster");
      const result = await EventsProvider.getEvents(void 0, void 0, 2e4, void 0, true);
      return {
        data: result.events.slice(0, pageSize),
        total: result.events.length,
        hasMore: result.events.length > pageSize,
        lastCursor: null
      };
    } catch (err) {
      console.error("[ExploreService] loadEvents (global) error:", err);
      return { data: [], total: 0, hasMore: false, lastCursor: null };
    }
  }
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.EVENTS),
      (0, import_firestore2.orderBy)("__name__"),
      (0, import_firestore2.limit)(pageSize + 30)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    let events = snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        ...data,
        startDate: data.startDate?.toDate?.() ?? /* @__PURE__ */ new Date(),
        endDate: data.endDate?.toDate?.() ?? /* @__PURE__ */ new Date(),
        createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
        updatedAt: data.updatedAt?.toDate?.() ?? /* @__PURE__ */ new Date()
      };
    });
    events = events.filter((e) => e.active !== false);
    if (options?.trending) events = events.filter((e) => e.trending === true);
    if (options?.featured) events = events.filter((e) => e.featured === true);
    if (options?.category) {
      const categoryLower = options.category.toLowerCase();
      events = events.filter((e) => {
        const cat = (e.category || "").toLowerCase();
        return cat.includes(categoryLower) || categoryLower.includes(cat);
      });
    }
    if (options?.country) {
      const countryLower = options.country.toLowerCase();
      events = events.filter((e) => (e.country || "").toLowerCase().includes(countryLower));
    }
    if (options?.city) {
      const cityLower = options.city.toLowerCase();
      events = events.filter((e) => (e.city || "").toLowerCase().includes(cityLower));
    }
    if (options?.destinationId) {
      events = events.filter((e) => e.destinationId === options.destinationId);
    }
    events.sort((a, b) => a.startDate.getTime() - b.startDate.getTime());
    const hasMore = events.length > pageSize;
    const sliced = events.slice(0, pageSize);
    return {
      data: sliced,
      total: sliced.length,
      hasMore,
      lastCursor: hasMore ? sliced[sliced.length - 1]?.id ?? null : null
    };
  } catch (err) {
    console.error("[ExploreService] loadEvents error:", err);
    return { data: [], total: 0, hasMore: false, lastCursor: null };
  }
}
async function getEventById(eventId) {
  try {
    const d = await (0, import_firestore2.getDoc)(docRef(FIRESTORE_COLLECTIONS.EVENTS, eventId));
    if (!d.exists()) {
      return { data: null, loading: false, error: "Event not found" };
    }
    const data = d.data();
    return {
      data: {
        id: d.id,
        ...data,
        startDate: data.startDate?.toDate?.() ?? /* @__PURE__ */ new Date(),
        endDate: data.endDate?.toDate?.() ?? /* @__PURE__ */ new Date(),
        createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date(),
        updatedAt: data.updatedAt?.toDate?.() ?? /* @__PURE__ */ new Date()
      },
      loading: false,
      error: null
    };
  } catch (err) {
    console.error("[ExploreService] getEventById error:", err);
    return { data: null, loading: false, error: "Failed to load event" };
  }
}
async function loadJourneyFeed(options) {
  const pageSize = options?.pageSize ?? PAGINATION.FEED_LIMIT;
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.JOURNEY_FEED),
      (0, import_firestore2.orderBy)("__name__"),
      (0, import_firestore2.limit)(pageSize + 10)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    let items = snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        ...data,
        expiryDate: data.expiryDate?.toDate?.() ?? null,
        createdAt: data.createdAt?.toDate?.() ?? /* @__PURE__ */ new Date()
      };
    });
    items.sort((a, b) => {
      const p = (b.priority ?? 0) - (a.priority ?? 0);
      if (p !== 0) return p;
      return b.createdAt.getTime() - a.createdAt.getTime();
    });
    const hasMore = items.length > pageSize;
    const sliced = items.slice(0, pageSize);
    return {
      data: sliced,
      total: sliced.length,
      hasMore,
      lastCursor: hasMore ? sliced[sliced.length - 1]?.id ?? null : null
    };
  } catch (err) {
    console.error("[ExploreService] loadJourneyFeed error:", err);
    return { data: [], total: 0, hasMore: false, lastCursor: null };
  }
}
async function loadRecommendations(userId) {
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.RECOMMENDATIONS),
      (0, import_firestore2.orderBy)("__name__"),
      (0, import_firestore2.limit)(PAGINATION.DEFAULT_PAGE_SIZE)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    const recommendations = snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        ...data,
        expiresAt: data.expiresAt?.toDate?.() ?? null
      };
    }).filter((r) => r.active !== false).sort((a, b) => (b.score ?? 0) - (a.score ?? 0));
    return { data: recommendations, loading: false, error: null };
  } catch (err) {
    console.error("[ExploreService] loadRecommendations error:", err);
    return { data: null, loading: false, error: "Failed to load recommendations" };
  }
}
async function loadNearbyPlaces(options) {
  const pageSize = options?.pageSize ?? PAGINATION.NEARBY_LIMIT;
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.NEARBY_PLACES),
      (0, import_firestore2.orderBy)("__name__"),
      (0, import_firestore2.limit)(pageSize + 30)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    let items = snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        ...data
      };
    });
    items = items.filter((i) => i.active !== false);
    if (options?.types?.length) {
      const typeSet = new Set(options.types);
      items = items.filter((i) => typeSet.has(i.type));
    }
    if (options.sortBy === "rating") {
      items.sort((a, b) => (b.rating ?? 0) - (a.rating ?? 0));
    } else if (options.sortBy === "popularity") {
      items.sort((a, b) => (b.reviewCount || 0) - (a.reviewCount || 0));
    } else {
      items.sort((a, b) => a.distance - b.distance);
    }
    const hasMore = items.length > pageSize;
    const sliced = items.slice(0, pageSize);
    return {
      data: sliced,
      total: sliced.length,
      hasMore,
      lastCursor: hasMore ? sliced[sliced.length - 1]?.id ?? null : null
    };
  } catch (err) {
    console.error("[ExploreService] loadNearbyPlaces error:", err);
    return { data: [], total: 0, hasMore: false, lastCursor: null };
  }
}
async function saveDestination(userId, destinationId, destination) {
  try {
    let alreadySaved = false;
    try {
      const existingQ = (0, import_firestore2.query)(
        firestoreCollection(FIRESTORE_COLLECTIONS.SAVED_PLACES),
        (0, import_firestore2.where)("userId", "==", userId),
        (0, import_firestore2.limit)(50)
      );
      const existingSnap = await (0, import_firestore2.getDocs)(existingQ);
      alreadySaved = existingSnap.docs.some(
        (d) => d.data().destinationId === destinationId
      );
    } catch (checkErr) {
      console.warn("[ExploreService] Duplicate check failed, proceeding:", checkErr);
    }
    if (alreadySaved) {
      return true;
    }
    const savedRef = firestoreCollection(
      `${FIRESTORE_COLLECTIONS.SAVED_PLACES}`
    );
    await (0, import_firestore2.addDoc)(savedRef, {
      userId,
      destinationId,
      name: destination?.name || "",
      country: destination?.country || "",
      countryCode: destination?.countryCode || "",
      imageUrl: destination ? getDestinationPrimaryImage(destination) : "",
      description: destination?.description || "",
      rating: destination?.rating || 0,
      coordinates: destination?.coordinates || { lat: 0, lng: 0 },
      city: destination?.city || "",
      savedAt: import_firestore2.Timestamp.now(),
      type: "destination"
    });
    try {
      const destRef = docRef(FIRESTORE_COLLECTIONS.DESTINATIONS, destinationId);
      await (0, import_firestore2.setDoc)(destRef, { popularity: (0, import_firestore2.increment)(1) }, { merge: true });
    } catch (popErr) {
      console.warn("[ExploreService] Popularity increment skipped (destination may not exist in Firestore):", popErr);
    }
    return true;
  } catch (err) {
    console.error("[ExploreService] saveDestination error:", err);
    return false;
  }
}
async function unsaveDestination(userId, destinationId) {
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.SAVED_PLACES),
      (0, import_firestore2.where)("userId", "==", userId),
      (0, import_firestore2.limit)(50)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    const deletePromises = snap.docs.filter((d) => d.data().destinationId === destinationId).map((d) => (0, import_firestore2.deleteDoc)(d.ref));
    await Promise.all(deletePromises);
    return true;
  } catch (err) {
    console.error("[ExploreService] unsaveDestination error:", err);
    return false;
  }
}
async function trackRecentlyViewed(userId, destinationId) {
  try {
    const userRef = (0, import_firestore2.doc)(db(), "users", userId);
    await (0, import_firestore2.setDoc)(
      userRef,
      {
        recentlyViewed: (0, import_firestore2.arrayUnion)({
          destinationId,
          viewedAt: import_firestore2.Timestamp.now()
        })
      },
      { merge: true }
    );
  } catch (err) {
    console.error("[ExploreService] trackRecentlyViewed error:", err);
  }
}
async function getRecentlyViewed(userId) {
  try {
    const userDoc = await (0, import_firestore2.getDoc)((0, import_firestore2.doc)(db(), "users", userId));
    if (!userDoc.exists()) return [];
    const data = userDoc.data();
    const recent = data?.recentlyViewed ?? [];
    if (!recent.length) return [];
    const destIds = recent.slice(0, PAGINATION.RECENTLY_VIEWED_LIMIT).map((r) => r.destinationId);
    const dests = [];
    for (const id of destIds) {
      const destResult = await getDestinationById(id);
      if (destResult.data) {
        dests.push(destResult.data);
      }
    }
    return dests;
  } catch (err) {
    console.error("[ExploreService] getRecentlyViewed error:", err);
    return [];
  }
}
async function trackAnalyticsEvent(userId, event, metadata = {}) {
  try {
    const analyticsRef = firestoreCollection(FIRESTORE_COLLECTIONS.EXPLORE_ANALYTICS);
    await (0, import_firestore2.addDoc)(analyticsRef, {
      userId,
      event,
      metadata,
      timestamp: import_firestore2.Timestamp.now()
    });
  } catch (err) {
    console.error("[ExploreService] trackAnalyticsEvent error:", err);
  }
}
async function saveSearchQuery(userId, queryText) {
  try {
    const historyRef = firestoreCollection(FIRESTORE_COLLECTIONS.SEARCH_HISTORY);
    await (0, import_firestore2.addDoc)(historyRef, {
      userId,
      query: queryText,
      searchedAt: import_firestore2.Timestamp.now()
    });
  } catch (err) {
    console.error("[ExploreService] saveSearchQuery error:", err);
  }
}
async function getSearchHistory(userId) {
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.SEARCH_HISTORY),
      (0, import_firestore2.where)("userId", "==", userId),
      (0, import_firestore2.limit)(PAGINATION.SEARCH_LIMIT * 3)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    return snap.docs.map((d) => {
      const data = d.data();
      return {
        id: d.id,
        text: data.query,
        type: "destination",
        subtext: "Recent search",
        icon: "history",
        searchedAt: data.searchedAt?.toDate?.() ?? /* @__PURE__ */ new Date(0)
      };
    }).sort((a, b) => b.searchedAt.getTime() - a.searchedAt.getTime()).slice(0, PAGINATION.SEARCH_LIMIT).map(({ searchedAt, ...rest }) => rest);
  } catch (err) {
    console.error("[ExploreService] getSearchHistory error:", err);
    return [];
  }
}
async function clearSearchHistory(userId) {
  try {
    const q = (0, import_firestore2.query)(
      firestoreCollection(FIRESTORE_COLLECTIONS.SEARCH_HISTORY),
      (0, import_firestore2.where)("userId", "==", userId)
    );
    const snap = await (0, import_firestore2.getDocs)(q);
    const deletePromises = snap.docs.map((d) => (0, import_firestore2.deleteDoc)(d.ref));
    await Promise.all(deletePromises);
  } catch (err) {
    console.error("[ExploreService] clearSearchHistory error:", err);
  }
}
var ExploreService = {
  // Categories
  loadCategories,
  getCategoryById,
  // Destinations
  loadDestinations,
  getDestinationById,
  loadTrendingDestinations,
  loadPopularDestinations,
  loadFeaturedDestinations,
  loadDestinationsByCategory,
  // Events
  loadEvents,
  getEventById,
  // Journey Feed
  loadJourneyFeed,
  // Recommendations
  loadRecommendations,
  // Nearby
  loadNearbyPlaces,
  // User Actions
  saveDestination,
  unsaveDestination,
  trackRecentlyViewed,
  getRecentlyViewed,
  // Analytics
  trackAnalyticsEvent,
  // Search History
  saveSearchQuery,
  getSearchHistory,
  clearSearchHistory
};

// src/modules/explore/services/discovery/providers/FirestoreDestinationProvider.ts
function num(v) {
  if (typeof v !== "number") return null;
  if (!Number.isFinite(v)) return null;
  return v;
}
function extractCoordinates(dest) {
  const tryPair = (a, b) => {
    const lat = num(a);
    const lng = num(b);
    if (lat === null || lng === null) return null;
    if (lat === 0 && lng === 0) return null;
    if (lat < -90 || lat > 90 || lng < -180 || lng > 180) return null;
    return { lat, lng };
  };
  const topLevel = tryPair(dest.lat, dest.lng);
  if (topLevel) return topLevel;
  const topLevel2 = tryPair(dest.latitude, dest.longitude);
  if (topLevel2) return topLevel2;
  if (dest.coordinates && typeof dest.coordinates === "object") {
    const c = dest.coordinates;
    const nested = tryPair(c.lat ?? c.latitude, c.lng ?? c.longitude);
    if (nested) return nested;
  }
  if (dest.location && typeof dest.location === "object") {
    const l = dest.location;
    const loc = tryPair(l.latitude ?? l.lat, l.longitude ?? l.lng);
    if (loc) return loc;
  }
  return null;
}
function haversine2(lat1, lng1, lat2, lng2) {
  const R = 6371;
  const dLat = (lat2 - lat1) * Math.PI / 180;
  const dLng = (lng2 - lng1) * Math.PI / 180;
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLng / 2) * Math.sin(dLng / 2);
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
function normalizeName2(name) {
  return name.toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function toTicsDestination(dest) {
  const coords = extractCoordinates(dest);
  if (!coords) return null;
  return {
    id: dest.id,
    sourceId: `firestore/${dest.id}`,
    name: dest.name,
    latitude: coords.lat,
    longitude: coords.lng,
    description: dest.description || void 0,
    country: dest.country,
    countryCode: dest.countryCode,
    city: dest.city,
    categories: dest.categories?.length ? dest.categories : ["Curated"],
    imageUrl: dest.images?.[0]?.url || void 0,
    rating: dest.rating,
    reviewCount: dest.reviewCount,
    source: "firestore",
    sourceConfidence: 0.95,
    // manually curated → highest base confidence
    qualityScore: 0.9,
    metadata: { curated: true }
  };
}
var FirestoreDestinationProvider = {
  name: "firestore",
  kind: "fallback",
  isAvailable() {
    return true;
  },
  async searchNearby({ lat, lng, radiusKm = 100, limit: limit2 = 30 }) {
    try {
      const result = await ExploreService.loadDestinations({ pageSize: limit2 });
      const loaded = result.data || [];
      const radius = radiusKm || 100;
      const mapped = loaded.map(toTicsDestination).filter((d) => d !== null).map((d) => {
        const dist = haversine2(lat, lng, d.latitude, d.longitude);
        return { d, dist };
      });
      const within = mapped.filter(({ dist }) => dist <= radius).sort((a, b) => a.dist - b.dist).slice(0, limit2).map(({ d, dist }) => {
        d.distanceKm = Math.round(dist * 100) / 100;
        return d;
      });
      console.log(
        `[FirestoreDestinationProvider] loaded=${loaded.length} validCoordinates=${mapped.length} withinRadius=${within.length} returned=${Math.min(within.length, limit2)}`
      );
      return within;
    } catch (err) {
      console.warn("[FirestoreDestinationProvider] ERROR searchNearby:", err);
      return [];
    }
  },
  async searchByQuery(query2, options = {}) {
    const q = normalizeName2(query2.trim());
    if (q.length < 2) return [];
    try {
      const result = await ExploreService.loadDestinations({ pageSize: options.limit || 30 });
      const matches = (result.data || []).map(toTicsDestination).filter((d) => d !== null).filter((d) => normalizeName2(d.name).includes(q)).sort((a, b) => {
        const aExact = normalizeName2(a.name) === q ? 0 : 1;
        const bExact = normalizeName2(b.name) === q ? 0 : 1;
        return aExact - bExact;
      }).slice(0, options.limit || 10);
      return matches;
    } catch (err) {
      console.warn("[FirestoreDestinationProvider] ERROR searchByQuery:", err);
      return [];
    }
  }
};

// src/modules/explore/services/discovery/providers/WikidataProvider.ts
var WB_ACTION_URL = "https://www.wikidata.org/w/api.php";
var REQUEST_TIMEOUT_MS = 5e3;
var cache2 = /* @__PURE__ */ new Map();
var inFlight2 = /* @__PURE__ */ new Map();
async function getJson(url) {
  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), REQUEST_TIMEOUT_MS);
  try {
    const res = await fetch(url, { signal: ctrl.signal });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return res.json();
  } finally {
    clearTimeout(timer);
  }
}
function countryCodeFromQ(qid) {
  const map = {
    Q183: { country: "Germany", countryCode: "DE" },
    Q142: { country: "France", countryCode: "FR" },
    Q145: { country: "United Kingdom", countryCode: "GB" },
    Q30: { country: "United States", countryCode: "US" },
    Q17: { country: "Japan", countryCode: "JP" },
    Q148: { country: "China", countryCode: "CN" },
    Q668: { country: "India", countryCode: "IN" },
    Q1036: { country: "Uganda", countryCode: "UG" },
    Q258: { country: "South Africa", countryCode: "ZA" },
    Q953: { country: "Kenya", countryCode: "KE" },
    Q924: { country: "Tanzania", countryCode: "TZ" }
  };
  return map[qid] || {};
}
async function enrichDestination(dest) {
  if (!dest || !dest.name) return {};
  const key = dest.name.toLowerCase();
  const hit = cache2.get(key);
  if (hit) return hit;
  const existing = inFlight2.get(key);
  if (existing) return existing;
  const promise = (async () => {
    try {
      const searchUrl = `${WB_ACTION_URL}?action=wbsearchentities&format=json&language=en&uselang=en&type=item&limit=3&search=${encodeURIComponent(dest.name)}`;
      const searchRes = await getJson(searchUrl);
      const entity = searchRes.search?.[0];
      if (!entity) return {};
      const entityUrl = `${WB_ACTION_URL}?action=wbgetentities&format=json&ids=${entity.id}&props=descriptions|claims|sitelinks|labels`;
      const res = await getJson(entityUrl);
      const data = res.entities?.[entity.id];
      if (!data) return {};
      const partial = {
        metadata: { wikidataId: entity.id, wikidataSitelinks: data.sitelinks || entity.sitelinks || 0 }
      };
      if (!dest.description && entity.description) partial.description = entity.description;
      if (!dest.country) {
        const countryQ = data.claims?.P17?.[0]?.mainsnak?.datavalue?.value;
        const cc = countryQ?.id ? countryCodeFromQ(countryQ.id) : {};
        if (cc.country) partial.country = cc.country;
        if (cc.countryCode) partial.countryCode = cc.countryCode;
      }
      if (!dest.imageUrl) {
        const imageClaim = data.claims?.P18?.[0]?.mainsnak?.datavalue?.value;
        if (imageClaim) {
          partial.imageUrl = `https://commons.wikimedia.org/wiki/Special:FilePath/${encodeURIComponent(imageClaim.replace(/ /g, "_"))}?width=800`;
        }
      }
      cache2.set(key, partial);
      return partial;
    } catch {
      return {};
    }
  })();
  inFlight2.set(key, promise);
  try {
    return await promise;
  } finally {
    inFlight2.delete(key);
  }
}
async function enrichDestinations(dests) {
  const enriched = await Promise.all(
    dests.slice(0, 12).map(async (d) => {
      const partial = await enrichDestination(d);
      return Object.assign(d, partial);
    })
  );
  const count = enriched.filter((d) => d.metadata?.wikidataId).length;
  console.log(
    `[WikidataProvider]
Wikidata:
enrichmentMatches=${count}
standaloneCandidates=0 (coordinateless search results are used for enrichment only)`
  );
  return dests;
}
var WikidataProvider = {
  name: "wikidata",
  kind: "enrichment",
  isAvailable() {
    return true;
  },
  // OSM is the primary; Wikidata search is a secondary, best-effort search
  // source for cities / landmarks so "Paris" surfaces the city.
  async searchNearby() {
    return [];
  },
  async searchByQuery(query2, options = {}) {
    if (!query2 || query2.trim().length < 2) return [];
    try {
      const searchUrl = `${WB_ACTION_URL}?action=wbsearchentities&format=json&language=en&uselang=en&type=item&limit=${options.limit || 10}&search=${encodeURIComponent(query2.trim())}`;
      const res = await getJson(searchUrl);
      const items = res.search || [];
      const dests = items.filter((it) => it.id && it.label).map((it) => ({
        id: `tics:wikidata:${it.id}`,
        sourceId: it.id,
        name: it.label,
        latitude: 0,
        longitude: 0,
        description: it.description,
        categories: ["Curated"],
        source: "wikidata",
        sourceConfidence: 0.5,
        qualityScore: 0.5,
        metadata: { wikidataId: it.id, sitelinks: it.sitelinks || 0 }
      }));
      return dests;
    } catch {
      return [];
    }
  }
};

// src/modules/explore/services/discovery/providers/GeoNamesProvider.ts
var GEONAMES_BASE = "https://api.geonames.org/searchJSON";
var REQUEST_TIMEOUT_MS2 = 5e3;
function geonamesUsername() {
  return process.env.EXPO_PUBLIC_GEONAMES_USERNAME || "";
}
async function getJson2(url) {
  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), REQUEST_TIMEOUT_MS2);
  try {
    const res = await fetch(url, { signal: ctrl.signal });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return res.json();
  } finally {
    clearTimeout(timer);
  }
}
var GeoNamesProvider = {
  name: "geonames",
  kind: "enrichment",
  isAvailable() {
    return Boolean(geonamesUsername());
  },
  async searchNearby() {
    return [];
  },
  async searchByQuery(query2, options = {}) {
    if (!geonamesUsername() || !query2 || query2.trim().length < 2) return [];
    try {
      const params = new URLSearchParams({
        q: query2.trim(),
        username: geonamesUsername(),
        maxRows: String(options.limit || 8),
        style: "SHORT",
        lang: "en",
        featureClass: "P"
      });
      const res = await getJson2(`${GEONAMES_BASE}?${params.toString()}`);
      const hits = (res.geonames || []).filter((h) => typeof h.lat === "number" && typeof h.lng === "number");
      return hits.map((h) => ({
        id: `tics:geonames:${h.geonameId}`,
        sourceId: String(h.geonameId),
        name: h.name,
        latitude: h.lat,
        longitude: h.lng,
        country: h.countryName,
        countryCode: h.countryCode,
        region: h.adminName1,
        city: h.name,
        categories: ["City"],
        type: "city",
        source: "geonames",
        sourceConfidence: 0.7,
        qualityScore: 0.7,
        metadata: { population: h.population || 0 }
      }));
    } catch {
      return [];
    }
  }
};

// src/modules/explore/services/discovery/providers/config.ts
var FALSE_VALUES = /* @__PURE__ */ new Set(["false", "0", "no", "off"]);
var TRUE_VALUES = /* @__PURE__ */ new Set(["true", "1", "yes", "on"]);
function explicitFlag() {
  const raw = process.env.TICS_ENABLE_GOOGLE_PLACES ?? process.env.EXPO_PUBLIC_TICS_ENABLE_GOOGLE_PLACES;
  if (typeof raw !== "string") return void 0;
  const v = raw.trim().toLowerCase();
  if (TRUE_VALUES.has(v)) return true;
  if (FALSE_VALUES.has(v)) return false;
  return void 0;
}
var loggedDisabled = false;
function isGooglePlacesEnabled() {
  const flag = explicitFlag();
  const key = process.env.EXPO_PUBLIC_GOOGLE_PLACES_API_KEY;
  const enabled = flag === true && Boolean(key);
  if (!enabled && !loggedDisabled) {
    loggedDisabled = true;
    console.log("[DestinationProviders] Google Places: DISABLED");
  }
  return enabled;
}
function googlePlacesAvailable() {
  try {
    return isGooglePlacesEnabled();
  } catch {
    return false;
  }
}
function isGoogleMapsEnabled() {
  const key = process.env.EXPO_PUBLIC_GOOGLE_MAPS_API_KEY;
  return Boolean(key);
}
function getProviderConfigSummary() {
  return {
    openstreetmap: true,
    geonames: Boolean(process.env.EXPO_PUBLIC_GEONAMES_USERNAME),
    wikidata: true,
    firestore: true,
    google_places: googlePlacesAvailable(),
    google_maps: isGoogleMapsEnabled()
  };
}

// src/modules/explore/services/discovery/PlacesProvider.ts
var GOOGLE_PLACE_TYPES = [
  "tourist_attraction",
  "museum",
  "park",
  "beach",
  "natural_feature",
  "zoo",
  "aquarium",
  "art_gallery",
  "amusement_park",
  "campground",
  "church",
  "mosque",
  "temple",
  "historical_landmark",
  "point_of_interest"
];
var PLACES_API_BASE = "https://maps.googleapis.com/maps/api/place/nearbysearch/json";
var PLACE_PHOTO_BASE = "https://maps.googleapis.com/maps/api/place/photo";
function getApiKey() {
  return process.env.EXPO_PUBLIC_GOOGLE_PLACES_API_KEY || "";
}
function buildNearbyUrl(lat, lng, radius, type) {
  const key = getApiKey();
  if (!key) return "";
  const params = new URLSearchParams({
    location: `${lat},${lng}`,
    radius: String(radius),
    type,
    key,
    language: "en"
  });
  return `${PLACES_API_BASE}?${params.toString()}`;
}
function buildPhotoUrl(photoRef, maxWidth = 400) {
  const key = getApiKey();
  if (!key) return "";
  return `${PLACE_PHOTO_BASE}?maxwidth=${maxWidth}&photoreference=${photoRef}&key=${key}`;
}
async function fetchByType(lat, lng, radius, type) {
  const url = buildNearbyUrl(lat, lng, radius, type);
  if (!url) return [];
  try {
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), PROVIDER_TIMEOUTS.GOOGLE_PLACES);
    const response = await fetch(url, {
      signal: controller.signal,
      headers: { "Accept-Language": "en" }
    });
    clearTimeout(timeout);
    if (!response.ok) {
      console.warn(`[PlacesProvider] HTTP ${response.status} for type ${type}`);
      return [];
    }
    const data = await response.json();
    if (data.status === "OK" && data.results?.length) {
      return data.results;
    }
    if (data.status === "ZERO_RESULTS") return [];
    console.warn(`[PlacesProvider] API status ${data.status} for ${type}:`, data.error_message);
    return [];
  } catch (err) {
    if (err?.name === "AbortError" || err?.code === 20) {
      console.warn(`[PlacesProvider] Timeout for type ${type}`);
    } else {
      console.error(`[PlacesProvider] Error fetching ${type}:`, err);
    }
    return [];
  }
}
function transformToNearbyItem(result, userLocation) {
  const coords = {
    lat: result.geometry.location.lat,
    lng: result.geometry.location.lng
  };
  const distance = calculateHaversineDistance(userLocation, coords);
  const firstPhoto = result.photos?.[0];
  const type = mapGoogleTypeToNearbyType(result.types);
  return {
    id: `gp_${result.place_id}`,
    name: result.name,
    type,
    description: result.vicinity || result.formatted_address || "",
    imageUrl: firstPhoto ? buildPhotoUrl(firstPhoto.photo_reference) : "https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=400",
    coordinates: coords,
    distance: Math.round(distance * 100) / 100,
    rating: result.rating || 0,
    reviewCount: result.user_ratings_total || 0,
    priceLevel: result.price_level ?? 1,
    openingHours: result.opening_hours?.open_now ? "Open now" : "",
    phone: "",
    website: "",
    tags: result.types,
    destinationId: null,
    isOpen: result.opening_hours?.open_now ?? true
  };
}
function mapGoogleTypeToNearbyType(types) {
  const typeMap = {
    tourist_attraction: "attraction",
    museum: "museum",
    park: "park",
    beach: "beach",
    lodging: "hotel",
    restaurant: "restaurant",
    cafe: "restaurant",
    shopping_mall: "mall",
    zoo: "attraction",
    aquarium: "attraction",
    art_gallery: "museum",
    amusement_park: "attraction",
    stadium: "sports_venue",
    night_club: "theater",
    movie_theater: "theater",
    casino: "theater",
    church: "historical_site",
    mosque: "historical_site",
    temple: "historical_site"
  };
  for (const t of types) {
    if (typeMap[t]) return typeMap[t];
  }
  return "attraction";
}
function calculateHaversineDistance(from, to) {
  const R = 6371;
  const dLat = toRad2(to.lat - from.lat);
  const dLon = toRad2(to.lng - from.lng);
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(toRad2(from.lat)) * Math.cos(toRad2(to.lat)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function toRad2(deg) {
  return deg * (Math.PI / 180);
}
var placesCache = /* @__PURE__ */ new Map();
var PLACES_CACHE_TTL_MS = 3e4;
function getCacheKey(lat, lng, radiusKm) {
  return `${lat.toFixed(4)},${lng.toFixed(4)},${radiusKm}`;
}
var PlacesProvider = {
  /**
   * Fetch nearby places from Google Places API across all configured categories.
   * Runs all category queries concurrently using Promise.allSettled.
   * Merges results, removes duplicates, computes Haversine distance, and sorts.
   *
   * @param lat - User's latitude
   * @param lng - User's longitude
   * @param radiusKm - Search radius in km (default: 100)
   * @returns Merged, sorted array of NearbyItem
   */
  async getNearbyPlaces(lat, lng, radiusKm = 100) {
    if (!googlePlacesAvailable()) {
      return { places: [], provider: "google_places", fetchedAt: Date.now() };
    }
    const cacheKey2 = getCacheKey(lat, lng, radiusKm);
    const cached = placesCache.get(cacheKey2);
    if (cached && cached.expiresAt > Date.now()) {
      return cached.result;
    }
    const location = { lat, lng };
    const radiusMeters = radiusKm * 1e3;
    const results = await Promise.allSettled(
      GOOGLE_PLACE_TYPES.map(
        (type) => fetchByType(lat, lng, radiusMeters, type)
      )
    );
    const allResults = [];
    for (const result2 of results) {
      if (result2.status === "fulfilled" && result2.value.length > 0) {
        allResults.push(...result2.value);
      }
    }
    const seen = /* @__PURE__ */ new Set();
    const uniqueResults = allResults.filter((r) => {
      if (seen.has(r.place_id)) return false;
      seen.add(r.place_id);
      return true;
    });
    const places = uniqueResults.map(
      (r) => transformToNearbyItem(r, location)
    );
    places.sort((a, b) => {
      const distDiff = a.distance - b.distance;
      if (Math.abs(distDiff) > 1) return distDiff;
      return (b.rating ?? 0) - (a.rating ?? 0);
    });
    console.log(
      `[PlacesProvider] Fetched ${places.length} places (${uniqueResults.length} unique) within ${radiusKm}km`
    );
    const result = {
      places: places.slice(0, 30),
      // Return top 30
      provider: "google_places",
      fetchedAt: Date.now()
    };
    placesCache.set(cacheKey2, { result, expiresAt: Date.now() + PLACES_CACHE_TTL_MS });
    return result;
  },
  /**
   * Get place types used by this provider.
   */
  getSupportedTypes() {
    return GOOGLE_PLACE_TYPES;
  }
};

// src/modules/explore/services/discovery/providers/GooglePlacesProvider.ts
function toTics(item) {
  return {
    id: `tics:google_places:${item.id}`,
    sourceId: item.id,
    name: item.name,
    latitude: item.coordinates.lat,
    longitude: item.coordinates.lng,
    description: void 0,
    categories: item.tags || ["Attraction"],
    imageUrl: item.imageUrl || void 0,
    rating: item.rating,
    reviewCount: item.reviewCount,
    distanceKm: item.distance,
    phone: item.phone,
    website: item.website,
    source: "google_places",
    sourceConfidence: 0.8,
    qualityScore: 0.7
  };
}
var GooglePlacesProvider = {
  name: "google_places",
  kind: "optional",
  isAvailable() {
    return googlePlacesAvailable();
  },
  async searchNearby({ lat, lng, radiusKm = 50, limit: limit2 = 20 }) {
    if (!this.isAvailable()) {
      return [];
    }
    try {
      const result = await PlacesProvider.getNearbyPlaces(lat, lng, radiusKm);
      return result.places.slice(0, limit2).map(toTics);
    } catch (err) {
      console.warn("[GooglePlacesProvider] ERROR searchNearby (optional, ignored):", err);
      return [];
    }
  },
  async searchByQuery() {
    return [];
  }
};

// src/modules/explore/services/discovery/providers/index.ts
var ALL_PROVIDERS = [
  OpenStreetMapProvider,
  WikidataProvider,
  GeoNamesProvider,
  FirestoreDestinationProvider,
  GooglePlacesProvider
];
function getEnabledDestinationProviders() {
  return ALL_PROVIDERS.filter((p) => p.isAvailable());
}
function getEnabledProviderNames() {
  return getEnabledDestinationProviders().map((p) => p.name);
}
function logProviderConfig() {
  const summary = getProviderConfigSummary();
  console.log("[DestinationAggregator] Provider config:", JSON.stringify(summary));
  console.log(`[DestinationAggregator] Providers enabled: ${getEnabledProviderNames().join(", ")}`);
}

// src/modules/explore/services/discovery/DestinationAggregator.ts
var loggedConfig = false;
function normalizeName3(name) {
  return name.toLowerCase().replace(/^(the|a|an)\s+/, "").replace(/[^a-z0-9]+/g, "");
}
function haversine3(lat1, lng1, lat2, lng2) {
  const R = 6371;
  const dLat = (lat2 - lat1) * Math.PI / 180;
  const dLng = (lng2 - lng1) * Math.PI / 180;
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLng / 2) * Math.sin(dLng / 2);
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
function dedupKey(d) {
  if (d.sourceId) return `${d.source}:${d.sourceId}`;
  const lat = d.latitude.toFixed(2);
  const lng = d.longitude.toFixed(2);
  return `${normalizeName3(d.name)}_${lat}_${lng}`;
}
function bestDescription(a, b) {
  if (!a) return b;
  if (!b) return a;
  return a.length >= b.length ? a : b;
}
function bestImage(a, b) {
  return a || b;
}
function mergePair(base, incoming) {
  const sources = Array.from(/* @__PURE__ */ new Set([...base.sources || [base.source], ...incoming.sources || [incoming.source]]));
  const categories = Array.from(/* @__PURE__ */ new Set([...base.categories || [], ...incoming.categories || []]));
  const tags = Array.from(/* @__PURE__ */ new Set([...base.tags || [], ...incoming.tags || []]));
  const useFs = incoming.source === "firestore";
  const preferredDesc = useFs ? bestDescription(incoming.description, base.description) : bestDescription(base.description, incoming.description);
  const preferredImg = useFs ? bestImage(incoming.imageUrl, base.imageUrl) : bestImage(base.imageUrl, incoming.imageUrl);
  return {
    ...base,
    ...incoming,
    name: base.name || incoming.name,
    latitude: base.latitude || incoming.latitude,
    longitude: base.longitude || incoming.longitude,
    description: preferredDesc,
    imageUrl: preferredImg,
    categories,
    tags,
    sources,
    source: base.source,
    sourceConfidence: Math.max(base.sourceConfidence || 0, incoming.sourceConfidence || 0),
    qualityScore: Math.max(base.qualityScore || 0, incoming.qualityScore || 0),
    rating: base.rating ?? incoming.rating,
    reviewCount: base.reviewCount ?? incoming.reviewCount,
    country: base.country || incoming.country,
    countryCode: base.countryCode || incoming.countryCode,
    metadata: { ...base.metadata || {}, ...incoming.metadata || {} }
  };
}
function mergeDestinations(dests) {
  const byKey = /* @__PURE__ */ new Map();
  for (const d of dests) {
    const key = dedupKey(d);
    const existing = byKey.get(key);
    if (!existing) byKey.set(key, { ...d });
    else byKey.set(key, mergePair(existing, d));
  }
  return Array.from(byKey.values());
}
function ensureConfigLogged() {
  if (!loggedConfig) {
    logProviderConfig();
    loggedConfig = true;
  }
}
function enrichInBackground(dests) {
  if (!dests.length) return;
  try {
    void enrichDestinations(dests).catch(() => void 0);
  } catch {
  }
}
function matchesCategory(d, category) {
  if (!category || category === "all") return true;
  const c = category.toLowerCase();
  const hay = [d.type || "", ...d.categories || [], ...d.tags || [], d.name].join(" ").toLowerCase();
  return hay.includes(c);
}
async function nearby({
  lat,
  lng,
  radiusKm = 50,
  category,
  limit: limit2 = 30,
  budgetMs
}) {
  ensureConfigLogged();
  if (typeof lat !== "number" || typeof lng !== "number") return [];
  const radius = radiusKm || 50;
  const osmBudget = budgetMs;
  const results = await Promise.allSettled([
    OpenStreetMapProvider.searchNearby({ lat, lng, radiusKm: radius, category, limit: limit2 * 2, budgetMs: osmBudget }),
    FirestoreDestinationProvider.searchNearby({ lat, lng, radiusKm: radius, limit: limit2 }),
    GooglePlacesProvider.isAvailable() ? GooglePlacesProvider.searchNearby({ lat, lng, radiusKm: radius, limit: limit2 }) : Promise.resolve([])
  ]);
  const raw = [];
  const providerResult = {};
  results.forEach((res, idx) => {
    const names = ["OSM", "Firestore", "Google"];
    if (res.status === "fulfilled") {
      providerResult[names[idx]] = res.value.length;
      raw.push(...res.value);
    } else {
      providerResult[names[idx]] = -1;
    }
  });
  const merged = mergeDestinations(raw);
  const filtered = merged.filter((d) => matchesCategory(d, category));
  filtered.sort((a, b) => (a.distanceKm ?? 1e9) - (b.distanceKm ?? 1e9));
  enrichInBackground(filtered);
  const osmOutcome = getLastOsmOutcome().status;
  console.log(
    `[DestinationAggregator] Raw candidates: ${raw.length} | OSM ${providerResult.OSM} (${osmOutcome}) \xB7 Firestore ${providerResult.Firestore} \xB7 Google ${providerResult.Google ?? 0} | After deduplication: ${merged.length} | Filtered: ${filtered.length}`
  );
  return filtered.slice(0, limit2);
}
function hasUsableCoordinates(d) {
  return !(d.latitude === 0 && d.longitude === 0);
}
async function search(query2, options = {}) {
  ensureConfigLogged();
  const q = query2.trim();
  if (q.length < 2) return [];
  const providers = getEnabledDestinationProviders();
  const results = await Promise.allSettled(
    providers.filter((p) => p.searchByQuery).map((p) => p.searchByQuery(q, options))
  );
  const raw = [];
  for (const res of results) {
    if (res.status === "fulfilled") raw.push(...res.value);
  }
  const merged = mergeDestinations(raw).filter(hasUsableCoordinates);
  const ranked = rankSearchResults(merged, q);
  console.log(`[DestinationAggregator] Search "${q}" -> ${ranked.length} unique destinations`);
  return ranked.slice(0, options.limit || 15);
}
function rankSearchResults(dests, query2) {
  const q = normalizeName3(query2);
  const scored = dests.map((d) => {
    const n = normalizeName3(d.name);
    let score = 0;
    if (n === q) score += 100;
    else if (n.startsWith(q)) score += 60;
    else if (n.includes(q)) score += 30;
    if (d.type === "city" || d.categories?.includes("City")) score += 25;
    if (d.country) score += 10;
    score += (d.qualityScore || 0) * 15;
    score += Math.min(d.metadata?.sitelinks || 0, 50) / 3;
    return { d, score };
  });
  scored.sort((a, b) => b.score - a.score || (a.d.distanceKm ?? 1e9) - (b.d.distanceKm ?? 1e9));
  return scored.map((s) => s.d);
}
var NEARBY_TYPES = {
  museum: "museum",
  art_gallery: "museum",
  park: "park",
  restaurant: "restaurant",
  cafe: "restaurant",
  bar: "restaurant",
  hotel: "hotel",
  lodging: "hotel",
  airport: "airport",
  beach: "beach",
  castle: "historical_site",
  monument: "historical_site",
  historic_site: "historical_site",
  archaeological_site: "historical_site",
  place_of_worship: "historical_site",
  landmark: "historical_site",
  theme_park: "attraction",
  amusement_park: "attraction",
  theater: "theater",
  movie_theater: "theater",
  entertainment: "attraction"
};
function mapType(type) {
  if (type && NEARBY_TYPES[type]) return NEARBY_TYPES[type];
  return "attraction";
}
function toNearbyItems(dests, center) {
  return dests.map((d) => {
    let distance = d.distanceKm;
    if (typeof distance !== "number" && center) {
      distance = haversine3(center.lat, center.lng, d.latitude, d.longitude);
    }
    return {
      id: d.id,
      name: d.name,
      type: mapType(d.type),
      description: d.description || "",
      imageUrl: d.imageUrl || "",
      coordinates: { lat: d.latitude, lng: d.longitude },
      distance: Math.round((distance ?? 0) * 100) / 100,
      rating: d.rating ?? 0,
      reviewCount: d.reviewCount ?? 0,
      priceLevel: 1,
      openingHours: typeof d.openingHours === "string" ? d.openingHours : "",
      phone: d.phone || "",
      website: d.website || "",
      tags: [...d.tags || [], ...d.categories || []],
      destinationId: d.id,
      isOpen: d.isOpen
    };
  });
}
function toDestination(d) {
  return {
    id: d.id,
    name: d.name,
    slug: d.name.toLowerCase().replace(/[^a-z0-9]+/g, "-"),
    description: d.description || "",
    country: d.country || "",
    countryCode: d.countryCode || "",
    city: d.city || "",
    coordinates: { lat: d.latitude, lng: d.longitude },
    images: d.imageUrl ? [{ url: d.imageUrl, caption: "", credit: "" }] : [],
    categories: d.categories,
    travelTips: [],
    nearbyAirport: null,
    nearbyHotels: [],
    nearbyAttractions: [],
    weatherSummary: null,
    bestSeason: "",
    bestTimeToVisit: "",
    popularity: d.rating ?? 0,
    rating: d.rating ?? 0,
    reviewCount: d.reviewCount ?? 0,
    reviews: [],
    travelRequirements: [],
    emergencyContacts: [],
    currency: "",
    language: "",
    timezone: "",
    timezoneOffset: "",
    estimatedBudget: null,
    topAttractions: [],
    relatedDestinationIds: [],
    featured: false,
    trending: false,
    active: true,
    createdAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date()
  };
}
var DestinationAggregator = {
  nearby,
  search,
  toNearbyItems,
  toDestination,
  mergeDestinations
};

// src/modules/explore/services/discovery/TravelClassifier.ts
var TRAVEL_CATEGORIES = {
  // Nature
  beach: { category: "Beach", tags: ["Nature", "Relaxation", "Photography"] },
  national_park: { category: "National Park", tags: ["Nature", "Wildlife", "Hiking"] },
  park: { category: "Park", tags: ["Nature", "Relaxation", "Outdoor"] },
  waterfall: { category: "Waterfall", tags: ["Nature", "Adventure", "Photography"] },
  lake: { category: "Lake", tags: ["Nature", "Relaxation", "Water Activities"] },
  mountain: { category: "Mountain", tags: ["Nature", "Adventure", "Hiking"] },
  nature_reserve: { category: "Nature Reserve", tags: ["Nature", "Wildlife", "Conservation"] },
  scenic_viewpoint: { category: "Scenic Viewpoint", tags: ["Nature", "Photography", "Sightseeing"] },
  island: { category: "Island", tags: ["Nature", "Beach", "Adventure"] },
  forest: { category: "Forest", tags: ["Nature", "Hiking", "Adventure"] },
  // Wildlife
  zoo: { category: "Zoo", tags: ["Wildlife", "Family", "Education"] },
  safari: { category: "Safari", tags: ["Wildlife", "Adventure", "Photography"] },
  wildlife_park: { category: "Wildlife Park", tags: ["Wildlife", "Nature", "Safari"] },
  aquarium: { category: "Aquarium", tags: ["Wildlife", "Family", "Education"] },
  // Culture
  museum: { category: "Museum", tags: ["Culture", "Education", "History"] },
  art_gallery: { category: "Art Gallery", tags: ["Culture", "Art", "Photography"] },
  historic_site: { category: "Historic Site", tags: ["Culture", "History", "Sightseeing"] },
  castle: { category: "Castle", tags: ["Culture", "History", "Architecture"] },
  monument: { category: "Monument", tags: ["Culture", "History", "Photography"] },
  archaeological_site: { category: "Archaeological Site", tags: ["Culture", "History", "Adventure"] },
  // Entertainment
  amusement_park: { category: "Amusement Park", tags: ["Entertainment", "Family", "Adventure"] },
  theme_park: { category: "Theme Park", tags: ["Entertainment", "Family", "Adventure"] },
  water_park: { category: "Water Park", tags: ["Entertainment", "Family", "Fun"] },
  live_music: { category: "Live Music Venue", tags: ["Entertainment", "Music", "Nightlife"] },
  theater: { category: "Theater", tags: ["Entertainment", "Culture", "Arts"] },
  // Food
  restaurant: { category: "Restaurant", tags: ["Food", "Dining", "Cuisine"] },
  cafe: { category: "Caf\xE9", tags: ["Food", "Relaxation", "Coffee"] },
  rooftop_bar: { category: "Rooftop Bar", tags: ["Food", "Nightlife", "Views"] },
  brewery: { category: "Brewery", tags: ["Food", "Drinks", "Social"] },
  food_market: { category: "Food Market", tags: ["Food", "Local", "Shopping"] },
  // Shopping
  shopping_mall: { category: "Shopping Mall", tags: ["Shopping", "Retail", "Entertainment"] },
  artisan_market: { category: "Artisan Market", tags: ["Shopping", "Local", "Culture"] },
  local_market: { category: "Local Market", tags: ["Shopping", "Local", "Food"] },
  // Adventure
  hiking_area: { category: "Hiking Area", tags: ["Adventure", "Nature", "Fitness"] },
  campground: { category: "Campground", tags: ["Adventure", "Nature", "Outdoor"] },
  marina: { category: "Marina", tags: ["Adventure", "Water", "Boating"] },
  diving: { category: "Diving Spot", tags: ["Adventure", "Water", "Marine"] },
  cycling: { category: "Cycling Route", tags: ["Adventure", "Fitness", "Outdoor"] }
};
var BLACKLISTED_TYPES = /* @__PURE__ */ new Set([
  "church",
  "mosque",
  "synagogue",
  "temple",
  "school",
  "university",
  "college",
  "hospital",
  "clinic",
  "pharmacy",
  "dentist",
  "doctor",
  "government_office",
  "police",
  "courthouse",
  "embassy",
  "bank",
  "atm",
  "accounting",
  "lawyer",
  "insurance",
  "real_estate_agency",
  "storage",
  "self_storage",
  "cemetery",
  "funeral_home",
  "mortuary",
  "electrician",
  "plumber",
  "contractor",
  "roofing_contractor",
  "car_repair",
  "car_dealer",
  "car_wash",
  "gas_station",
  "parking",
  "office",
  "post_office",
  "fire_station",
  "laundry",
  "dry_cleaning",
  "hair_care",
  "beauty_salon",
  "spa",
  "gym",
  "fitness_center",
  "yoga_studio",
  "supermarket",
  "grocery_store",
  "convenience_store",
  "liquor_store",
  "hardware_store",
  "home_goods_store",
  "furniture_store",
  "electronics_store",
  "computer_store",
  "mobile_phone_store",
  "pet_store",
  "veterinary_care",
  "travel_agency",
  "lodging",
  "motel",
  "hostel",
  "bus_station",
  "train_station",
  "airport",
  "taxi_stand",
  "subway_station",
  "transit_station",
  "local_government_office",
  "city_hall"
]);
var GOOGLE_TYPE_TO_TRAVEL = {
  // Nature
  beach: "beach",
  national_park: "national_park",
  park: "park",
  natural_feature: "scenic_viewpoint",
  viewpoint: "scenic_viewpoint",
  waterfall: "waterfall",
  lake: "lake",
  mountain: "mountain",
  nature_reserve: "nature_reserve",
  island: "island",
  forest: "forest",
  // Wildlife
  zoo: "zoo",
  aquarium: "aquarium",
  wildlife_park: "wildlife_park",
  // Culture
  museum: "museum",
  art_gallery: "art_gallery",
  historic_site: "historic_site",
  castle: "castle",
  monument: "monument",
  archaeological_site: "archaeological_site",
  landmark: "historic_site",
  place_of_worship: "historic_site",
  // Only if it's a famous landmark
  // Entertainment
  amusement_park: "amusement_park",
  theme_park: "theme_park",
  water_park: "water_park",
  night_club: "live_music",
  casino: "entertainment",
  movie_theater: "theater",
  performing_arts_theater: "theater",
  // Food
  restaurant: "restaurant",
  cafe: "cafe",
  bar: "rooftop_bar",
  bakery: "cafe",
  meal_takeaway: "restaurant",
  meal_delivery: "restaurant",
  food: "restaurant",
  nightlife: "rooftop_bar",
  // Shopping
  shopping_mall: "shopping_mall",
  market: "local_market",
  flea_market: "local_market",
  artisan_market: "artisan_market",
  tourist_attraction: "historic_site",
  // Adventure
  hiking_area: "hiking_area",
  campground: "campground",
  marina: "marina",
  diving_center: "diving",
  bicycle_store: "cycling",
  gym: "fitness_center",
  stadium: "entertainment",
  sports_complex: "entertainment",
  athletic_field: "entertainment",
  golf_course: "entertainment",
  ski_resort: "adventure",
  rafting: "adventure",
  rock_climbing: "adventure",
  zip_line: "adventure"
};
var TravelClassifier = {
  /**
   * Classify a place into travel categories.
   * Returns null if the place should be excluded.
   */
  classify(place) {
    const types = place.tags || [];
    const name = place.name?.toLowerCase() || "";
    const type = place.type || "";
    for (const tag of types) {
      if (BLACKLISTED_TYPES.has(tag)) {
        return {
          travelCategory: "Excluded",
          travelTags: [],
          isTravelRelevant: false,
          exclusionReason: `Blacklisted type: ${tag}`
        };
      }
    }
    const blacklistKeywords = [
      "church",
      "mosque",
      "temple",
      "synagogue",
      "chapel",
      "school",
      "university",
      "college",
      "academy",
      "institute",
      "hospital",
      "clinic",
      "pharmacy",
      "medical",
      "dental",
      "government",
      "police",
      "courthouse",
      "embassy",
      "consulate",
      "bank",
      "atm",
      "accounting",
      "lawyer",
      "attorney",
      "insurance",
      "real estate",
      "storage",
      "cemetery",
      "funeral",
      "electrician",
      "plumber",
      "contractor",
      "car repair",
      "garage",
      "gas station",
      "petrol",
      "office",
      "post office",
      "laundry",
      "dry cleaning",
      "hair salon",
      "barber",
      "supermarket",
      "grocery",
      "convenience store",
      "liquor",
      "hardware",
      "furniture",
      "electronics",
      "pet store",
      "veterinary",
      "bus station",
      "train station",
      "taxi"
    ];
    for (const keyword of blacklistKeywords) {
      if (name.includes(keyword)) {
        return {
          travelCategory: "Excluded",
          travelTags: [],
          isTravelRelevant: false,
          exclusionReason: `Blacklisted name keyword: ${keyword}`
        };
      }
    }
    let travelKey = null;
    if (type && GOOGLE_TYPE_TO_TRAVEL[type]) {
      travelKey = GOOGLE_TYPE_TO_TRAVEL[type];
    }
    if (!travelKey) {
      for (const tag of types) {
        if (GOOGLE_TYPE_TO_TRAVEL[tag]) {
          travelKey = GOOGLE_TYPE_TO_TRAVEL[tag];
          break;
        }
      }
    }
    if (!travelKey) {
      const nameKeywords = {
        "beach": "beach",
        "park": "park",
        "museum": "museum",
        "gallery": "art_gallery",
        "waterfall": "waterfall",
        "lake": "lake",
        "mountain": "mountain",
        "viewpoint": "scenic_viewpoint",
        "view point": "scenic_viewpoint",
        "lookout": "scenic_viewpoint",
        "zoo": "zoo",
        "aquarium": "aquarium",
        "safari": "safari",
        "wildlife": "wildlife_park",
        "castle": "castle",
        "monument": "monument",
        "historic": "historic_site",
        "heritage": "historic_site",
        "ruins": "archaeological_site",
        "amusement": "amusement_park",
        "theme park": "theme_park",
        "water park": "water_park",
        "theatre": "theater",
        "theater": "theater",
        "restaurant": "restaurant",
        "cafe": "cafe",
        "coffee": "cafe",
        "brewery": "brewery",
        "market": "local_market",
        "hiking": "hiking_area",
        "trail": "hiking_area",
        "campground": "campground",
        "camping": "campground",
        "marina": "marina",
        "diving": "diving",
        "cycling": "cycling",
        "bike": "cycling",
        "resort": "resort",
        "garden": "park",
        "botanical": "park",
        "observatory": "scenic_viewpoint",
        "lighthouse": "historic_site",
        "palace": "historic_site",
        "tower": "monument",
        "bridge": "historic_site",
        "square": "historic_site",
        "fountain": "monument",
        "statue": "monument",
        "memorial": "monument",
        "temple": "historic_site",
        // Famous temples are tourist attractions
        "cathedral": "historic_site",
        // Famous cathedrals are tourist attractions
        "mosque": "historic_site"
        // Famous mosques are tourist attractions
      };
      for (const [keyword, category] of Object.entries(nameKeywords)) {
        if (name.includes(keyword)) {
          travelKey = category;
          break;
        }
      }
    }
    if (!travelKey) {
      return {
        travelCategory: "Excluded",
        travelTags: [],
        isTravelRelevant: false,
        exclusionReason: "No travel category matched"
      };
    }
    const travelInfo = TRAVEL_CATEGORIES[travelKey];
    if (!travelInfo) {
      return {
        travelCategory: "Excluded",
        travelTags: [],
        isTravelRelevant: false,
        exclusionReason: `Unknown travel key: ${travelKey}`
      };
    }
    return {
      travelCategory: travelInfo.category,
      travelTags: travelInfo.tags,
      isTravelRelevant: true
    };
  },
  /**
   * Filter an array of places to only include travel-relevant ones.
   */
  filterTravelRelevant(places) {
    const filtered = [];
    const excluded = [];
    for (const place of places) {
      const classification = this.classify(place);
      if (classification && classification.isTravelRelevant) {
        place.travelCategory = classification.travelCategory;
        place.travelTags = classification.travelTags;
        filtered.push(place);
      } else if (classification) {
        excluded.push(`${place.name} (${classification.exclusionReason})`);
      }
    }
    if (excluded.length > 0) {
      console.log(`[TravelClassifier] Excluded ${excluded.length} non-travel places:`, excluded.slice(0, 5));
    }
    console.log(`[TravelClassifier] Filtered ${places.length} \u2192 ${filtered.length} travel-relevant places`);
    return filtered;
  },
  /**
   * Get all supported travel categories.
   */
  getTravelCategories() {
    return Object.values(TRAVEL_CATEGORIES).map((t) => t.category);
  },
  /**
   * Check if a place type is blacklisted.
   */
  isBlacklisted(type) {
    return BLACKLISTED_TYPES.has(type);
  }
};

// src/modules/explore/services/discovery/TravelRankingEngine.ts
var SCORE_WEIGHTS = {
  RATING: 0.15,
  // Rating (only when REAL data exists — never fabricated)
  REVIEW_COUNT: 0.1,
  // Review count (only when REAL data exists)
  CATEGORY_PRIORITY: 0.25,
  // Travel category priority
  DISTANCE: 0.15,
  // Proximity to user
  COMPLETENESS: 0.15,
  // Data completeness (website/hours/images/description)
  PHOTOS: 0.1,
  // Has photos
  CURATED: 0.05,
  // Firestore curated / provider confidence
  OPEN_NOW: 0.05
  // Currently open
};
function effectiveWeights(hasReviewData) {
  if (hasReviewData) return SCORE_WEIGHTS;
  const freed = SCORE_WEIGHTS.RATING + SCORE_WEIGHTS.REVIEW_COUNT;
  return {
    ...SCORE_WEIGHTS,
    RATING: 0,
    REVIEW_COUNT: 0,
    CATEGORY_PRIORITY: SCORE_WEIGHTS.CATEGORY_PRIORITY + freed * 0.6,
    COMPLETENESS: SCORE_WEIGHTS.COMPLETENESS + freed * 0.4
  };
}
var CATEGORY_PRIORITY = {
  // Highest priority - must-see destinations
  "National Park": 100,
  "Beach": 95,
  "Safari": 95,
  "Wildlife Park": 90,
  "Waterfall": 90,
  "Island": 90,
  "Scenic Viewpoint": 85,
  "Historic Site": 85,
  "Museum": 80,
  "Lake": 80,
  "Mountain": 80,
  "Nature Reserve": 80,
  "Castle": 80,
  "Monument": 75,
  "Archaeological Site": 75,
  "Art Gallery": 70,
  "Amusement Park": 70,
  "Theme Park": 70,
  "Water Park": 65,
  "Zoo": 65,
  "Aquarium": 65,
  "Forest": 65,
  "Park": 60,
  "Hiking Area": 60,
  "Campground": 55,
  "Marina": 50,
  "Diving Spot": 60,
  "Cycling Route": 50,
  "Live Music Venue": 60,
  "Theater": 60,
  "Restaurant": 55,
  "Caf\xE9": 45,
  "Rooftop Bar": 50,
  "Brewery": 45,
  "Food Market": 55,
  "Shopping Mall": 40,
  "Artisan Market": 55,
  "Local Market": 55
};
var TravelRankingEngine = {
  /**
   * Calculate a travel score for a place.
   */
  calculateScore(place, options) {
    const rating = place.rating || 0;
    const reviewCount = place.reviewCount || 0;
    const hasReviewData = rating > 0 && rating <= 5;
    const W = effectiveWeights(hasReviewData);
    let score = 0;
    if (hasReviewData) {
      score += rating / 5 * 100 * W.RATING;
    }
    if (hasReviewData) {
      const reviewScore = Math.min(100, reviewCount / 1e3 * 100);
      score += reviewScore * W.REVIEW_COUNT;
    }
    const travelCategory = place.travelCategory;
    if (travelCategory && CATEGORY_PRIORITY[travelCategory]) {
      score += CATEGORY_PRIORITY[travelCategory] * W.CATEGORY_PRIORITY;
    } else {
      score += 50 * W.CATEGORY_PRIORITY;
    }
    const distance = place.distance || 0;
    if (distance > 0 && options?.userLocation) {
      if (distance >= 1 && distance <= 50) {
        score += 100 * W.DISTANCE;
      } else if (distance < 1) {
        score += 60 * W.DISTANCE;
      } else if (distance <= 100) {
        score += 80 * W.DISTANCE;
      } else if (distance <= 200) {
        score += 50 * W.DISTANCE;
      } else {
        score += 20 * W.DISTANCE;
      }
    } else {
      score += 50 * W.DISTANCE;
    }
    const meta = place.metadata || {};
    let completeness = 0;
    if (place.website) completeness += 30;
    if (place.openingHours) completeness += 25;
    if (place.description) completeness += 25;
    if ((place.tags || []).length > 1) completeness += 20;
    if (typeof meta.qualityScore === "number") {
      completeness = Math.max(completeness, Math.round(meta.qualityScore * 100));
    }
    score += completeness * W.COMPLETENESS;
    const images = place.images || place.imageUrl;
    if (images) {
      if (Array.isArray(images) && images.length > 0) {
        score += 100 * W.PHOTOS;
      } else if (typeof images === "string" && images.length > 0) {
        score += 80 * W.PHOTOS;
      }
    }
    const curated = meta.curated === true || typeof meta.wikidataId === "string" || typeof place.sourceConfidence === "number" && place.sourceConfidence >= 0.9;
    if (curated) {
      score += 100 * W.CURATED;
    }
    const openNow = place.openNow;
    if (openNow === true) {
      score += 100 * W.OPEN_NOW;
    }
    if (options?.hasNearbyEvents) {
      score += 5;
    }
    return Math.round(score);
  },
  /**
   * Rank an array of places by travel score descending.
   */
  rank(places, options) {
    const scored = places.map((place) => ({
      place,
      score: this.calculateScore(place, options)
    }));
    scored.sort((a, b) => b.score - a.score);
    const topN = options?.topN || places.length;
    const ranked = scored.slice(0, topN).map((s) => s.place);
    console.log(
      `[TravelRankingEngine] Ranked ${places.length} places, top scores:`,
      scored.slice(0, 5).map((s) => `${s.place.name}: ${s.score}`)
    );
    return ranked;
  },
  /**
   * Get the category priority for a travel category.
   */
  getCategoryPriority(category) {
    return CATEGORY_PRIORITY[category] || 50;
  }
};

// src/modules/explore/services/discovery/CategoryBalancer.ts
var MAX_PER_CATEGORY = 5;
var TARGET_RESULT_COUNT = 20;
var CategoryBalancer = {
  /**
   * Balance an array of places to ensure category diversity.
   * Takes the top places but ensures no single category dominates.
   *
   * @param places - Array of places with travelCategory attached
   * @param maxPerCategory - Maximum places from same category (default: 5)
   * @param maxTotal - Maximum total places to return (default: 20)
   * @returns Balanced array of places
   */
  balance(places, maxPerCategory = MAX_PER_CATEGORY, maxTotal = TARGET_RESULT_COUNT) {
    if (places.length === 0) return [];
    const grouped = {};
    for (const place of places) {
      const category = place.travelCategory || "Uncategorized";
      if (!grouped[category]) {
        grouped[category] = [];
      }
      grouped[category].push(place);
    }
    const categoryNames = Object.keys(grouped);
    const numCategories = categoryNames.length;
    const effectiveMaxPerCategory = Math.max(
      maxPerCategory,
      Math.ceil(maxTotal / Math.max(numCategories, 1))
    );
    const categoryCounts = {};
    const balanced = [];
    const sortedCategories = [...categoryNames].sort((a, b) => {
      const priorityOrder = [
        "National Park",
        "Beach",
        "Safari",
        "Wildlife Park",
        "Waterfall",
        "Island",
        "Historic Site",
        "Museum",
        "Lake",
        "Mountain",
        "Scenic Viewpoint"
      ];
      const aIdx = priorityOrder.indexOf(a);
      const bIdx = priorityOrder.indexOf(b);
      if (aIdx >= 0 && bIdx >= 0) return aIdx - bIdx;
      if (aIdx >= 0) return -1;
      if (bIdx >= 0) return 1;
      return 0;
    });
    const indices = {};
    for (const cat of sortedCategories) {
      indices[cat] = 0;
    }
    let added = 0;
    while (added < maxTotal) {
      let anyAdded = false;
      for (const category of sortedCategories) {
        const idx = indices[category];
        const count = categoryCounts[category] || 0;
        if (idx < grouped[category].length && count < effectiveMaxPerCategory && added < maxTotal) {
          balanced.push(grouped[category][idx]);
          categoryCounts[category] = count + 1;
          indices[category]++;
          added++;
          anyAdded = true;
        }
      }
      if (!anyAdded) break;
    }
    const categorySpread = Object.entries(categoryCounts).map(([cat, count]) => `${cat}: ${count}`).join(", ");
    console.log(`[CategoryBalancer] Balanced ${places.length} \u2192 ${balanced.length} places (${categorySpread})`);
    return balanced;
  },
  /**
   * Get diversity score for a set of places.
   * Higher means more diverse.
   */
  getDiversityScore(places) {
    if (places.length === 0) return 0;
    const categories = /* @__PURE__ */ new Set();
    for (const place of places) {
      const cat = place.travelCategory || "Uncategorized";
      categories.add(cat);
    }
    return categories.size / places.length;
  }
};

// src/modules/explore/services/discovery/ProviderDataCoordinator.ts
var CACHE_TTL_MS2 = 3e4;
var osmCache = /* @__PURE__ */ new Map();
var googleCache = /* @__PURE__ */ new Map();
var providerHealth = {};
function buildKey(lat, lng, radiusKm) {
  return `${lat.toFixed(3)},${lng.toFixed(3)},${radiusKm}`;
}
function coalesceRequest(cache3, key, label, fetchFn, getHealth) {
  const now = Date.now();
  const existing = cache3.get(key);
  if (existing && !existing.result) return existing.promise;
  if (existing && existing.result && existing.expiresAt > now) {
    return Promise.resolve(existing.result);
  }
  const startTime = Date.now();
  const entry = {
    promise: fetchFn().then((result) => {
      entry.result = result;
      entry.expiresAt = Date.now() + CACHE_TTL_MS2;
      const health = getHealth(result);
      providerHealth[label] = {
        provider: label,
        status: health.rawCount > 0 ? "SUCCESS_WITH_DATA" : "SUCCESS_EMPTY",
        rawCount: health.rawCount,
        usableCount: health.usableCount,
        latencyMs: Date.now() - startTime,
        queriedAt: Date.now()
      };
      return result;
    }).catch((err) => {
      providerHealth[label] = {
        provider: label,
        status: "NETWORK_ERROR",
        rawCount: 0,
        usableCount: 0,
        latencyMs: Date.now() - startTime,
        error: err?.message || String(err),
        queriedAt: Date.now()
      };
      throw err;
    }),
    expiresAt: 0
  };
  cache3.set(key, entry);
  return entry.promise;
}
function toNearby(dests, lat, lng) {
  return DestinationAggregator.toNearbyItems(dests, { lat, lng });
}
var ProviderDataCoordinator = {
  /**
   * OpenStreetMap-first destination data (merged with Firestore curated, plus
   * optional Google enrichment). Returns NearbyItem[] for compatibility.
   */
  async getOpenStreetMap(lat, lng, radiusKm = 100) {
    const key = buildKey(lat, lng, radiusKm);
    return coalesceRequest(
      osmCache,
      key,
      "openstreetmap",
      async () => {
        const dests = await DestinationAggregator.nearby({ lat, lng, radiusKm, limit: 40 });
        return {
          places: toNearby(dests, lat, lng),
          provider: "openstreetmap",
          fetchedAt: Date.now()
        };
      },
      (result) => ({ rawCount: result.places.length, usableCount: result.places.length })
    );
  },
  /**
   * Google Places data. OPTIONAL — returns empty when disabled so the pipeline
   * never depends on it. Never throws into the critical path.
   */
  async getGooglePlaces(lat, lng, radiusKm = 100) {
    if (!googlePlacesAvailable()) {
      return { places: [], provider: "google_places", fetchedAt: Date.now() };
    }
    const key = buildKey(lat, lng, radiusKm);
    return coalesceRequest(
      googleCache,
      key,
      "google_places",
      async () => {
        const dests = await GooglePlacesProvider.searchNearby({ lat, lng, radiusKm, limit: 30 });
        return {
          places: toNearby(dests, lat, lng),
          provider: "google_places",
          fetchedAt: Date.now()
        };
      },
      (result) => ({ rawCount: result.places.length, usableCount: result.places.length })
    );
  },
  /** Get provider health information for diagnostics. */
  getProviderHealth() {
    return { ...providerHealth };
  },
  /** Clear all caches (e.g. on location change). */
  clearCaches() {
    osmCache.clear();
    googleCache.clear();
  },
  /** Get a summary of provider health for logging. */
  getHealthSummary() {
    const entries = Object.entries(providerHealth);
    if (entries.length === 0) return "No providers queried yet";
    return entries.map(([name, h]) => `${name}: ${h.status} (raw: ${h.rawCount}, usable: ${h.usableCount}, ${h.latencyMs}ms)`).join("\n");
  }
};

// src/modules/explore/services/discovery/TrendingEngine.ts
var MIN_TRENDING_REQUIRED = 15;
function nearbyToTrending(place, source, score) {
  return {
    id: place.id,
    name: place.name,
    slug: place.name.toLowerCase().replace(/\s+/g, "-"),
    description: place.description || "",
    country: "",
    countryCode: "",
    city: "",
    coordinates: place.coordinates,
    images: place.imageUrl ? [{ url: place.imageUrl, caption: "", credit: "" }] : [],
    categories: [place.travelCategory || "attraction"],
    travelTips: [],
    nearbyAirport: null,
    nearbyHotels: [],
    nearbyAttractions: [],
    weatherSummary: null,
    bestSeason: "",
    bestTimeToVisit: "",
    popularity: place.rating || 0,
    rating: place.rating || 0,
    reviewCount: place.reviewCount || 0,
    reviews: [],
    travelRequirements: [],
    emergencyContacts: [],
    currency: "",
    language: "",
    timezone: "",
    timezoneOffset: "",
    estimatedBudget: null,
    topAttractions: [],
    relatedDestinationIds: [],
    featured: false,
    trending: false,
    active: true,
    createdAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date(),
    trendingScore: score,
    source
  };
}
var TrendingEngine = {
  /**
   * Compute trending destinations from live API data.
   * Firestore is only used as fallback if APIs return < 15 results.
   */
  async getTrendingDestinations(lat, lng) {
    const sourceBreakdown = { google: 0, osm: 0, firestore: 0 };
    let firestoreReason;
    console.log("[TrendingEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[TrendingEngine] Computing trending from live APIs...");
    console.log("[TrendingEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    let allOsmPlaces = [];
    try {
      const osmResult = await ProviderDataCoordinator.getOpenStreetMap(lat || 0, lng || 0, 100);
      allOsmPlaces = osmResult.places;
      console.log(`[TrendingEngine] OpenStreetMap + Curated: ${allOsmPlaces.length} places`);
    } catch (err) {
      console.warn("[TrendingEngine] OpenStreetMap failed:", err);
    }
    const merged = this.mergePlaces(allOsmPlaces);
    console.log(`[TrendingEngine] Combined: ${merged.length} places`);
    const travelFiltered = TravelClassifier.filterTravelRelevant(merged);
    console.log(`[TrendingEngine] TravelClassifier: ${merged.length} \u2192 ${travelFiltered.length}`);
    const ranked = TravelRankingEngine.rank(travelFiltered, {
      userLocation: lat && lng ? { lat, lng } : void 0
    });
    const balanced = CategoryBalancer.balance(ranked, 5, 20);
    let trending = balanced.map((place) => {
      const source = "osm";
      const score = TravelRankingEngine.calculateScore(place, {
        userLocation: lat && lng ? { lat, lng } : void 0
      });
      return nearbyToTrending(place, source, score);
    });
    for (const t of trending) {
      sourceBreakdown[t.source]++;
    }
    if (trending.length < MIN_TRENDING_REQUIRED) {
      firestoreReason = `Live candidates: ${trending.length}. Required: ${MIN_TRENDING_REQUIRED}.`;
      console.log(`[TrendingEngine] \u26A0 ${firestoreReason} Injecting Firestore fallback...`);
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: MIN_TRENDING_REQUIRED - trending.length + 10
        });
        const existingIds = new Set(trending.map((t) => t.id));
        for (const dest of firestoreResult.data) {
          if (!existingIds.has(dest.id) && trending.length < MIN_TRENDING_REQUIRED) {
            trending.push({
              ...dest,
              trendingScore: 50,
              source: "firestore"
            });
            sourceBreakdown.firestore++;
            existingIds.add(dest.id);
          }
        }
      } catch (err) {
        console.warn("[TrendingEngine] Firestore fallback also failed:", err);
      }
    }
    trending.sort((a, b) => b.trendingScore - a.trendingScore);
    console.log(`[TrendingEngine] \u2713 Trending computed:
  Live (OSM+Curated): ${sourceBreakdown.google + sourceBreakdown.osm}
  OSM: ${sourceBreakdown.osm}
  Firestore: ${sourceBreakdown.firestore}${firestoreReason ? `
  \u26A0 Firestore reason: ${firestoreReason}` : ""}`);
    return {
      destinations: trending.slice(0, 20),
      computedAt: Date.now(),
      sourceBreakdown,
      firestoreReason
    };
  },
  /**
   * Merge places from multiple providers, deduplicating by coordinates.
   */
  mergePlaces(...placeArrays) {
    const merged = /* @__PURE__ */ new Map();
    for (const places of placeArrays) {
      for (const place of places) {
        const key = `${place.name.toLowerCase()}_${place.coordinates.lat.toFixed(3)}_${place.coordinates.lng.toFixed(3)}`;
        if (!merged.has(key)) {
          merged.set(key, place);
        } else {
          const existing = merged.get(key);
          if ((place.rating || 0) > (existing.rating || 0)) {
            merged.set(key, place);
          }
        }
      }
    }
    return Array.from(merged.values());
  }
};

// src/modules/explore/services/discovery/CategoryDiscoveryEngine.ts
var CATEGORY_PLACE_TYPES = {
  beach: ["beach"],
  adventure: ["amusement_park", "zoo", "stadium", "campground"],
  nature: ["park", "natural_feature", "national_park", "botanical_garden"],
  cultural: ["museum", "art_gallery", "church", "mosque", "temple", "synagogue", "archaeological_site"],
  food: ["restaurant", "cafe", "bakery"],
  city: ["shopping_mall", "night_club", "movie_theater", "casino"],
  wildlife: ["zoo", "aquarium", "natural_feature"],
  historical: ["church", "mosque", "temple", "museum", "archaeological_site"],
  luxury: ["lodging", "spa", "restaurant"],
  family: ["amusement_park", "zoo", "aquarium", "park", "museum"],
  wellness: ["spa", "park", "natural_feature"]
};
async function countNearbyPlaces(lat, lng, radiusKm, types) {
  try {
    const result = await ProviderDataCoordinator.getOpenStreetMap(lat, lng, radiusKm);
    const matchingTypes = new Set(types);
    return result.places.filter((p) => p.tags.some((t) => matchingTypes.has(t))).length;
  } catch {
    return 0;
  }
}
async function countWorldwideDestinations(categorySlug) {
  try {
    const result = await ExploreService.loadDestinationsByCategory(categorySlug);
    return result.total || result.data.length;
  } catch {
    const estimates = {
      beach: 1050,
      adventure: 1204,
      nature: 1204,
      cultural: 980,
      food: 1500,
      city: 890,
      wildlife: 760,
      historical: 850,
      luxury: 620,
      family: 1100,
      wellness: 540
    };
    return estimates[categorySlug] || 500;
  }
}
function getCategoryImage(category) {
  if (category.imageUrl) return category.imageUrl;
  const fallbackImages = {
    beach: "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?w=400",
    adventure: "https://images.unsplash.com/photo-1530866495561-507c9faab2ed?w=400",
    nature: "https://images.unsplash.com/photo-1441974231531-c6227db76b6e?w=400",
    cultural: "https://images.unsplash.com/photo-1524666041070-9d876df3e5d5?w=400",
    food: "https://images.unsplash.com/photo-1504674900247-0877df9cc836?w=400",
    city: "https://images.unsplash.com/photo-1477959858617-67f85cf4f1df?w=400",
    wildlife: "https://images.unsplash.com/photo-1546182990-dffeafbe841d?w=400",
    historical: "https://images.unsplash.com/photo-1461360228754-6e81c478b882?w=400",
    luxury: "https://images.unsplash.com/photo-1571896349842-33c89424de2d?w=400",
    family: "https://images.unsplash.com/photo-1502784444186-3590a34f0ef5?w=400",
    wellness: "https://images.unsplash.com/photo-1545205597-3d9d02c29597?w=400"
  };
  return fallbackImages[category.slug] || category.imageUrl || "https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=400";
}
var CategoryDiscoveryEngine = {
  /**
   * Discover categories with dynamic counts and trending images.
   *
   * @param lat - User's latitude for nearby counts
   * @param lng - User's longitude for nearby counts
   * @param radiusKm - Search radius for nearby counts
   * @returns Categories enriched with dynamic data
   */
  async getCategories(lat, lng, radiusKm = 100) {
    const result = await ExploreService.loadCategories();
    const categories = result.data ?? [];
    if (categories.length === 0) {
      if (result.error) {
        console.warn(`[CategoryDiscoveryEngine] \u26A0 loadCategories returned error: "${result.error}". This means the Firestore query failed (network, permissions, or missing collection).`);
      } else {
        console.warn(`[CategoryDiscoveryEngine] \u26A0 loadCategories returned 0 categories with no error. This means the Firestore collection "${FIRESTORE_COLLECTIONS?.EXPLORE_CATEGORIES || "explore_categories"}" exists but is empty, OR all documents have active===false (filtered out client-side).`);
        console.warn(`[CategoryDiscoveryEngine] \u26A0 Check: (1) Is the collection seeded? (2) Do documents have active: true? (3) Is the collection name correct?`);
      }
      return {
        categories: [],
        computedAt: Date.now()
      };
    }
    console.log(`[CategoryDiscoveryEngine] Loaded ${categories.length} raw categories from Firestore. Names:`, categories.map((c) => c.name).join(", "));
    const enriched = await Promise.all(
      categories.map(async (category) => {
        const placeTypes = CATEGORY_PLACE_TYPES[category.slug] || [];
        const [nearbyCount, worldwideCount] = await Promise.allSettled([
          countNearbyPlaces(lat, lng, radiusKm, placeTypes),
          countWorldwideDestinations(category.slug)
        ]);
        let trendingDest = null;
        try {
          const destResult = await ExploreService.loadDestinationsByCategory(category.slug);
          if (destResult.data.length > 0) {
            trendingDest = destResult.data[0];
          }
        } catch {
        }
        return {
          ...category,
          nearbyCount: nearbyCount.status === "fulfilled" ? nearbyCount.value : 0,
          worldwideCount: worldwideCount.status === "fulfilled" ? worldwideCount.value : 500,
          trendingDestination: trendingDest,
          featuredImage: getCategoryImage(category),
          discoveredAt: Date.now()
        };
      })
    );
    enriched.sort((a, b) => {
      const orderDiff = (a.sortOrder ?? 99) - (b.sortOrder ?? 99);
      if (orderDiff !== 0) return orderDiff;
      return b.nearbyCount + b.worldwideCount - (a.nearbyCount + a.worldwideCount);
    });
    console.log(
      `[CategoryDiscoveryEngine] Enriched ${enriched.length} categories with dynamic counts`
    );
    return {
      categories: enriched,
      computedAt: Date.now()
    };
  }
};

// src/modules/explore/services/discovery/PopularityEngine.ts
var MIN_POPULAR_REQUIRED = 15;
function nearbyToPopular(place, source, score) {
  return {
    id: place.id,
    name: place.name,
    slug: place.name.toLowerCase().replace(/\s+/g, "-"),
    description: place.description || "",
    country: "",
    countryCode: "",
    city: "",
    coordinates: place.coordinates,
    images: place.imageUrl ? [{ url: place.imageUrl, caption: "", credit: "" }] : [],
    categories: [place.travelCategory || "attraction"],
    travelTips: [],
    nearbyAirport: null,
    nearbyHotels: [],
    nearbyAttractions: [],
    weatherSummary: null,
    bestSeason: "",
    bestTimeToVisit: "",
    popularity: place.rating || 0,
    rating: place.rating || 0,
    reviewCount: place.reviewCount || 0,
    reviews: [],
    travelRequirements: [],
    emergencyContacts: [],
    currency: "",
    language: "",
    timezone: "",
    timezoneOffset: "",
    estimatedBudget: null,
    topAttractions: [],
    relatedDestinationIds: [],
    featured: false,
    trending: false,
    active: true,
    createdAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date(),
    popularityScore: score,
    source
  };
}
var PopularityEngine = {
  /**
   * Compute popular destinations from live API data.
   * Firestore is only used as fallback if APIs return < 15 results.
   */
  async getPopularDestinations(lat, lng) {
    const sourceBreakdown = { google: 0, osm: 0, firestore: 0 };
    let firestoreReason;
    console.log("[PopularityEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[PopularityEngine] Computing popular from live APIs...");
    console.log("[PopularityEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    let allOsmPlaces = [];
    try {
      const osmResult = await ProviderDataCoordinator.getOpenStreetMap(lat || 0, lng || 0, 100);
      allOsmPlaces = osmResult.places;
      console.log(`[PopularityEngine] OpenStreetMap+Curated: ${allOsmPlaces.length} places`);
    } catch (err) {
      console.warn("[PopularityEngine] OpenStreetMap failed:", err);
    }
    const merged = this.mergePlaces(allOsmPlaces);
    console.log(`[PopularityEngine] Combined: ${merged.length} places`);
    const travelFiltered = TravelClassifier.filterTravelRelevant(merged);
    console.log(`[PopularityEngine] TravelClassifier: ${merged.length} \u2192 ${travelFiltered.length}`);
    const ranked = TravelRankingEngine.rank(travelFiltered, { topN: 40 });
    const balanced = CategoryBalancer.balance(ranked, 5, 20);
    let popular = balanced.map((place) => {
      const source = "osm";
      const score = (place.reviewCount || 0) * (place.rating || 0);
      return nearbyToPopular(place, source, score);
    });
    for (const p of popular) {
      sourceBreakdown[p.source]++;
    }
    if (popular.length < MIN_POPULAR_REQUIRED) {
      firestoreReason = `Live candidates: ${popular.length}. Required: ${MIN_POPULAR_REQUIRED}.`;
      console.log(`[PopularityEngine] \u26A0 ${firestoreReason} Injecting Firestore fallback...`);
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: MIN_POPULAR_REQUIRED - popular.length + 5
        });
        const existingIds = new Set(popular.map((p) => p.id));
        for (const dest of firestoreResult.data) {
          if (!existingIds.has(dest.id) && popular.length < MIN_POPULAR_REQUIRED) {
            popular.push({
              ...dest,
              popularityScore: 50,
              source: "firestore"
            });
            sourceBreakdown.firestore++;
            existingIds.add(dest.id);
          }
        }
      } catch (err) {
        console.warn("[PopularityEngine] Firestore fallback also failed:", err);
      }
    }
    popular.sort((a, b) => b.popularityScore - a.popularityScore);
    console.log(`[PopularityEngine] \u2713 Popular computed:
  Live (OSM): ${sourceBreakdown.google + sourceBreakdown.osm}
  OSM: ${sourceBreakdown.osm}
  Firestore: ${sourceBreakdown.firestore}${firestoreReason ? `
  \u26A0 Firestore reason: ${firestoreReason}` : ""}`);
    return {
      destinations: popular.slice(0, 20),
      computedAt: Date.now(),
      sourceBreakdown,
      firestoreReason
    };
  },
  /**
   * Merge places from multiple providers.
   */
  mergePlaces(...placeArrays) {
    const merged = /* @__PURE__ */ new Map();
    for (const places of placeArrays) {
      for (const place of places) {
        const key = `${place.name.toLowerCase()}_${place.coordinates.lat.toFixed(3)}_${place.coordinates.lng.toFixed(3)}`;
        if (!merged.has(key)) {
          merged.set(key, place);
        } else {
          const existing = merged.get(key);
          if ((place.rating || 0) > (existing.rating || 0)) {
            merged.set(key, place);
          }
        }
      }
    }
    return Array.from(merged.values());
  }
};

// src/modules/explore/services/discovery/WeekendEscapeClassifier.ts
var ESCAPE_DESTINATION_TYPES = [
  "National Park",
  "Nature Reserve",
  "Wildlife Park",
  "Safari",
  "Beach",
  "Island",
  "Lake",
  "Waterfall",
  "Mountain",
  "Scenic Viewpoint",
  "Forest",
  "Hiking Area",
  "Castle",
  "Monument",
  "Archaeological Site",
  "Historic Site",
  "Museum",
  "Art Gallery",
  "Zoo",
  "Aquarium",
  "Amusement Park",
  "Theme Park",
  "Water Park",
  "Campground",
  "Marina",
  "Diving Spot",
  "Garden"
];
var REJECTED_NAME_PATTERNS = [
  // Places of worship
  /\b(mosque|masjid|church|cathedral|chapel|temple|synagogue|shrine)\b/i,
  /\b(masjid|mescit)\b/i,
  // Educational
  /\b(school|university|college|academy|institute|campus|library)\b/i,
  /\b(gate|entrance|exit|main gate)\b/i,
  // Government / business
  /\b(government|ministry|embassy|consulate|office|headquarters|hq)\b/i,
  /\b(bank|atm|accounting|lawyer|attorney|insurance|real estate|estate agency)\b/i,
  /\b(company|ltd|limited|incorporated|corporation|llc)\b/i,
  // Commercial / retail
  /\b(supermarket|grocery|convenience store|shop|store|mall|market)\b/i,
  /\b(pharmacy|clinic|hospital|medical|dental|optician)\b/i,
  /\b(salon|barber|spa|laundry|dry cleaning|tailor)\b/i,
  /\b(petrol|gas station|filling station|car wash|garage|parking)\b/i,
  // Transport
  /\b(bus stop|taxi stand|train station|bus station|airport|terminal)\b/i,
  // Residential / generic
  /\b(residence|apartment|housing|estate|building)\b/i,
  /\b(warehouse|factory|plant|workshop|distributor)\b/i,
  /\b(guest house|bed & breakfast|hostel)\b/i
];
var REJECTED_PROVIDER_TYPES = /* @__PURE__ */ new Set([
  "church",
  "mosque",
  "synagogue",
  "temple",
  "place_of_worship",
  "school",
  "university",
  "college",
  "university_gate",
  "office",
  "government_office",
  "local_government_office",
  "bank",
  "atm",
  "supermarket",
  "grocery_store",
  "convenience_store",
  "pharmacy",
  "hospital",
  "clinic",
  "dentist",
  "doctor",
  "gas_station",
  "car_wash",
  "car_repair",
  "garage",
  "parking",
  "travel_agency",
  "bus_station",
  "train_station",
  "taxi_stand",
  "airport",
  "real_estate_agency",
  "storage",
  "self_storage",
  "home_goods_store",
  "electronics_store",
  "furniture_store",
  "hardware_store",
  "laundry",
  "dry_cleaning",
  "hair_care",
  "beauty_salon",
  "gym",
  "fitness_center",
  "spa",
  "lodging",
  "motel",
  "hostel"
]);
var MIN_ESCAPE_DISTANCE_KM = 5;
var MAX_ESCAPE_DISTANCE_KM = 300;
var MIN_RATING = 3.5;
var MIN_REVIEW_COUNT = 5;
var WeekendEscapeClassifier = {
  /**
   * Determine if a place is eligible as a Weekend Escape destination.
   * This runs BEFORE ranking - no amount of score can make an ineligible
   * place eligible.
   */
  classify(place) {
    const name = place.name || place.id || "";
    const type = place.type || "";
    const travelCategory = place.travelCategory || "";
    const tags = place.tags || [];
    const distance = place.distance || 0;
    for (const tag of tags) {
      if (REJECTED_PROVIDER_TYPES.has(tag)) {
        return {
          isEligible: false,
          category: "Rejected",
          rejectionReason: `Rejected provider type: ${tag}`
        };
      }
    }
    for (const pattern of REJECTED_NAME_PATTERNS) {
      if (pattern.test(name)) {
        return {
          isEligible: false,
          category: "Rejected",
          rejectionReason: `Rejected name pattern: ${pattern}`
        };
      }
    }
    if (type && REJECTED_PROVIDER_TYPES.has(type)) {
      return {
        isEligible: false,
        category: "Rejected",
        rejectionReason: `Rejected type: ${type}`
      };
    }
    const isEscapeCategory = ESCAPE_DESTINATION_TYPES.some(
      (cat) => travelCategory === cat || travelCategory.toLowerCase().includes(cat.toLowerCase())
    );
    if (!isEscapeCategory) {
      return {
        isEligible: false,
        category: "Rejected",
        rejectionReason: `Not an escape destination type: ${travelCategory || "unknown"}`
      };
    }
    if (distance > 0) {
      if (distance < MIN_ESCAPE_DISTANCE_KM) {
        return {
          isEligible: false,
          category: "Rejected",
          rejectionReason: `Too close (${distance.toFixed(1)}km < ${MIN_ESCAPE_DISTANCE_KM}km)`
        };
      }
      if (distance > MAX_ESCAPE_DISTANCE_KM) {
        return {
          isEligible: false,
          category: "Rejected",
          rejectionReason: `Too far (${distance.toFixed(1)}km > ${MAX_ESCAPE_DISTANCE_KM}km)`
        };
      }
    }
    const rating = place.rating || 0;
    const reviewCount = place.reviewCount || 0;
    if (rating > 0 && rating < MIN_RATING) {
      return {
        isEligible: false,
        category: "Rejected",
        rejectionReason: `Low rating (${rating} < ${MIN_RATING})`
      };
    }
    if (reviewCount > 0 && reviewCount < MIN_REVIEW_COUNT) {
      return {
        isEligible: false,
        category: "Rejected",
        rejectionReason: `Too few reviews (${reviewCount} < ${MIN_REVIEW_COUNT})`
      };
    }
    return {
      isEligible: true,
      category: travelCategory || "Uncategorized"
    };
  },
  /**
   * Filter an array of places to only include Weekend Escape eligible ones.
   * Logs the candidate funnel with rejection reasons.
   */
  filterEligible(places) {
    const eligible = [];
    const rejected = {};
    for (const place of places) {
      const result = this.classify(place);
      if (result.isEligible) {
        eligible.push(place);
      } else if (result.rejectionReason) {
        const key = result.rejectionReason.replace(/\d+\.\d+|\d+/g, "X");
        rejected[key] = (rejected[key] || 0) + 1;
      }
    }
    const breakdown = Object.entries(rejected).sort((a, b) => b[1] - a[1]).slice(0, 8).map(([reason, count]) => `${count}x ${reason}`).join(", ");
    console.log(`[WeekendEscapeClassifier] Eligible: ${eligible.length}/${places.length}`);
    if (breakdown) {
      console.log(`[WeekendEscapeClassifier] Rejected: ${breakdown}`);
    }
    return eligible;
  },
  /**
   * Get the allowed destination types.
   */
  getDestinationTypes() {
    return ESCAPE_DESTINATION_TYPES;
  }
};

// src/modules/explore/services/discovery/WeekendEscapeIntelligence.ts
var MIN_ESCAPES_REQUIRED = 5;
var WeekendEscapeIntelligence = {
  /**
   * Discover weekend escapes by searching for real destination-worthy places.
   * Falls back to Firestore if live APIs return insufficient eligible candidates.
   */
  async discoverEscapes(lat, lng, maxRadiusKm = 250) {
    console.log("[WeekendEscapeIntelligence] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[WeekendEscapeIntelligence] \u{1F3D5}\uFE0F Discovering weekend escapes");
    console.log("[WeekendEscapeIntelligence] Radius: 5-300km from user");
    console.log("[WeekendEscapeIntelligence] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    const osmResult = await Promise.allSettled([
      ProviderDataCoordinator.getOpenStreetMap(lat, lng, 120)
    ]);
    let allCandidates = [];
    const osmPlaces = osmResult[0].status === "fulfilled" ? osmResult[0].value.places : [];
    allCandidates = (osmPlaces || []).filter((p) => p.distance >= 5);
    console.log(`[WeekendEscapeIntelligence] OSM+Curated (up to 120km): ${allCandidates.length} candidates`);
    const unique = this.deduplicate(allCandidates);
    console.log(`[WeekendEscapeIntelligence] Total unique candidates: ${unique.length}`);
    const travelRelevant = TravelClassifier.filterTravelRelevant(unique);
    console.log(`[WeekendEscapeIntelligence] Travel relevant: ${travelRelevant.length}`);
    const escapeEligible = WeekendEscapeClassifier.filterEligible(travelRelevant);
    console.log(`[WeekendEscapeIntelligence] Weekend escape eligible: ${escapeEligible.length}`);
    if (escapeEligible.length >= MIN_ESCAPES_REQUIRED) {
      const ranked = TravelRankingEngine.rank(escapeEligible, {
        userLocation: { lat, lng }
      });
      const balanced = CategoryBalancer.balance(ranked, 3, 10);
      const escapes = balanced.map((place, index) => ({
        id: `escape_${place.id}`,
        destinationId: place.id,
        type: place.distance <= 100 ? "day_trip" : place.distance <= 200 ? "weekend" : "road_trip",
        distance: place.distance,
        travelTime: this.estimateTravelTime(place.distance),
        reason: this.generateEscapeReason(place),
        destination: this.toDestination(place)
      }));
      console.log(`[WeekendEscapeIntelligence] \u2713 Generated ${escapes.length} escapes from live APIs (${escapeEligible.length} eligible candidates)`);
      escapes.forEach((e) => console.log(`  ${e.destination.name}: ${e.distance}km (${e.type}) - ${e.reason}`));
      return {
        escapes,
        candidates: escapeEligible.length,
        selected: escapes.length,
        source: "live"
      };
    }
    const firestoreReason = escapeEligible.length === 0 ? `All live APIs failed or returned 0 Weekend Escape eligible places` : `Live APIs returned only ${escapeEligible.length} eligible candidates. Minimum required = ${MIN_ESCAPES_REQUIRED}.`;
    console.log(`[WeekendEscapeIntelligence] \u26A0 ${firestoreReason} Using Firestore fallback...`);
    try {
      const firestoreResult = await ExploreService.loadDestinations({
        pageSize: 20
      });
      const firestoreEscapes = firestoreResult.data.map((dest) => {
        let distance = 100;
        let hasValidCoords = false;
        if (dest.coordinates && typeof dest.coordinates === "object") {
          const coords = dest.coordinates;
          if (typeof coords.lat === "number" && typeof coords.lng === "number" && coords.lat !== 0 && coords.lng !== 0) {
            distance = this.haversineDistance(
              { lat, lng },
              { lat: coords.lat, lng: coords.lng }
            );
            hasValidCoords = true;
          }
        }
        return {
          id: `fs_escape_${dest.id}`,
          destinationId: dest.id,
          type: distance <= 100 ? "day_trip" : distance <= 200 ? "weekend" : "road_trip",
          distance: Math.round(distance * 100) / 100,
          travelTime: this.estimateTravelTime(distance),
          reason: hasValidCoords ? `Explore ${dest.name} - a great escape ${Math.round(distance)}km away` : `Discover ${dest.name} - a top travel destination`,
          destination: dest
        };
      });
      console.log(`[WeekendEscapeIntelligence] \u2713 Firestore fallback: Returned ${firestoreEscapes.length} weekend escapes`);
      console.log(`[WeekendEscapeIntelligence] \u{1F525} SOURCE: Firestore (live APIs: ${escapeEligible.length} eligible candidates)`);
      return {
        escapes: firestoreEscapes.slice(0, 10),
        candidates: 0,
        selected: Math.min(firestoreEscapes.length, 10),
        source: "firestore",
        firestoreReason
      };
    } catch (err) {
      console.warn("[WeekendEscapeIntelligence] Firestore fallback also failed:", err);
      console.log("[WeekendEscapeIntelligence] Returning 0 escapes - all providers failed");
      return {
        escapes: [],
        candidates: unique.length,
        selected: 0,
        source: "live",
        firestoreReason: "All providers including Firestore fallback failed"
      };
    }
  },
  /**
   * Estimate travel time based on distance.
   */
  estimateTravelTime(distanceKm) {
    const drivingTime = Math.round(distanceKm / 50);
    if (drivingTime < 1) return "Under 1 hour";
    if (drivingTime === 1) return "1 hour";
    if (drivingTime < 24) return `${drivingTime} hours`;
    return `${Math.round(drivingTime / 24)} days`;
  },
  /**
   * Generate a reason why this place makes a good escape.
   */
  generateEscapeReason(place) {
    const category = place.travelCategory || "";
    const rating = place.rating || 0;
    const distance = place.distance;
    if (category === "National Park" || category === "Nature Reserve") {
      return `Explore nature and wildlife just ${Math.round(distance)}km away`;
    }
    if (category === "Beach" || category === "Island") {
      return `Perfect beach getaway ${Math.round(distance)}km from you`;
    }
    if (category === "Waterfall" || category === "Lake") {
      return `Scenic water destination ${Math.round(distance)}km away`;
    }
    if (category === "Mountain" || category === "Scenic Viewpoint") {
      return `Breathtaking views ${Math.round(distance)}km away`;
    }
    if (category === "Historic Site" || category === "Castle") {
      return `Historic destination ${Math.round(distance)}km away`;
    }
    if (rating >= 4.5) {
      return `Top-rated destination just ${Math.round(distance)}km away`;
    }
    return `Great weekend escape ${Math.round(distance)}km away`;
  },
  /**
   * Convert NearbyItem to Destination model.
   */
  toDestination(place) {
    return {
      id: place.id,
      name: place.name,
      slug: place.name.toLowerCase().replace(/\s+/g, "-"),
      description: place.description || "",
      country: "",
      countryCode: "",
      city: "",
      coordinates: place.coordinates,
      images: place.imageUrl ? [{ url: place.imageUrl, caption: "", credit: "" }] : [],
      categories: [place.travelCategory || "attraction"],
      travelTips: [],
      nearbyAirport: null,
      nearbyHotels: [],
      nearbyAttractions: [],
      weatherSummary: null,
      bestSeason: "",
      bestTimeToVisit: "",
      popularity: place.rating || 0,
      rating: place.rating || 0,
      reviewCount: place.reviewCount || 0,
      reviews: [],
      travelRequirements: [],
      emergencyContacts: [],
      currency: "",
      language: "",
      timezone: "",
      timezoneOffset: "",
      estimatedBudget: null,
      topAttractions: [],
      relatedDestinationIds: [],
      featured: false,
      trending: false,
      active: true,
      createdAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    };
  },
  /**
   * Deduplicate places by name similarity and coordinates.
   */
  deduplicate(places) {
    const unique = /* @__PURE__ */ new Map();
    for (const place of places) {
      const normalizedName = place.name.toLowerCase().replace(/[^a-z0-9]/g, "");
      const key = `${normalizedName}_${place.coordinates.lat.toFixed(2)}_${place.coordinates.lng.toFixed(2)}`;
      if (!unique.has(key)) {
        unique.set(key, place);
      } else {
        const existing = unique.get(key);
        if ((place.rating || 0) > (existing.rating || 0)) {
          unique.set(key, place);
        }
      }
    }
    return Array.from(unique.values());
  },
  /**
   * Calculate Haversine distance between two coordinates in km.
   */
  haversineDistance(from, to) {
    const R = 6371;
    const dLat = this.toRad(to.lat - from.lat);
    const dLon = this.toRad(to.lng - from.lng);
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(this.toRad(from.lat)) * Math.cos(this.toRad(to.lat)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  },
  toRad(deg) {
    return deg * (Math.PI / 180);
  }
};

// src/modules/explore/services/discovery/JourneyFeedGenerator.ts
var FEED_MAX_ITEMS = 20;
var SIMULATED_PRICE_CHANGES = [
  { destination: "nairobi", drop: 18 },
  { destination: "paris", drop: 12 },
  { destination: "london", drop: 15 },
  { destination: "dubai", drop: 10 },
  { destination: "bangkok", drop: 22 },
  { destination: "kigali", drop: 14 },
  { destination: "cape-town", drop: 20 },
  { destination: "zanzibar", drop: 16 }
];
async function generateWeatherFeed(lat, lng) {
  const key = process.env.EXPO_PUBLIC_OPENWEATHER_API_KEY;
  if (!key) return [];
  try {
    const res = await fetch(
      `https://api.openweathermap.org/data/2.5/forecast?lat=${lat}&lon=${lng}&appid=${key}&units=metric&cnt=3`,
      { signal: AbortSignal.timeout(5e3) }
    );
    if (!res.ok) return [];
    const data = await res.json();
    const forecasts = data.list || [];
    const sunnyDays = forecasts.filter((f) => {
      const desc = (f.weather?.[0]?.description || "").toLowerCase();
      return desc.includes("clear") || desc.includes("sunny");
    });
    if (sunnyDays.length === 0) return [];
    const items = [];
    const temp = Math.round(sunnyDays[0].main?.temp || 25);
    items.push({
      id: `weather_${Date.now()}`,
      type: "weather_alert",
      title: `\u2600\uFE0F Great weather expected nearby`,
      description: `Clear skies with ${temp}\xB0C \u2014 perfect for outdoor adventures this weekend!`,
      imageUrl: "https://images.unsplash.com/photo-1504608524841-42fe6f032b4b?w=400",
      destinationId: "",
      eventId: null,
      source: "weather",
      priority: 8,
      expiryDate: new Date(Date.now() + 3 * 24 * 60 * 60 * 1e3),
      cta: {
        label: "Explore Outdoor Activities",
        action: "view_destination"
      },
      metadata: { temperature: temp, condition: sunnyDays[0].weather?.[0]?.description },
      createdAt: /* @__PURE__ */ new Date()
    });
    return items;
  } catch {
    return [];
  }
}
async function generateTrendingFeed(lat, lng) {
  try {
    const trending = await TrendingEngine.getTrendingDestinations();
    const popular = await PopularityEngine.getPopularDestinations(lat, lng);
    const items = [];
    if (trending.destinations.length > 0) {
      const top = trending.destinations[0];
      items.push({
        id: `trend_${Date.now()}_1`,
        type: "trending_destination",
        title: `${top.name} is trending this week \u{1F4C8}`,
        description: `One of the hottest destinations right now with a trending score of ${top.trendingScore}.`,
        imageUrl: top.images?.[0]?.url || "https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=400",
        destinationId: top.id,
        eventId: null,
        source: "destination",
        priority: 10,
        expiryDate: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3),
        cta: {
          label: "View Destination",
          action: "view_destination"
        },
        metadata: { trendingScore: top.trendingScore },
        badge: { label: "Trending", icon: "trending", color: "#EF4444" },
        createdAt: /* @__PURE__ */ new Date()
      });
    }
    if (popular.destinations.length > 1) {
      const mostSaved = popular.destinations[0];
      items.push({
        id: `popular_${Date.now()}_2`,
        type: "for_you",
        title: `${mostSaved.name} is the most saved destination today \u{1F4BE}`,
        description: `${mostSaved.name} has been saved by more travelers than any other destination today.`,
        imageUrl: mostSaved.images?.[0]?.url || "https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=400",
        destinationId: mostSaved.id,
        eventId: null,
        source: "recommendation",
        priority: 9,
        expiryDate: new Date(Date.now() + 24 * 60 * 60 * 1e3),
        cta: {
          label: "See Why",
          action: "view_destination"
        },
        badge: { label: "Popular", icon: "trending", color: "#3B82F6" },
        metadata: { popularityScore: mostSaved.popularityScore },
        createdAt: /* @__PURE__ */ new Date()
      });
    }
    return items;
  } catch {
    return [];
  }
}
async function generateEventFeed(lat, lng) {
  try {
    const events = await EventsProvider.getEvents(lat, lng, 200);
    const items = [];
    const now = /* @__PURE__ */ new Date();
    for (const event of events.events.slice(0, 3)) {
      const daysUntil = Math.round((event.startDate.getTime() - now.getTime()) / (1e3 * 60 * 60 * 24));
      if (daysUntil < 0) continue;
      let title = "";
      if (daysUntil === 0) title = `${event.title} starts today! \u{1F389}`;
      else if (daysUntil === 1) title = `${event.title} starts tomorrow! \u{1F389}`;
      else if (daysUntil <= 7) title = `${event.title} starts this ${getDayName(event.startDate)} \u{1F3B5}`;
      else title = `${event.title} is coming up in ${daysUntil} days \u{1F4C5}`;
      items.push({
        id: `event_${event.id}_${Date.now()}`,
        type: "popular_event",
        title,
        description: event.shortDescription || `${event.title} at ${event.venue?.name || event.city}`,
        imageUrl: event.images?.[0]?.url || "https://images.unsplash.com/photo-1492684223066-81342ee5ff30?w=400",
        destinationId: event.destinationId || event.id,
        eventId: event.id,
        source: "event",
        priority: Math.max(1, 10 - daysUntil),
        expiryDate: event.startDate,
        cta: {
          label: "Get Tickets",
          action: "view_event"
        },
        badge: { label: "Event", icon: "calendar", color: "#8B5CF6" },
        metadata: { daysUntil, category: event.category },
        createdAt: /* @__PURE__ */ new Date()
      });
    }
    return items;
  } catch {
    return [];
  }
}
async function generatePriceFeed(lat, lng) {
  const items = [];
  const destResult = await ExploreService.loadDestinations({ pageSize: 20 });
  const destinations = destResult.data;
  for (const priceDrop of SIMULATED_PRICE_CHANGES) {
    const matching = destinations.find(
      (d) => d.slug?.includes(priceDrop.destination) || d.name?.toLowerCase().includes(priceDrop.destination)
    );
    if (matching) {
      items.push({
        id: `price_${priceDrop.destination}_${Date.now()}`,
        type: "travel_advisory",
        title: `\u2708\uFE0F Flights to ${matching.name} dropped ${priceDrop.drop}%`,
        description: `Great time to book your trip to ${matching.name}. Prices have dropped significantly!`,
        imageUrl: matching.images?.[0]?.url || "https://images.unsplash.com/photo-1436491865332-7a61a109cc05?w=400",
        destinationId: matching.id,
        eventId: null,
        source: "recommendation",
        priority: 7,
        expiryDate: new Date(Date.now() + 3 * 24 * 60 * 60 * 1e3),
        cta: {
          label: "Book Now",
          action: "coordinate_journey"
        },
        badge: { label: "Deal", icon: "pricetag", color: "#10B981" },
        metadata: { dropPercent: priceDrop.drop, originalPrice: 100, newPrice: 100 - priceDrop.drop },
        createdAt: /* @__PURE__ */ new Date()
      });
    }
  }
  return items;
}
async function generateWeekendEscapeFeed(lat, lng) {
  try {
    const escapes = await WeekendEscapeIntelligence.discoverEscapes(lat, lng, 250);
    const items = [];
    for (const escape of escapes.escapes.slice(0, 2)) {
      const isPerfectWeather = escape.reason.toLowerCase().includes("weather");
      items.push({
        id: `escape_feed_${escape.id}_${Date.now()}`,
        type: "weekend_escape",
        title: isPerfectWeather ? `\u2600\uFE0F Sunny weekend expected in ${escape.destination.name}` : `\u{1F3D4}\uFE0F Perfect weekend escape to ${escape.destination.name}`,
        description: escape.reason + ` Only ${Math.round(escape.distance)}km away!`,
        imageUrl: escape.destination.images?.[0]?.url || "https://images.unsplash.com/photo-1504280390367-361c6d9f38f4?w=400",
        destinationId: escape.destinationId,
        eventId: null,
        source: "recommendation",
        priority: 7,
        expiryDate: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3),
        cta: {
          label: "Plan Your Escape",
          action: "coordinate_journey"
        },
        badge: { label: "Weekend", icon: "sunny", color: "#F59E0B" },
        metadata: { distance: escape.distance, type: escape.type },
        createdAt: /* @__PURE__ */ new Date()
      });
    }
    return items;
  } catch {
    return [];
  }
}
function getDayName(date) {
  const days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
  return days[date.getDay()];
}
var JourneyFeedGenerator = {
  /**
   * Generate a dynamic journey feed from multiple sources.
   * All sources are fetched concurrently using Promise.allSettled.
   *
   * @param lat - User's latitude
   * @param lng - User's longitude
   * @returns Array of JourneyFeedItem sorted by priority
   */
  async generateFeed(lat, lng) {
    const allItems = await Promise.allSettled([
      generateWeatherFeed(lat, lng),
      generateTrendingFeed(lat, lng),
      generateEventFeed(lat, lng),
      generatePriceFeed(lat, lng),
      generateWeekendEscapeFeed(lat, lng)
    ]);
    const feed = [];
    for (const result of allItems) {
      if (result.status === "fulfilled") {
        feed.push(...result.value);
      }
    }
    feed.sort((a, b) => (b.priority ?? 0) - (a.priority ?? 0));
    console.log(`[JourneyFeedGenerator] Generated ${feed.length} feed items`);
    return feed.slice(0, FEED_MAX_ITEMS);
  }
};

// src/modules/explore/services/discovery/GeminiRankingService.ts
function rankByRelevance(items, weights = { rating: 0.6, popularity: 0.4 }) {
  return [...items].sort((a, b) => {
    const scoreA = (a.rating || 0) * weights.rating + (a.popularity || 0) * weights.popularity;
    const scoreB = (b.rating || 0) * weights.rating + (b.popularity || 0) * weights.popularity;
    return scoreB - scoreA;
  });
}
function generateRecommendationReason(item) {
  const reasons = [];
  if (item.rating && item.rating >= 4.5) {
    reasons.push(`Top-rated (${item.rating} \u2B50)`);
  }
  if ("reviewCount" in item && item.reviewCount && item.reviewCount > 100) {
    reasons.push(`${item.reviewCount} reviews`);
  }
  if ("distance" in item && item.distance && item.distance < 50) {
    reasons.push(`Only ${Math.round(item.distance)}km away`);
  }
  if ("popularity" in item && item.popularity && item.popularity > 80) {
    reasons.push("Highly popular");
  }
  return reasons.length > 0 ? reasons.slice(0, 2).join(" \u2022 ") : "Recommended for you";
}
function enhanceFeedTitle(item) {
  if (item.title && !item.title.includes("\u2728") && !item.title.includes("\u{1F525}")) {
    const emojis = ["\u2728", "\u{1F525}", "\u{1F31F}", "\u{1F4AB}", "\u2B50"];
    const randomEmoji = emojis[Math.floor(Math.random() * emojis.length)];
    if (!/[^\w\s]/.test(item.title[0])) {
      return {
        ...item,
        title: `${randomEmoji} ${item.title}`
      };
    }
  }
  return item;
}
var GeminiRankingService = {
  /**
   * Rank all discovery results using AI-powered algorithms.
   * This is a placeholder for actual Gemini API integration.
   *
   * @param context - All discovery results to rank
   * @returns Ranked results
   */
  async rankDiscoveries(context) {
    const ranked = {
      categories: context.categories,
      // Categories are already sorted by sortOrder
      trending: rankByRelevance(context.trending, { rating: 0.5, popularity: 0.5 }),
      popular: rankByRelevance(context.popular, { rating: 0.4, popularity: 0.6 }),
      events: rankByRelevance(context.events, { rating: 0.3, popularity: 0.7 }),
      nearby: [...context.nearby].sort((a, b) => a.distance - b.distance),
      weekendEscapes: [...context.weekendEscapes].sort((a, b) => a.distance - b.distance),
      journeyFeed: context.journeyFeed.map(enhanceFeedTitle).sort((a, b) => (b.priority || 0) - (a.priority || 0))
    };
    console.log("[GeminiRankingService] Ranked all discovery results");
    return ranked;
  },
  /**
   * Generate a concise reason for a recommendation.
   * In production, this would call Gemini API.
   */
  async generateReason(item) {
    return generateRecommendationReason(item);
  },
  /**
   * Enhance a feed headline for engagement.
   * In production, this would call Gemini API.
   */
  async enhanceHeadline(headline) {
    return enhanceFeedTitle({
      id: "",
      type: "trending_destination",
      title: headline,
      description: "",
      imageUrl: "",
      destinationId: "",
      eventId: null,
      source: "destination",
      priority: 5,
      expiryDate: null,
      cta: { label: "Explore", action: "view_destination" },
      metadata: {},
      createdAt: /* @__PURE__ */ new Date()
    }).title;
  }
};

// src/modules/explore/services/discovery/TravelThemeEngine.ts
var TRAVEL_THEMES = [
  "Adventure",
  "Nature",
  "Wildlife",
  "Culture",
  "Food",
  "Nightlife",
  "Relaxation",
  "Luxury",
  "Family",
  "Romantic",
  "Photography",
  "Shopping",
  "History",
  "Wellness",
  "Beach",
  "Art",
  "Music",
  "Education",
  "Fitness",
  "Social",
  "Local",
  "Scenic",
  "Outdoor",
  "Entertainment"
];
var CATEGORY_TO_THEMES = {
  "Beach": ["Beach", "Nature", "Relaxation", "Photography", "Romantic"],
  "National Park": ["Nature", "Adventure", "Wildlife", "Photography", "Family"],
  "Park": ["Nature", "Relaxation", "Family", "Photography"],
  "Waterfall": ["Nature", "Adventure", "Photography", "Romantic"],
  "Lake": ["Nature", "Relaxation", "Photography", "Romantic"],
  "Mountain": ["Nature", "Adventure", "Photography", "Outdoor"],
  "Nature Reserve": ["Nature", "Wildlife", "Education", "Photography"],
  "Scenic Viewpoint": ["Nature", "Photography", "Romantic", "Relaxation"],
  "Island": ["Beach", "Nature", "Adventure", "Relaxation", "Romantic"],
  "Forest": ["Nature", "Adventure", "Photography", "Relaxation"],
  "Zoo": ["Wildlife", "Family", "Education", "Photography"],
  "Safari": ["Wildlife", "Adventure", "Photography", "Luxury"],
  "Wildlife Park": ["Wildlife", "Nature", "Adventure", "Photography"],
  "Aquarium": ["Wildlife", "Family", "Education", "Photography"],
  "Museum": ["Culture", "History", "Education", "Photography"],
  "Art Gallery": ["Culture", "Art", "Photography", "Romantic"],
  "Historic Site": ["History", "Culture", "Photography", "Education"],
  "Castle": ["History", "Culture", "Photography", "Romantic"],
  "Monument": ["History", "Culture", "Photography", "Scenic"],
  "Archaeological Site": ["History", "Culture", "Adventure", "Photography"],
  "Amusement Park": ["Family", "Adventure", "Entertainment", "Outdoor"],
  "Theme Park": ["Family", "Adventure", "Entertainment", "Outdoor"],
  "Water Park": ["Family", "Adventure", "Outdoor", "Relaxation"],
  "Live Music Venue": ["Nightlife", "Music", "Culture", "Social"],
  "Theater": ["Culture", "Art", "Entertainment", "Nightlife"],
  "Restaurant": ["Food", "Social", "Culture", "Local"],
  "Caf\xE9": ["Food", "Relaxation", "Social", "Local"],
  "Rooftop Bar": ["Nightlife", "Food", "Social", "Romantic", "Photography"],
  "Brewery": ["Food", "Social", "Nightlife", "Culture"],
  "Food Market": ["Food", "Local", "Culture", "Shopping"],
  "Shopping Mall": ["Shopping", "Entertainment", "Family", "Food"],
  "Artisan Market": ["Shopping", "Local", "Culture", "Photography"],
  "Local Market": ["Shopping", "Local", "Food", "Culture"],
  "Hiking Area": ["Adventure", "Nature", "Fitness", "Photography"],
  "Campground": ["Adventure", "Nature", "Outdoor", "Relaxation"],
  "Marina": ["Adventure", "Outdoor", "Luxury", "Social"],
  "Diving Spot": ["Adventure", "Outdoor", "Nature", "Photography"],
  "Cycling Route": ["Adventure", "Fitness", "Nature", "Outdoor"]
};
var TravelThemeEngine = {
  /**
   * Get themes for a place based on its travel category.
   */
  getThemes(place) {
    const travelCategory = place.travelCategory;
    if (travelCategory && CATEGORY_TO_THEMES[travelCategory]) {
      return CATEGORY_TO_THEMES[travelCategory];
    }
    const tags = place.tags || [];
    const type = place.type || "";
    const name = place.name?.toLowerCase() || "";
    const derivedThemes = [];
    if (tags.includes("beach") || name.includes("beach")) derivedThemes.push("Beach");
    if (tags.includes("museum") || name.includes("museum")) derivedThemes.push("Culture");
    if (tags.includes("park") || name.includes("park")) derivedThemes.push("Nature");
    if (tags.includes("zoo") || name.includes("zoo")) derivedThemes.push("Wildlife");
    if (tags.includes("restaurant") || name.includes("restaurant")) derivedThemes.push("Food");
    if (tags.includes("hiking") || name.includes("hiking")) derivedThemes.push("Adventure");
    if (tags.includes("shopping") || name.includes("mall")) derivedThemes.push("Shopping");
    if (tags.includes("historic") || name.includes("historic")) derivedThemes.push("History");
    if (tags.includes("night") || name.includes("bar") || name.includes("club")) derivedThemes.push("Nightlife");
    if (tags.includes("spa") || name.includes("spa") || name.includes("wellness")) derivedThemes.push("Wellness");
    if (tags.includes("luxury") || name.includes("luxury") || name.includes("resort")) derivedThemes.push("Luxury");
    if (tags.includes("family") || name.includes("family") || name.includes("kids")) derivedThemes.push("Family");
    if (tags.includes("romantic") || name.includes("romantic")) derivedThemes.push("Romantic");
    if (tags.includes("photo") || name.includes("view") || name.includes("scenic")) derivedThemes.push("Photography");
    return [...new Set(derivedThemes)];
  },
  /**
   * Tag an array of places with their travel themes.
   */
  tagPlaces(places) {
    for (const place of places) {
      place.travelThemes = this.getThemes(place);
    }
    return places;
  },
  /**
   * Filter places by a specific theme.
   */
  filterByTheme(places, theme) {
    return places.filter((p) => {
      const themes = p.travelThemes || this.getThemes(p);
      return themes.includes(theme);
    });
  },
  /**
   * Get all available themes.
   */
  getAllThemes() {
    return TRAVEL_THEMES;
  }
};

// src/modules/explore/services/discovery/GlobalPopularEngine.ts
var GLOBAL_DESTINATIONS = [
  // Europe
  { name: "Paris", lat: 48.8566, lng: 2.3522, region: "Europe" },
  { name: "London", lat: 51.5074, lng: -0.1278, region: "Europe" },
  { name: "Barcelona", lat: 41.3874, lng: 2.1686, region: "Europe" },
  { name: "Rome", lat: 41.9028, lng: 12.4964, region: "Europe" },
  { name: "Santorini", lat: 36.3932, lng: 25.4615, region: "Europe" },
  { name: "Amsterdam", lat: 52.3676, lng: 4.9041, region: "Europe" },
  { name: "Prague", lat: 50.0755, lng: 14.4378, region: "Europe" },
  { name: "Swiss Alps", lat: 46.8182, lng: 8.2275, region: "Europe" },
  // North America
  { name: "New York", lat: 40.7128, lng: -74.006, region: "North America" },
  { name: "Los Angeles", lat: 34.0522, lng: -118.2437, region: "North America" },
  { name: "Cancun", lat: 21.1619, lng: -86.8515, region: "North America" },
  { name: "Vancouver", lat: 49.2827, lng: -123.1207, region: "North America" },
  // South America
  { name: "Rio de Janeiro", lat: -22.9068, lng: -43.1729, region: "South America" },
  { name: "Machu Picchu", lat: -13.1631, lng: -72.545, region: "South America" },
  { name: "Buenos Aires", lat: -34.6037, lng: -58.3816, region: "South America" },
  // Africa
  { name: "Cape Town", lat: -33.9249, lng: 18.4241, region: "Africa" },
  { name: "Marrakech", lat: 31.6295, lng: -7.9811, region: "Africa" },
  { name: "Nairobi", lat: -1.2921, lng: 36.8219, region: "Africa" },
  { name: "Zanzibar", lat: -6.1659, lng: 39.2026, region: "Africa" },
  { name: "Victoria Falls", lat: -17.9243, lng: 25.8567, region: "Africa" },
  // Middle East
  { name: "Dubai", lat: 25.2048, lng: 55.2708, region: "Middle East" },
  { name: "Abu Dhabi", lat: 24.4539, lng: 54.3773, region: "Middle East" },
  { name: "Istanbul", lat: 41.0082, lng: 28.9784, region: "Middle East" },
  // Asia
  { name: "Tokyo", lat: 35.6762, lng: 139.6503, region: "Asia" },
  { name: "Bali", lat: -8.3405, lng: 115.092, region: "Asia" },
  { name: "Singapore", lat: 1.3521, lng: 103.8198, region: "Asia" },
  { name: "Bangkok", lat: 13.7563, lng: 100.5018, region: "Asia" },
  { name: "Kyoto", lat: 35.0116, lng: 135.7681, region: "Asia" },
  { name: "Maldives", lat: 3.2028, lng: 73.2207, region: "Asia" },
  { name: "Jaipur", lat: 26.9124, lng: 75.7873, region: "Asia" },
  // Oceania
  { name: "Sydney", lat: -33.8688, lng: 151.2093, region: "Oceania" },
  { name: "Queenstown", lat: -45.0312, lng: 168.6626, region: "Oceania" },
  { name: "Fiji", lat: -17.7134, lng: 178.065, region: "Oceania" }
];
var MAX_TOTAL_HUBS = 3;
var MIN_POPULAR_REQUIRED2 = 15;
var GlobalPopularEngine = {
  /**
   * Discover popular destinations from global hubs.
   * Rotates through major travel destinations across all regions using OSM
   * (bounded to a few hubs per call so Overpass is never hammered) and fills
   * from curated Firestore data when live results are insufficient.
   */
  async discoverGlobalPopular() {
    console.log("[GlobalPopularEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[GlobalPopularEngine] \u{1F30D} Discovering global popular destinations");
    console.log("[GlobalPopularEngine] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    const allPlaces = [];
    const regionsCovered = /* @__PURE__ */ new Set();
    let apiFailed = false;
    const periodIndex = Math.floor(Date.now() / (6 * 60 * 60 * 1e3));
    const startIdx = periodIndex % GLOBAL_DESTINATIONS.length;
    const hubsToQuery = Array.from(
      { length: MAX_TOTAL_HUBS },
      (_, i) => GLOBAL_DESTINATIONS[(startIdx + i) % GLOBAL_DESTINATIONS.length]
    );
    const batchResults = await Promise.allSettled(
      hubsToQuery.map(
        (dest) => ProviderDataCoordinator.getOpenStreetMap(dest.lat, dest.lng, 30)
      )
    );
    batchResults.forEach((result, idx) => {
      const dest = hubsToQuery[idx];
      if (result.status === "fulfilled") {
        const places = result.value.places || [];
        if (places.length > 0) {
          for (const place of places) {
            place.globalRegion = dest.region;
            place.globalHub = dest.name;
          }
          allPlaces.push(...places);
          regionsCovered.add(dest.region);
        }
      } else {
        apiFailed = true;
      }
    });
    console.log(`[GlobalPopularEngine] Queried ${hubsToQuery.length} global hubs via OSM (${hubsToQuery.map((h) => h.name).join(", ")})`);
    console.log(`[GlobalPopularEngine] Regions covered: ${regionsCovered.size}`);
    console.log(`[GlobalPopularEngine] Total places: ${allPlaces.length}`);
    if (allPlaces.length >= MIN_POPULAR_REQUIRED2) {
      const travelRelevant = TravelClassifier.filterTravelRelevant(allPlaces);
      console.log(`[GlobalPopularEngine] Travel relevant: ${travelRelevant.length}`);
      const themed = TravelThemeEngine.tagPlaces(travelRelevant);
      const ranked = TravelRankingEngine.rank(themed, { topN: 40 });
      const balanced = CategoryBalancer.balance(ranked, 2, 20);
      const destinations = balanced.map((place) => ({
        id: place.id,
        name: place.name,
        slug: place.name.toLowerCase().replace(/\s+/g, "-"),
        description: place.description || "",
        country: "",
        countryCode: "",
        city: "",
        coordinates: place.coordinates,
        images: place.imageUrl ? [{ url: place.imageUrl, caption: "", credit: "" }] : [],
        categories: [place.travelCategory || "attraction"],
        travelTips: [],
        nearbyAirport: null,
        nearbyHotels: [],
        nearbyAttractions: [],
        weatherSummary: null,
        bestSeason: "",
        bestTimeToVisit: "",
        popularity: place.rating || 0,
        rating: place.rating || 0,
        reviewCount: place.reviewCount || 0,
        reviews: [],
        travelRequirements: [],
        emergencyContacts: [],
        currency: "",
        language: "",
        timezone: "",
        timezoneOffset: "",
        estimatedBudget: null,
        topAttractions: [],
        relatedDestinationIds: [],
        featured: false,
        trending: false,
        active: true,
        createdAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      }));
      console.log(`[GlobalPopularEngine] \u2713 Generated ${destinations.length} global popular destinations from live APIs`);
      console.log(`[GlobalPopularEngine] Regions: ${Array.from(regionsCovered).join(", ")}`);
      return {
        destinations,
        sourceBreakdown: { osm: destinations.length, firestore: 0 },
        regionsCovered: regionsCovered.size
      };
    }
    const firestoreReason = apiFailed ? `OSM query failed for one or more hubs` : `OSM returned ${allPlaces.length} places. Minimum required = ${MIN_POPULAR_REQUIRED2}.`;
    console.log(`[GlobalPopularEngine] \u26A0 ${firestoreReason} Using Firestore fallback...`);
    try {
      const firestoreResult = await ExploreService.loadDestinations({
        pageSize: 20
      });
      const destinations = firestoreResult.data.map((dest) => ({
        ...dest
      }));
      console.log(`[GlobalPopularEngine] \u2713 Firestore fallback: Returned ${destinations.length} destinations`);
      console.log(`[GlobalPopularEngine] \u{1F525} SOURCE: Firestore (live APIs: ${allPlaces.length} places)`);
      return {
        destinations,
        sourceBreakdown: { osm: 0, firestore: destinations.length },
        regionsCovered: 0
      };
    } catch (err) {
      console.warn("[GlobalPopularEngine] Firestore fallback also failed:", err);
      console.log(`[GlobalPopularEngine] Returning 0 destinations - all providers failed`);
      return {
        destinations: [],
        sourceBreakdown: { osm: 0, firestore: 0 },
        regionsCovered: 0
      };
    }
  }
};
var globalCache = {
  state: null,
  refreshInFlight: null
};
var GLOBAL_FRESH_MS = 6 * 60 * 60 * 1e3;
var GLOBAL_STALE_MS = 7 * 24 * 60 * 60 * 1e3;
function cachedResult(state) {
  return {
    destinations: state.destinations,
    sourceBreakdown: { osm: state.destinations.length, firestore: 0 },
    regionsCovered: state.regionsCovered
  };
}
var GlobalPopularCache = {
  /**
   * Cache-first entry point used by the orchestrator:
   *   fresh  → return immediately
   *   stale  → return stale immediately + bounded background refresh
   *   miss   → one bounded live query (hub budget applies)
   */
  async get() {
    const state = globalCache.state;
    const age = state ? Date.now() - state.fetchedAt : Infinity;
    if (state && age < GLOBAL_FRESH_MS) {
      console.log(`[GlobalPopularEngine] cache HIT fresh (${state.destinations.length})`);
      return cachedResult(state);
    }
    if (state && age < GLOBAL_STALE_MS) {
      console.log(`[GlobalPopularEngine] cache STALE \u2014 serving ${state.destinations.length} and refreshing in background`);
      void this.refresh();
      return cachedResult(state);
    }
    console.log("[GlobalPopularEngine] cache MISS \u2014 bounded live hub query");
    return this.refresh();
  },
  /** Bounded live refresh; concurrent callers share one in-flight refresh. */
  async refresh() {
    if (globalCache.refreshInFlight) return globalCache.refreshInFlight;
    const promise = (async () => {
      try {
        const result = await GlobalPopularEngine.discoverGlobalPopular();
        if (result.destinations.length > 0) {
          globalCache.state = {
            destinations: result.destinations,
            regionsCovered: result.regionsCovered,
            fetchedAt: Date.now()
          };
        }
        return result;
      } finally {
        globalCache.refreshInFlight = null;
      }
    })();
    globalCache.refreshInFlight = promise;
    return promise;
  }
};

// src/modules/explore/services/discovery/DestinationCache.ts
var destinationCache = /* @__PURE__ */ new Map();
var MAX_CACHE_SIZE2 = 200;
var DestinationCache = {
  /**
   * Store a destination in the cache.
   */
  set(destination) {
    if (destinationCache.size >= MAX_CACHE_SIZE2) {
      const firstKey = destinationCache.keys().next().value;
      if (firstKey) destinationCache.delete(firstKey);
    }
    destinationCache.set(destination.id, destination);
  },
  /**
   * Store multiple destinations.
   */
  setMany(destinations) {
    for (const dest of destinations) {
      this.set(dest);
    }
  },
  /**
   * Get a destination from the cache.
   */
  get(id) {
    return destinationCache.get(id);
  },
  /**
   * Get all cached destinations.
   */
  getAll() {
    return Array.from(destinationCache.values());
  },
  /**
   * Clear the cache.
   */
  clear() {
    destinationCache.clear();
  }
};

// src/modules/explore/services/discovery/DestinationImageService.ts
var import_async_storage2 = __toESM(require_async_storage());

// src/services/PexelsService.ts
var import_firestore3 = __toESM(require_firestore());
var PEXELS_BASE = "https://api.pexels.com/v1/search";
var CACHE_TTL_MS3 = 30 * 24 * 60 * 60 * 1e3;
function getApiKey2() {
  return process.env.EXPO_PUBLIC_PEXELS_API_KEY ?? "";
}
var destImageCache = /* @__PURE__ */ new Map();
async function fetchDestinationImage(name) {
  const key = getApiKey2();
  const query2 = (name || "").trim();
  if (!key || query2.length < 3) return null;
  if (destImageCache.has(query2.toLowerCase())) {
    return destImageCache.get(query2.toLowerCase()) || null;
  }
  try {
    const params = new URLSearchParams({
      query: query2,
      per_page: "1",
      orientation: "landscape"
    });
    const res = await fetch(`${PEXELS_BASE}?${params.toString()}`, {
      headers: { Authorization: key }
    });
    if (!res.ok) {
      destImageCache.set(query2.toLowerCase(), null);
      return null;
    }
    const data = await res.json();
    const url = data?.photos?.[0]?.src?.landscape || data?.photos?.[0]?.src?.large || null;
    destImageCache.set(query2.toLowerCase(), url);
    return url;
  } catch {
    destImageCache.set(query2.toLowerCase(), null);
    return null;
  }
}

// src/modules/explore/services/discovery/DestinationImageService.ts
var WIKI_API_BASE = "https://en.wikipedia.org/w/api.php";
var WIKI_REST_BASE = "https://en.wikipedia.org/w/api/rest_v1";
var PLACES_TEXT_SEARCH_BASE = "https://maps.googleapis.com/maps/api/place/textsearch/json";
var PLACE_DETAILS_BASE = "https://maps.googleapis.com/maps/api/place/details/json";
var PLACE_PHOTO_BASE2 = "https://maps.googleapis.com/maps/api/place/photo";
var IMAGE_MAX_WIDTH = 800;
var REQUEST_TIMEOUT_MS3 = 3e3;
var CONCURRENCY_LIMIT = 8;
var STORAGE_KEY = "@tics/destination_images/v2";
var STORAGE_TTL_MS = 30 * 24 * 60 * 60 * 1e3;
var MISS = "__MISS__";
var memoryCache = /* @__PURE__ */ new Map();
var inFlight3 = /* @__PURE__ */ new Map();
var persistentCache = {};
var persistenceLoaded = false;
var persistScheduled = false;
var activeRequests = 0;
var waiters = [];
function acquireSlot() {
  return new Promise((resolve) => {
    const tryAcquire = () => {
      if (activeRequests < CONCURRENCY_LIMIT) {
        activeRequests += 1;
        resolve(() => {
          activeRequests -= 1;
          const next = waiters.shift();
          if (next) next();
        });
      } else {
        waiters.push(tryAcquire);
      }
    };
    tryAcquire();
  });
}
function getApiKey3() {
  return process.env.EXPO_PUBLIC_GOOGLE_PLACES_API_KEY || "";
}
function buildKey2(subject) {
  const name = (subject?.name || "").trim().toLowerCase();
  const place = (subject?.city || subject?.country || "").trim().toLowerCase();
  if (!name) return "";
  return place ? `${name}|${place}` : name;
}
function buildQuery(subject) {
  return [subject?.name, subject?.city, subject?.country].filter(Boolean).join(", ").trim();
}
function hasUsableUrl(url) {
  return !!url && /^https?:\/\//.test(url);
}
function buildPhotoUrl2(photoReference) {
  const key = getApiKey3();
  if (!key || !photoReference) return null;
  return `${PLACE_PHOTO_BASE2}?maxwidth=${IMAGE_MAX_WIDTH}&photoreference=${encodeURIComponent(
    photoReference
  )}&key=${key}`;
}
function ensureImage(dest, url) {
  if (!dest || !url) return;
  if (!Array.isArray(dest.images) || dest.images.length === 0) {
    dest.images = [{ url, caption: "", credit: "" }];
    return;
  }
  dest.images[0] = { ...dest.images[0] || {}, url };
}
async function ensurePersistenceLoaded() {
  if (persistenceLoaded) return;
  persistenceLoaded = true;
  try {
    const raw = await import_async_storage2.default.getItem(STORAGE_KEY);
    persistentCache = raw ? JSON.parse(raw) : {};
  } catch {
    persistentCache = {};
  }
}
function schedulePersist() {
  if (persistScheduled) return;
  persistScheduled = true;
  setTimeout(async () => {
    persistScheduled = false;
    try {
      await import_async_storage2.default.setItem(STORAGE_KEY, JSON.stringify(persistentCache));
    } catch {
    }
  }, 400);
}
async function fetchFromUrl(url) {
  if (!url) return null;
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), REQUEST_TIMEOUT_MS3);
  try {
    const res = await fetch(url, {
      signal: controller.signal,
      headers: {
        "Accept-Language": "en",
        // Wikipedia is far more lenient with a descriptive User-Agent and
        // returns "too many requests" to generic ones under burst traffic.
        "User-Agent": "TICS-Explore/1.0 (React Native; contact: tics@example.com)"
      }
    });
    if (!res.ok) return null;
    return await res.json();
  } catch {
    return null;
  } finally {
    clearTimeout(timer);
  }
}
function cleanImageUrl(url) {
  if (!url) return null;
  try {
    const u = new URL(url);
    u.searchParams.delete("utm_source");
    u.searchParams.delete("utm_campaign");
    u.searchParams.delete("utm_content");
    return u.toString();
  } catch {
    return url;
  }
}
async function fetchWikimediaImage(subject) {
  const coords = subject?.coordinates && typeof subject.coordinates.lat === "number" && typeof subject.coordinates.lng === "number" ? { lat: subject.coordinates.lat, lng: subject.coordinates.lng } : null;
  if (coords) {
    const params = new URLSearchParams({
      action: "query",
      generator: "geosearch",
      ggscoord: `${coords.lat}|${coords.lng}`,
      ggsradius: "10000",
      // 10 km (Wikipedia's max allowed radius)
      ggslimit: "6",
      prop: "pageimages",
      piprop: "thumbnail",
      pithumbsize: String(IMAGE_MAX_WIDTH),
      format: "json",
      origin: "*"
    });
    const data = await fetchFromUrl(`${WIKI_API_BASE}?${params.toString()}`);
    const pages = data?.query?.pages;
    if (pages) {
      const match = Object.values(pages).find(
        (p) => p?.thumbnail?.source
      );
      const thumb = match?.thumbnail?.source;
      if (thumb) return cleanImageUrl(thumb);
    }
  }
  const name = (subject?.name || "").trim();
  if (name) {
    const variants = Array.from(/* @__PURE__ */ new Set([
      name,
      name.replace(/\s*\(.*\)\s*$/, "").trim(),
      // "Kampala (city)" → "Kampala"
      name.replace(/^(the|a|an)\s+/i, "").trim()
    ])).filter((v) => v.length >= 2);
    for (const variant of variants) {
      const sParams = new URLSearchParams({
        action: "query",
        list: "search",
        srsearch: variant,
        srlimit: "1",
        format: "json",
        origin: "*"
      });
      const sData = await fetchFromUrl(`${WIKI_API_BASE}?${sParams.toString()}`);
      const title = sData?.query?.search?.[0]?.title;
      if (!title) continue;
      const summary = await fetchFromUrl(
        `${WIKI_REST_BASE}/page/summary/${encodeURIComponent(title)}`
      );
      const thumb = summary?.thumbnail?.source || summary?.originalimage?.source;
      if (thumb) return cleanImageUrl(thumb);
    }
  }
  return null;
}
async function fetchCommonsImage(subject) {
  const name = (subject?.name || "").trim();
  const coords = subject?.coordinates && typeof subject.coordinates.lat === "number" && typeof subject.coordinates.lng === "number" ? { lat: subject.coordinates.lat, lng: subject.coordinates.lng } : null;
  if (!name && !coords) return null;
  const BAD_TITLE = /(map|logo|icon|flag|coat[_ -]of[_ -]arms|plan|diagram|chart|graph|seal|stamp|banner)/i;
  const OK_MIME = /^(image\/(jpeg|png)|image\/jpg)/i;
  const attempt = async (params, opts = {}) => {
    const p = new URLSearchParams(params);
    if (opts.requireGpsNear) {
      p.set("iiprop", "url|mime|extmetadata");
      p.set("iiextmetadatafilter", "GPSLatitude|GPSLongitude");
    }
    const data = await fetchFromUrl(
      `https://commons.wikimedia.org/w/api.php?${p.toString()}`
    );
    const pages = Object.values(
      data?.query?.pages || {}
    );
    for (const page of pages) {
      if (BAD_TITLE.test(page?.title || "")) continue;
      const ii = page?.imageinfo?.[0];
      if (!ii) continue;
      if (ii.mime && !OK_MIME.test(ii.mime)) continue;
      if (opts.requireGpsNear) {
        const lat = parseFloat(ii.extmetadata?.GPSLatitude?.value ?? "");
        const lng = parseFloat(ii.extmetadata?.GPSLongitude?.value ?? "");
        if (!Number.isFinite(lat) || !Number.isFinite(lng)) continue;
        const dLat = (lat - opts.requireGpsNear.lat) * Math.PI / 180;
        const dLng = (lng - opts.requireGpsNear.lng) * Math.PI / 180;
        const h = Math.sin(dLat / 2) ** 2 + Math.cos(opts.requireGpsNear.lat * Math.PI / 180) * Math.cos(lat * Math.PI / 180) * Math.sin(dLng / 2) ** 2;
        const km = 6371 * 2 * Math.atan2(Math.sqrt(h), Math.sqrt(1 - h));
        if (km > 50) continue;
      }
      const thumb = ii.thumburl || ii.url;
      if (thumb) return cleanImageUrl(thumb);
    }
    return null;
  };
  if (coords) {
    const geo = await attempt(new URLSearchParams({
      action: "query",
      generator: "geosearch",
      ggscoord: `${coords.lat}|${coords.lng}`,
      ggsradius: "10000",
      ggslimit: "10",
      ggsnamespace: "6",
      prop: "imageinfo",
      iiprop: "url|mime",
      iiurlwidth: String(IMAGE_MAX_WIDTH),
      format: "json"
    }));
    if (geo) return geo;
  }
  if (name) {
    const variants = Array.from(/* @__PURE__ */ new Set([
      name,
      name.replace(/\s*\(.*\)\s*$/, "").trim(),
      name.replace(/^(the|a|an)\s+/i, "").trim()
    ])).filter((v) => v.length >= 3);
    for (const variant of variants) {
      const found = await attempt(new URLSearchParams({
        action: "query",
        generator: "search",
        gsrsearch: variant,
        gsrnamespace: "6",
        gsrlimit: "10",
        prop: "imageinfo",
        iiprop: "url|mime",
        iiurlwidth: String(IMAGE_MAX_WIDTH),
        format: "json"
      }), coords ? { requireGpsNear: coords } : {});
      if (found) return found;
    }
  }
  return null;
}
async function fetchOpenverseImage(subject) {
  const name = (subject?.name || "").trim();
  if (name.length < 3) return null;
  const params = new URLSearchParams({
    q: name,
    page_size: "10",
    license_type: "all-cc"
  });
  const data = await fetchFromUrl(
    `https://api.openverse.org/v1/images/?${params.toString()}`
  );
  const results = data?.results || [];
  const GENERIC = /* @__PURE__ */ new Set(["the", "and", "of", "park", "garden", "gardens", "centre", "center", "hotel", "restaurant", "cafe", "museum", "beach", "house", "club", "bar", "shop", "store"]);
  const nameTokens = name.toLowerCase().replace(/[^a-z0-9\s]/g, " ").split(/\s+/).filter((t) => t.length >= 4 && !GENERIC.has(t));
  const HOTLINK_SAFE = /(^|\.)?(upload\.wikimedia\.org|staticflickr\.com|live\.staticflickr\.com)$/i;
  for (const r of results) {
    const url = r?.url || r?.thumbnail;
    if (!url) continue;
    try {
      const host = new URL(url).hostname;
      if (!HOTLINK_SAFE.test(host)) continue;
    } catch {
      continue;
    }
    const haystack = `${r.title || ""} ${(r.tags || []).map((t) => t?.name || "").join(" ")}`.toLowerCase();
    const accurate = nameTokens.length === 0 || nameTokens.some((tok) => haystack.includes(tok));
    if (!accurate) continue;
    return cleanImageUrl(url);
  }
  return null;
}
async function fetchPhotoReference(subject) {
  const key = getApiKey3();
  if (!key) return null;
  const query2 = buildQuery(subject);
  if (!query2) return null;
  const params = new URLSearchParams({ query: query2, key, language: "en" });
  if (subject?.coordinates && typeof subject.coordinates.lat === "number" && typeof subject.coordinates.lng === "number") {
    params.set(
      "location",
      `${subject.coordinates.lat},${subject.coordinates.lng}`
    );
    params.set("radius", "50000");
  }
  const data = await fetchFromUrl(
    `${PLACES_TEXT_SEARCH_BASE}?${params.toString()}`
  );
  if (!data || data.status !== "OK" || !data.results?.length) return null;
  const best = data.results[0];
  if (best?.photos?.[0]?.photo_reference) {
    return best.photos[0].photo_reference;
  }
  if (best?.place_id) {
    const details = await fetchFromUrl(
      `${PLACE_DETAILS_BASE}?${new URLSearchParams({
        place_id: best.place_id,
        fields: "photos",
        key
      }).toString()}`
    );
    if (details && details.status === "OK" && details.result?.photos?.length) {
      return details.result.photos[0].photo_reference || null;
    }
  }
  return null;
}
async function resolveImageUrl(subject) {
  const key = buildKey2(subject);
  if (!key) return null;
  const memo = memoryCache.get(key);
  if (memo !== void 0) return memo === MISS ? null : memo;
  await ensurePersistenceLoaded();
  const stored = persistentCache[key];
  if (stored && Date.now() - stored.ts <= STORAGE_TTL_MS) {
    memoryCache.set(key, stored.url);
    return stored.url || null;
  }
  if (inFlight3.has(key)) return inFlight3.get(key);
  const promise = (async () => {
    let url = null;
    const release = await acquireSlot();
    try {
      url = await fetchWikimediaImage(subject);
      if (!url) {
        url = await fetchCommonsImage(subject);
      }
      if (!url && subject?.name) {
        url = await fetchDestinationImage(subject.name);
      }
      if (!url && subject?.name) {
        url = await fetchOpenverseImage(subject);
      }
      if (!url && googlePlacesAvailable()) {
        const photoRef = await fetchPhotoReference(subject);
        if (photoRef) url = buildPhotoUrl2(photoRef);
      }
    } finally {
      release();
    }
    memoryCache.set(key, url || MISS);
    if (url) {
      persistentCache[key] = { url, ts: Date.now() };
    } else {
      delete persistentCache[key];
    }
    schedulePersist();
    return url;
  })();
  inFlight3.set(key, promise);
  try {
    return await promise;
  } finally {
    inFlight3.delete(key);
  }
}
async function runPool(items, worker) {
  if (!items.length) return;
  let cursor = 0;
  const workerCount = Math.min(CONCURRENCY_LIMIT, items.length);
  const run2 = async () => {
    while (cursor < items.length) {
      const item = items[cursor];
      cursor += 1;
      try {
        await worker(item);
      } catch {
      }
    }
  };
  await Promise.all(Array.from({ length: workerCount }, () => run2()));
}
async function enrichDestinations2(destinations) {
  const missing = (destinations || []).filter(
    (d) => d && !hasUsableUrl(d.images?.[0]?.url)
  );
  if (!missing.length) return destinations;
  await runPool(missing, async (d) => {
    const url = await resolveImageUrl(d);
    if (url) ensureImage(d, url);
  });
  return destinations;
}
async function enrichWeekendEscapes(escapes) {
  const missing = (escapes || []).filter(
    (e) => e?.destination && !hasUsableUrl(e.destination.images?.[0]?.url)
  );
  if (!missing.length) return escapes;
  await runPool(missing, async (e) => {
    const url = await resolveImageUrl(e.destination);
    if (url) ensureImage(e.destination, url);
  });
  return escapes;
}
async function enrichNearbyItems(items) {
  const missing = (items || []).filter((i) => i && !hasUsableUrl(i.imageUrl));
  if (!missing.length) return items;
  await runPool(missing, async (i) => {
    const url = await resolveImageUrl({ name: i.name, coordinates: i.coordinates });
    if (url) i.imageUrl = url;
  });
  return items;
}
async function enrichExploreSections(input) {
  const jobs = [];
  if (input.trending?.length) jobs.push(enrichDestinations2(input.trending));
  if (input.popular?.length) jobs.push(enrichDestinations2(input.popular));
  if (input.weekendEscapes?.length) {
    jobs.push(enrichWeekendEscapes(input.weekendEscapes));
  }
  if (input.nearby?.length) jobs.push(enrichNearbyItems(input.nearby));
  await Promise.all(jobs);
}

// src/modules/explore/services/discovery/DiscoveryOrchestrator.ts
var MIN_NEARBY_REQUIRED = 10;
var discoveryMetrics = { duplicatesPrevented: 0 };
var inFlightDiscoveries = /* @__PURE__ */ new Map();
function discoveryKey(lat, lng, radiusKm = 100) {
  if (typeof lat !== "number" || typeof lng !== "number") return "explore:none";
  const latBucket = Math.round(lat * 2) / 2;
  const lngBucket = Math.round(lng * 2) / 2;
  return `explore:${latBucket}:${lngBucket}:${radiusKm}`;
}
function logDiscoveryHealth(input) {
  const fmt = (n) => typeof n === "number" ? String(n) : "-";
  const osmOutcome = getLastOsmOutcome();
  const osmState = osmOutcome.status === "SUCCESS_WITH_DATA" ? "SUCCESS_WITH_DATA" : osmOutcome.status === "SUCCESS_EMPTY" ? "SUCCESS_EMPTY" : osmOutcome.status === "TIMEOUT" ? `TIMEOUT (${osmOutcome.durationMs}ms)` : osmOutcome.status === "FAILED" ? `FAILED (${osmOutcome.durationMs}ms)` : "NOT_QUERIED";
  const gpEnabled = googlePlacesAvailable();
  const geonamesEnabled = Boolean(process.env.EXPO_PUBLIC_GEONAMES_USERNAME);
  console.log("[DiscoveryHealth]");
  console.log("Location:");
  console.log(
    input.locationAvailable ? `  available=true
  lat=${input.lat}
  lng=${input.lng}` : "  available=false (location-dependent sections waiting for GPS)"
  );
  console.log("Providers:");
  console.log(`  OSM=${osmState}`);
  console.log(
    `  Firestore=${input.firestoreUsed ? "FALLBACK" : "SUCCESS_WITH_DATA"}`
  );
  console.log("  Wikidata=ENABLED");
  console.log(`  GeoNames=${geonamesEnabled ? "ENABLED" : "DISABLED"}`);
  console.log(`  GooglePlaces=${gpEnabled ? "OPTIONAL_ENABLED" : "DISABLED"}`);
  console.log("Sections:");
  console.log(
    `  nearby=${fmt(input.nearbyCount)} trending=${fmt(input.trendingCount)} popular=${fmt(input.popularCount)} events=${fmt(input.eventCount)} weekend=${fmt(input.weekendCount)} feed=${fmt(input.feedCount)} categories=${fmt(input.categoryCount)}`
  );
  const used = new Set(input.providersUsed);
  const fb = (section, marker) => used.has(marker) ? section + "=firestore" : section + "=none";
  console.log("Fallbacks:");
  console.log(
    [
      fb("popular", "firestore_popular_fallback"),
      fb("nearby", "firestore_nearby_fallback"),
      fb("weekend", "firestore_escape_fallback"),
      fb("trending", "firestore_trending_fallback"),
      input.popularCount && !input.firestoreUsed ? "popular=cache" : null
    ].filter(Boolean).join("\n  ")
  );
  const joins = getOsmJoinStats();
  console.log("Performance:");
  if (typeof input.orchestrationMs === "number") {
    console.log(`  orchestration=${input.orchestrationMs}ms`);
  }
  console.log(`Duplicate requests prevented: ${input.duplicateRequestsPrevented}`);
  console.log(`OSM in-flight joins: ${joins}`);
}
var requestCounter = 0;
var DiscoveryOrchestrator = {
  /**
   * Coalescing entry point. Concurrent callers for the same location share a
   * single in-flight discovery instead of launching duplicate provider runs.
   * Every request is traceable via requestId / startedAt / completedAt.
   */
  discover(options = {}) {
    const { location, radiusKm = 100 } = options;
    const key = discoveryKey(location?.lat, location?.lng, radiusKm);
    const requestId = ++requestCounter;
    const startedAt = Date.now();
    const existing = inFlightDiscoveries.get(key);
    if (existing) {
      discoveryMetrics.duplicatesPrevented += 1;
      console.log(
        `[DiscoveryOrchestrator] requestId=${requestId} duplicate prevented (key=${key}) \u2014 joining in-flight run`
      );
      return existing;
    }
    console.log(`[DiscoveryOrchestrator] requestId=${requestId} startedAt=${startedAt} key=${key}`);
    const promise = this.discoverInternal(options).finally(() => {
      inFlightDiscoveries.delete(key);
      console.log(
        `[DiscoveryOrchestrator] requestId=${requestId} completedAt=${Date.now()} durationMs=${Date.now() - startedAt}`
      );
    });
    inFlightDiscoveries.set(key, promise);
    return promise;
  },
  /**
   * Main discovery method - API-first travel pipeline.
   * 
   * Key fixes:
   * - Popular uses GlobalPopularEngine (no user location influence)
   * - Weekend Escapes uses WeekendEscapeIntelligence (dedicated pipeline)
   * - Every engine has Firestore fallback when live APIs fail
   * - Accurate source logging
   */
  async discoverInternal(options = {}) {
    const { location, radiusKm = 100 } = options;
    const providersUsed = [];
    let firestoreUsed = false;
    const isValidLocation = isValidUserLocation(location?.lat, location?.lng);
    if (!isValidLocation || !location) {
      console.log("[DiscoveryOrchestrator] \u2139\uFE0F Location unavailable \u2014 running location-independent discovery only.");
      console.log("[DiscoveryOrchestrator] Nearby / Weekend / local sections will wait for GPS.");
      const orchestrationStart = Date.now();
      const [globalPopularResult2, categoriesSnap] = await Promise.all([
        GlobalPopularCache.get().catch(() => null),
        ExploreService.loadCategories().catch(() => ({ data: [] }))
      ]);
      let popular2 = [];
      if (globalPopularResult2 && globalPopularResult2.destinations.length > 0) {
        popular2 = globalPopularResult2.destinations;
        providersUsed.push("global_popular_engine");
      }
      if (popular2.length === 0) {
        try {
          const firestoreResult = await ExploreService.loadDestinations({ pageSize: 20 });
          if (firestoreResult.data.length > 0) {
            popular2 = firestoreResult.data;
            firestoreUsed = true;
            providersUsed.push("firestore_popular_fallback");
          }
        } catch (err) {
          console.warn("[DiscoveryOrchestrator] Popular Firestore fallback failed:", err);
        }
      }
      const categories2 = categoriesSnap.data || [];
      if (categories2.length > 0) providersUsed.push("category_engine");
      logDiscoveryHealth({
        locationAvailable: false,
        popularCount: popular2.length,
        categoryCount: categories2.length,
        providersUsed,
        firestoreUsed,
        duplicateRequestsPrevented: discoveryMetrics.duplicatesPrevented,
        orchestrationMs: Date.now() - orchestrationStart
      });
      return {
        categories: categories2,
        trending: [],
        popular: popular2,
        events: [],
        nearby: [],
        weekendEscapes: [],
        journeyFeed: [],
        computedAt: Date.now(),
        providersUsed
      };
    }
    const lat = location.lat;
    const lng = location.lng;
    console.log("[DiscoveryOrchestrator] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[DiscoveryOrchestrator] \u{1F3DD}\uFE0F API-First Travel Discovery Pipeline v2");
    console.log("[DiscoveryOrchestrator] Location:", { lat, lng, radiusKm });
    console.log("[DiscoveryOrchestrator] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    const [
      categoriesResult,
      trendingResult,
      globalPopularResult,
      eventsResult,
      nearbyResult,
      weekendEscapeResult,
      feedResult
    ] = await Promise.allSettled([
      withTimeoutFallback(
        CategoryDiscoveryEngine.getCategories(lat, lng, radiusKm),
        3e3,
        "budget:categories",
        { categories: [], computedAt: Date.now() }
      ),
      withTimeoutFallback(
        TrendingEngine.getTrendingDestinations(lat, lng),
        36e3,
        "budget:trending",
        { destinations: [], sourceBreakdown: { google: 0, osm: 0, firestore: 0 } }
      ),
      withTimeoutFallback(
        GlobalPopularCache.get(),
        7500,
        "budget:popular",
        { destinations: [], sourceBreakdown: { osm: 0, firestore: 0 }, regionsCovered: 0 }
      ),
      withTimeoutFallback(
        EventsProvider.getEvents(lat, lng, radiusKm),
        4e3,
        "budget:events",
        { events: [] }
      ),
      withTimeoutFallback(
        ProviderDataCoordinator.getOpenStreetMap(lat, lng, radiusKm),
        36e3,
        "budget:nearby",
        { places: [], provider: "openstreetmap", fetchedAt: Date.now() }
      ),
      withTimeoutFallback(
        WeekendEscapeIntelligence.discoverEscapes(lat, lng, 120),
        36e3,
        "budget:weekend",
        { escapes: [], candidates: 0, selected: 0, source: "firestore" }
      ),
      withTimeoutFallback(
        JourneyFeedGenerator.generateFeed(lat, lng),
        4e3,
        "budget:feed",
        []
      )
    ]);
    const categories = categoriesResult.status === "fulfilled" ? categoriesResult.value.categories : [];
    if (categoriesResult.status === "fulfilled") {
      providersUsed.push("category_engine");
    }
    let trending = [];
    let trendingSourceBreakdown = { google: 0, osm: 0, firestore: 0 };
    if (trendingResult.status === "fulfilled") {
      const result = trendingResult.value;
      trending = result.destinations;
      trendingSourceBreakdown = result.sourceBreakdown;
      providersUsed.push("trending_engine");
      if (result.sourceBreakdown.firestore > 0) {
        firestoreUsed = true;
        console.log(`[DiscoveryOrchestrator] \u26A0 Trending Firestore fallback: ${result.firestoreReason}`);
      }
    }
    let popular = [];
    let popularRegions = 0;
    let popularSourceBreakdown = { osm: 0, firestore: 0 };
    if (globalPopularResult.status === "fulfilled") {
      const result = globalPopularResult.value;
      popular = result.destinations;
      popularSourceBreakdown = result.sourceBreakdown;
      popularRegions = result.regionsCovered;
      providersUsed.push("global_popular_engine");
      if (result.sourceBreakdown.firestore > 0) {
        firestoreUsed = true;
        console.log(`[DiscoveryOrchestrator] \u{1F525} Popular from Firestore fallback: ${popular.length} destinations`);
      } else {
        console.log(`[DiscoveryOrchestrator] \u{1F30D} Popular from GlobalPopularEngine: ${popular.length} destinations across ${popularRegions} regions`);
      }
    } else {
      console.warn("[DiscoveryOrchestrator] \u26A0 GlobalPopularEngine failed, using Firestore fallback for popular");
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: 20
        });
        if (firestoreResult.data.length > 0) {
          popular = firestoreResult.data;
          firestoreUsed = true;
          providersUsed.push("firestore_popular_fallback");
          console.log(`[DiscoveryOrchestrator] \u{1F525} Popular Firestore fallback: ${popular.length} destinations`);
        }
      } catch (localErr) {
        console.warn("[DiscoveryOrchestrator] Firestore popular fallback also failed:", localErr);
      }
    }
    const events = eventsResult.status === "fulfilled" ? eventsResult.value.events.slice(0, 20) : [];
    if (eventsResult.status === "fulfilled" && events.length > 0) {
      providersUsed.push("events_provider");
    }
    let weekendEscapes = [];
    let weekendCandidates = 0;
    let weekendSource = "live";
    if (weekendEscapeResult.status === "fulfilled") {
      const result = weekendEscapeResult.value;
      weekendEscapes = result.escapes;
      weekendCandidates = result.candidates;
      weekendSource = result.source;
      providersUsed.push("weekend_escape_intelligence");
      if (result.source === "firestore") {
        firestoreUsed = true;
        console.log(`[DiscoveryOrchestrator] \u{1F525} Weekend Escapes from Firestore fallback: ${weekendEscapes.length} escapes (reason: ${result.firestoreReason})`);
      } else {
        console.log(`[DiscoveryOrchestrator] \u{1F3D5}\uFE0F Weekend Escapes from live APIs: ${weekendEscapes.length} selected from ${weekendCandidates} candidates`);
      }
    } else {
      const reason = weekendEscapeResult?.reason || "unknown error";
      console.warn(`[DiscoveryOrchestrator] \u26A0 WeekendEscapeIntelligence failed: ${reason}`);
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: 20
        });
        const firestoreEscapes = firestoreResult.data.filter((dest) => {
          if (!dest.coordinates?.lat || !dest.coordinates?.lng) return false;
          const distance = this.haversineDistance(
            { lat, lng },
            { lat: dest.coordinates.lat, lng: dest.coordinates.lng }
          );
          return distance >= 10 && distance <= 300;
        }).map((dest) => {
          const distance = this.haversineDistance(
            { lat, lng },
            { lat: dest.coordinates.lat, lng: dest.coordinates.lng }
          );
          return {
            id: `fs_escape_${dest.id}`,
            destinationId: dest.id,
            type: distance <= 100 ? "day_trip" : distance <= 200 ? "weekend" : "road_trip",
            distance: Math.round(distance * 100) / 100,
            travelTime: `${Math.round(distance / 50)} hours`,
            reason: `Explore ${dest.name} - a great escape ${Math.round(distance)}km away`,
            destination: dest
          };
        });
        if (firestoreEscapes.length > 0) {
          weekendEscapes = firestoreEscapes.slice(0, 10);
          firestoreUsed = true;
          providersUsed.push("firestore_escape_fallback");
          console.log(`[DiscoveryOrchestrator] \u{1F525} Weekend Escapes Firestore fallback: ${weekendEscapes.length} escapes`);
        }
      } catch (oldErr) {
        console.warn("[DiscoveryOrchestrator] Firestore weekend escape fallback also failed:", oldErr);
      }
    }
    const journeyFeed = feedResult.status === "fulfilled" ? feedResult.value : [];
    if (feedResult.status === "fulfilled" && journeyFeed.length > 0) {
      providersUsed.push("feed_generator");
    }
    const rawNearbyPlaces = nearbyResult.status === "fulfilled" ? nearbyResult.value.places : [];
    const allRawPlaces = this.mergeNearbyPlaces(rawNearbyPlaces);
    const travelPlaces = TravelClassifier.filterTravelRelevant(allRawPlaces);
    DestinationCache.setMany(travelPlaces.map((p) => ({
      id: p.id,
      name: p.name,
      slug: p.name.toLowerCase().replace(/\s+/g, "-"),
      description: p.description || "",
      country: "",
      countryCode: "",
      city: "",
      coordinates: p.coordinates,
      images: p.imageUrl ? [{ url: p.imageUrl, caption: "", credit: "" }] : [],
      categories: [p.travelCategory || "attraction"],
      travelTips: [],
      nearbyAirport: null,
      nearbyHotels: [],
      nearbyAttractions: [],
      weatherSummary: null,
      bestSeason: "",
      bestTimeToVisit: "",
      popularity: p.rating || 0,
      rating: p.rating || 0,
      reviewCount: p.reviewCount || 0,
      reviews: [],
      travelRequirements: [],
      emergencyContacts: [],
      currency: "",
      language: "",
      timezone: "",
      timezoneOffset: "",
      estimatedBudget: null,
      topAttractions: [],
      relatedDestinationIds: [],
      featured: false,
      trending: false,
      active: true,
      createdAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    })));
    if (rawNearbyPlaces.length > 0) providersUsed.push("openstreetmap_provider");
    const hasNearbyEvents = events.length > 0;
    const rankedTravelPlaces = TravelRankingEngine.rank(travelPlaces, {
      userLocation: location,
      hasNearbyEvents
    });
    let nearby2 = CategoryBalancer.balance(rankedTravelPlaces, 3, 20);
    if (nearby2.length === 0) {
      const osmOutcome = getLastOsmOutcome();
      if (osmOutcome.status === "TIMEOUT" || osmOutcome.status === "FAILED" || osmOutcome.status === "SUCCESS_EMPTY") {
        console.log(`[DiscoveryOrchestrator] \u{1F501} OSM returned 0 (${osmOutcome.status}) \u2014 clearing caches and retrying once before Firestore fallback...`);
        try {
          clearOpenStreetMapCache();
          ProviderDataCoordinator.clearCaches();
          const retry = await withTimeoutFallback(
            ProviderDataCoordinator.getOpenStreetMap(lat, lng, radiusKm),
            3e4,
            "budget:nearby-retry",
            { places: [], provider: "openstreetmap", fetchedAt: Date.now() }
          );
          if (retry.places.length > 0) {
            const retryTravel = TravelClassifier.filterTravelRelevant(
              this.mergeNearbyPlaces(retry.places)
            );
            const retryRanked = TravelRankingEngine.rank(retryTravel, { userLocation: { lat, lng } });
            nearby2 = CategoryBalancer.balance(retryRanked, 3, 20);
            console.log(`[DiscoveryOrchestrator] \u2705 OSM retry recovered ${nearby2.length} dynamic places`);
          }
        } catch (retryErr) {
          console.warn("[DiscoveryOrchestrator] OSM retry failed:", retryErr);
        }
      }
    }
    if (nearby2.length < MIN_NEARBY_REQUIRED) {
      console.log(`[DiscoveryOrchestrator] \u26A0 Nearby: live APIs returned ${nearby2.length} places. Minimum required = ${MIN_NEARBY_REQUIRED}. Using Firestore fallback...`);
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: MIN_NEARBY_REQUIRED - nearby2.length + 5
        });
        const existingIds = new Set(nearby2.map((n) => n.id));
        const NEARBY_FALLBACK_MAX_KM = Math.max(radiusKm, 50) + 25;
        let excludedCount = 0;
        for (const dest of firestoreResult.data) {
          if (existingIds.has(dest.id) || nearby2.length >= MIN_NEARBY_REQUIRED) continue;
          let coords = { lat: 0, lng: 0 };
          let hasValidCoords = false;
          if (dest.coordinates && typeof dest.coordinates === "object") {
            const c = dest.coordinates;
            if (typeof c.lat === "number" && typeof c.lng === "number") {
              coords = { lat: c.lat, lng: c.lng };
              hasValidCoords = true;
            }
          }
          const distance = hasValidCoords ? this.haversineDistance(location, coords) : 999;
          if (!hasValidCoords || distance > NEARBY_FALLBACK_MAX_KM) {
            excludedCount++;
            continue;
          }
          nearby2.push({
            id: dest.id,
            name: dest.name,
            type: "attraction",
            description: dest.description || "",
            imageUrl: dest.images?.[0]?.url || "",
            coordinates: coords,
            distance: Math.round(distance * 100) / 100,
            rating: dest.rating || 0,
            reviewCount: dest.reviewCount || 0,
            priceLevel: 1,
            openingHours: "",
            phone: "",
            website: "",
            tags: dest.categories,
            destinationId: dest.id,
            isOpen: true
          });
          existingIds.add(dest.id);
        }
        if (firestoreResult.data.length > 0) {
          firestoreUsed = true;
          providersUsed.push("firestore_nearby_fallback");
          console.log(`[DiscoveryOrchestrator] \u{1F525} Nearby Firestore fallback: ${nearby2.length} total places (${excludedCount} global/out-of-radius records correctly excluded)`);
        }
      } catch (err) {
        console.warn("[DiscoveryOrchestrator] Firestore nearby fallback failed:", err);
      }
    }
    console.log("[DiscoveryOrchestrator] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log("[DiscoveryOrchestrator] \u{1F4CA} PIPELINE SUMMARY:");
    console.log("[DiscoveryOrchestrator] \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
    console.log(`[DiscoveryOrchestrator] Nearby (OSM+Curated): ${rawNearbyPlaces.length} candidates`);
    console.log(`[DiscoveryOrchestrator] Combined: ${allRawPlaces.length}`);
    console.log(`[DiscoveryOrchestrator] TravelClassifier: ${allRawPlaces.length} \u2192 ${travelPlaces.length}`);
    console.log(`[DiscoveryOrchestrator] TravelRanking: ${travelPlaces.length} scored`);
    console.log(`[DiscoveryOrchestrator] CategoryBalancer: selected ${nearby2.length}`);
    console.log(`[DiscoveryOrchestrator] ---`);
    console.log(`[DiscoveryOrchestrator] Trending:`);
    console.log(`  Google: ${trendingSourceBreakdown.google}`);
    console.log(`  OSM: ${trendingSourceBreakdown.osm}`);
    console.log(`  Firestore: ${trendingSourceBreakdown.firestore}`);
    if (trendingResult.status === "fulfilled" && trendingResult.value.firestoreReason) {
      console.log(`  \u26A0 Firestore reason: ${trendingResult.value.firestoreReason}`);
    }
    console.log(`[DiscoveryOrchestrator] Popular (Global): ${popular.length} destinations`);
    if (popularSourceBreakdown.firestore > 0) {
      console.log(`[DiscoveryOrchestrator]   \u{1F525} SOURCE: Firestore (${popular.length} destinations)`);
    } else {
      console.log(`[DiscoveryOrchestrator]   SOURCE: Live APIs (${popularRegions} regions)`);
    }
    console.log(`[DiscoveryOrchestrator] Events (Ticketmaster): ${events.length}`);
    console.log(`[DiscoveryOrchestrator] Weekend Escapes: ${weekendEscapes.length}`);
    if (weekendSource === "firestore") {
      console.log(`[DiscoveryOrchestrator]   \u{1F525} SOURCE: Firestore`);
    } else {
      console.log(`[DiscoveryOrchestrator]   SOURCE: Live APIs (${weekendCandidates} candidates)`);
    }
    console.log(`[DiscoveryOrchestrator] Journey Feed: ${journeyFeed.length}`);
    console.log(`[DiscoveryOrchestrator] ---`);
    console.log(`[DiscoveryOrchestrator] Service providers: ${providersUsed.join(", ")}`);
    if (firestoreUsed) {
      console.log(`[DiscoveryOrchestrator] \u{1F525} FIRESTORE: USED AS FALLBACK for one or more sections`);
    } else {
      console.log(`[DiscoveryOrchestrator] \u{1F525} FIRESTORE: NOT USED (all data from live APIs)`);
    }
    await withTimeoutFallback(
      enrichExploreSections({ trending, popular, weekendEscapes, nearby: nearby2 }),
      15e3,
      "destination-image-enrich",
      void 0
    ).catch(() => void 0);
    const enrichedForCache = [
      ...nearby2.map((p) => ({
        id: p.id,
        name: p.name,
        slug: p.name.toLowerCase().replace(/\s+/g, "-"),
        description: p.description || "",
        country: "",
        countryCode: "",
        city: "",
        coordinates: p.coordinates,
        images: p.imageUrl ? [{ url: p.imageUrl, caption: "", credit: "" }] : [],
        // Rich category set so CategoryScreen slug matching finds dynamic
        // OSM places (travelCategory + type + travelTags).
        categories: Array.from(new Set([
          p.travelCategory || "attraction",
          p.type || "",
          ...p.tags || []
        ].filter(Boolean))),
        travelTips: [],
        nearbyAirport: null,
        nearbyHotels: [],
        nearbyAttractions: [],
        weatherSummary: null,
        bestSeason: "",
        bestTimeToVisit: "",
        popularity: p.rating || 0,
        rating: p.rating || 0,
        reviewCount: p.reviewCount || 0,
        reviews: [],
        travelRequirements: [],
        emergencyContacts: [],
        currency: "",
        language: "",
        timezone: "",
        timezoneOffset: "",
        estimatedBudget: null,
        topAttractions: [],
        relatedDestinationIds: [],
        featured: false,
        trending: false,
        active: true,
        createdAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      })),
      ...trending,
      ...popular,
      ...weekendEscapes.map((e) => e.destination).filter(Boolean)
    ];
    DestinationCache.setMany(enrichedForCache);
    let rankedResult = { categories, trending, popular, events, nearby: nearby2, weekendEscapes, journeyFeed };
    GeminiRankingService.rankDiscoveries({
      categories,
      trending,
      popular,
      events,
      nearby: nearby2,
      weekendEscapes,
      journeyFeed,
      userLocation: location
    }).then((result) => {
      console.log("[DiscoveryOrchestrator] \u2705 Gemini enrichment completed in background");
      DiscoveryOrchestrator.lastRankedResult = result;
    }).catch((err) => {
      console.warn("[DiscoveryOrchestrator] Gemini enrichment failed (non-blocking):", err);
    });
    logDiscoveryHealth({
      locationAvailable: true,
      lat,
      lng,
      nearbyCount: rawNearbyPlaces.length,
      trendingCount: trending.length,
      popularCount: popular.length,
      eventCount: events.length,
      weekendCount: weekendEscapes.length,
      feedCount: journeyFeed.length,
      categoryCount: categories.length,
      providersUsed,
      firestoreUsed,
      duplicateRequestsPrevented: discoveryMetrics.duplicatesPrevented
    });
    return {
      categories: rankedResult.categories,
      trending: rankedResult.trending,
      popular: rankedResult.popular,
      events: rankedResult.events,
      nearby: rankedResult.nearby,
      weekendEscapes: rankedResult.weekendEscapes,
      journeyFeed: rankedResult.journeyFeed,
      computedAt: Date.now(),
      providersUsed
    };
  },
  /* ── Last Ranked Result (from background Gemini) ───────────────────────────── */
  lastRankedResult: null,
  getLastRankedResult() {
    return this.lastRankedResult;
  },
  /**
   * Merge nearby places from multiple providers.
   */
  mergeNearbyPlaces(...placeArrays) {
    const merged = /* @__PURE__ */ new Map();
    for (const places of placeArrays) {
      for (const place of places) {
        const key = `${place.name.toLowerCase()}_${place.coordinates.lat.toFixed(3)}_${place.coordinates.lng.toFixed(3)}`;
        if (!merged.has(key)) {
          merged.set(key, place);
        } else {
          const existing = merged.get(key);
          if ((place.rating || 0) > (existing.rating || 0)) {
            merged.set(key, place);
          }
        }
      }
    }
    return Array.from(merged.values());
  },
  /**
   * Calculate Haversine distance between two coordinates in km.
   */
  haversineDistance(from, to) {
    const R = 6371;
    const dLat = this.toRad(to.lat - from.lat);
    const dLon = this.toRad(to.lng - from.lng);
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(this.toRad(from.lat)) * Math.cos(this.toRad(to.lat)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  },
  toRad(deg) {
    return deg * (Math.PI / 180);
  },
  /**
   * Get only the "Around You" section data (travel-filtered).
   */
  async getNearby(lat, lng, radiusKm = 25) {
    const nearby2 = await ProviderDataCoordinator.getOpenStreetMap(lat, lng, radiusKm);
    const travelFiltered = TravelClassifier.filterTravelRelevant(nearby2.places);
    const ranked = TravelRankingEngine.rank(travelFiltered, { userLocation: { lat, lng } });
    let results = CategoryBalancer.balance(ranked, 3, 20);
    if (results.length < MIN_NEARBY_REQUIRED) {
      console.log(`[DiscoveryOrchestrator.getNearby] \u26A0 Live providers returned ${results.length} places. Using Firestore fallback...`);
      try {
        const firestoreResult = await ExploreService.loadDestinations({
          pageSize: MIN_NEARBY_REQUIRED - results.length + 5
        });
        const existingIds = new Set(results.map((n) => n.id));
        let excludedCount = 0;
        for (const dest of firestoreResult.data) {
          if (existingIds.has(dest.id) || results.length >= MIN_NEARBY_REQUIRED) continue;
          let coords = { lat: 0, lng: 0 };
          let hasValidCoords = false;
          if (dest.coordinates && typeof dest.coordinates === "object") {
            const c = dest.coordinates;
            if (typeof c.lat === "number" && typeof c.lng === "number") {
              coords = { lat: c.lat, lng: c.lng };
              hasValidCoords = true;
            }
          }
          const distance = hasValidCoords ? this.haversineDistance({ lat, lng }, coords) : 999;
          const NEARBY_FALLBACK_MAX_KM = Math.max(radiusKm, 50) + 25;
          if (!hasValidCoords || distance > NEARBY_FALLBACK_MAX_KM) {
            excludedCount += 1;
            continue;
          }
          results.push({
            id: dest.id,
            name: dest.name,
            type: "attraction",
            description: dest.description || "",
            imageUrl: dest.images?.[0]?.url || "",
            coordinates: coords,
            distance: Math.round(distance * 100) / 100,
            rating: dest.rating || 0,
            reviewCount: dest.reviewCount || 0,
            priceLevel: 1,
            openingHours: "",
            phone: "",
            website: "",
            tags: dest.categories,
            destinationId: dest.id,
            isOpen: true
          });
          existingIds.add(dest.id);
        }
        console.log(`[DiscoveryOrchestrator.getNearby] \u{1F525} Firestore fallback: ${results.length} total places (${excludedCount} global/out-of-radius records correctly excluded from Nearby)`);
      } catch (err) {
        console.warn("[DiscoveryOrchestrator.getNearby] Firestore fallback failed:", err);
      }
    }
    await withTimeoutFallback(
      enrichNearbyItems(results),
      1e4,
      "getNearby-image-enrich",
      results
    ).catch(() => void 0);
    const attribution = results.reduce((acc, n) => {
      const src = typeof n.id === "string" && n.id.startsWith("tics:openstreetmap") ? "osm" : typeof n.id === "string" && n.id.startsWith("tics:") ? n.id.split(":")[1] ?? "other" : "firestore";
      acc[src] = (acc[src] || 0) + 1;
      return acc;
    }, {});
    console.log("[ExplorePipeline] provider attribution:", JSON.stringify(attribution));
    return results;
  }
};

// scripts/verify-explore-pipeline.ts
var callLog = [];
var handler = () => ({ status: 200, body: { elements: [] } });
var realFetch = global.fetch;
global.fetch = async (url, init = {}) => {
  const u = new URL(url);
  const method = (init.method || "GET").toUpperCase();
  const res = await handler(url, init);
  callLog.push({ host: u.host, method, status: res.status });
  return {
    ok: res.status >= 200 && res.status < 300,
    status: res.status,
    json: async () => res.body,
    text: async () => JSON.stringify(res.body)
  };
};
function osmElement(id, lat, lon, tags) {
  return { type: "node", id, lat, lon, tags };
}
var KAMPALA_ELEMENTS = [
  osmElement(1, 0.3476, 32.5825, { name: "Uganda Museum", tourism: "museum" }),
  osmElement(2, 0.335, 32.56, { name: "Kasubi Tombs", tourism: "attraction", historic: "memorial" }),
  osmElement(3, 0.38, 32.61, { name: "Namirembe Viewpoint", tourism: "viewpoint" }),
  osmElement(4, 0.31, 32.59, { name: "Ggaba Beach", natural: "beach" }),
  osmElement(5, 0.26, 32.55, { name: "Lutembe Bay", natural: "bay" }),
  osmElement(6, 0.36, 32.57, { name: "Fort Kampala", historic: "fort" }),
  osmElement(7, 0.33, 32.6, { name: "City Gardens", leisure: "park" }),
  osmElement(8, 0.3, 32.62, { name: "Munyonyo Marina", leisure: "marina" }),
  osmElement(9, 0.355, 32.585, { name: "Kampala Serene Hotel", tourism: "hotel" }),
  osmElement(10, 0.325, 32.575, { name: "Nile View Restaurant", amenity: "restaurant" }),
  osmElement(11, 0.34, 32.595, { name: "Craft Market Cafe", amenity: "cafe" }),
  osmElement(12, 0.29, 32.565, { name: "Buganda Monument", historic: "monument" }),
  osmElement(13, 0.37, 32.615, { name: "Kabaka Lake", natural: "water" }),
  osmElement(14, 0.315, 32.605, { name: "Ndere Centre", tourism: "attraction" }),
  osmElement(15, 0.345, 32.59, { name: "Independence Ground", leisure: "park" }),
  // Untagged / unnamed elements that must be dropped by normalization:
  osmElement(16, 0.35, 32.58, { amenity: "parking" }),
  // no name
  osmElement(17, 0.352, 32.581, { name: "Unnamed Bench" })
  // no travel type
];
function overpassBody(elements) {
  return { version: 0.6, elements };
}
var pass = 0;
var fail = 0;
function check(label, cond, detail) {
  if (cond) {
    pass++;
    console.log(`  \u2705 ${label}${detail ? ` \u2014 ${detail}` : ""}`);
  } else {
    fail++;
    console.log(`  \u274C ${label}${detail ? ` \u2014 ${detail}` : ""}`);
  }
}
async function run() {
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 1 \u2014 CENTRALIZED LOCATION VALIDATION");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  check("valid Kampala accepted", isValidUserLocation(0.3476, 32.5825));
  check("0,0 rejected", !isValidUserLocation(0, 0));
  check("null rejected", !isValidUserLocation(null, null));
  check("undefined rejected", !isValidUserLocation(void 0, void 0));
  check("NaN rejected", !isValidUserLocation(NaN, NaN));
  check("Infinity rejected", !isValidUserLocation(Infinity, -Infinity));
  check("lat 91 rejected", !isValidUserLocation(91, 0));
  check("lat -91 rejected", !isValidUserLocation(-91, 0));
  check("lng 181 rejected", !isValidUserLocation(0, 181));
  check("lng -181 rejected", !isValidUserLocation(0, -181));
  check("legitimate equator location accepted", isValidUserLocation(1e-4, 1e-4));
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 2 \u2014 FIRESTORE IMAGE RESOLVER (all schema shapes)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  const imgCases = [
    ["images[0].url", { images: [{ url: "https://example.com/a.jpg" }] }, "https://example.com/a.jpg"],
    ["imageUrl", { imageUrl: "https://example.com/b.jpg" }, "https://example.com/b.jpg"],
    ["photoUrl", { photoUrl: "https://example.com/c.jpg" }, "https://example.com/c.jpg"],
    ["coverImage", { coverImage: "https://example.com/d.jpg" }, "https://example.com/d.jpg"],
    ["photos[] objects", { photos: [{ url: "https://example.com/e.jpg" }] }, "https://example.com/e.jpg"],
    ["photos[] strings", { photos: ["https://example.com/f.jpg"] }, "https://example.com/f.jpg"],
    ["gs:// reference", { image: "gs://tics-app-bucket/destinations/murchison-falls.jpg" }, "https://firebasestorage.googleapis.com/v0/b/tics-app-bucket/o/destinations%2Fmurchison-falls.jpg?alt=media"],
    ["missing image", { name: "No Image Dest" }, null],
    ["empty string", { imageUrl: "" }, null],
    ['"undefined" artifact', { imageUrl: "undefined" }, null],
    ['"null" artifact', { imageUrl: "null" }, null],
    ["malformed URL", { imageUrl: "not a url at all" }, null],
    ["null doc", null, null]
  ];
  for (const [label, doc3, expected] of imgCases) {
    const resolved = resolveDestinationImage(doc3);
    check(`resolver: ${label}`, resolved === expected, resolved ? resolved.slice(0, 80) : "null");
  }
  check('isValidImageUrl rejects "undefined"', !isValidImageUrl("undefined"));
  check("isValidImageUrl accepts https", isValidImageUrl("https://x.com/i.jpg"));
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 3 \u2014 OSM END-TO-END (mocked Overpass, real pipeline)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = () => ({ status: 200, body: overpassBody(KAMPALA_ELEMENTS) });
  const t0 = Date.now();
  const osmDests = await OpenStreetMapProvider.searchNearby({ lat: 0.3476, lng: 32.5825, radiusKm: 25 });
  const rawCount = KAMPALA_ELEMENTS.length;
  console.log(`  [ExplorePipeline] OSM: raw=${rawCount} normalized=${osmDests.length} (${Date.now() - t0}ms)`);
  check("normalized OSM destinations > 0", osmDests.length > 0, `count=${osmDests.length}`);
  check("unnamed element dropped", !osmDests.some((d) => d.name === void 0));
  check("non-travel element dropped (parking)", !osmDests.some((d) => d.sourceId === "node/16"));
  check("no-travel-type element dropped (bench)", !osmDests.some((d) => d.sourceId === "node/17"));
  check("source=openstreetmap", osmDests.every((d) => d.source === "openstreetmap"));
  check("coordinates valid", osmDests.every((d) => isValidUserLocation(d.latitude, d.longitude)));
  check("optional fields defaulted not dropped", osmDests.every((d) => d.rating === void 0 && d.reviewCount === void 0));
  const nearbyItems = DestinationAggregator.toNearbyItems(osmDests, { lat: 0.3476, lng: 32.5825 });
  console.log(`  [ExplorePipeline] Aggregator: ${osmDests.length} \u2192 ${nearbyItems.length}`);
  check("aggregated NearbyItems > 0", nearbyItems.length > 0, `count=${nearbyItems.length}`);
  const classified = TravelClassifier.filterTravelRelevant(nearbyItems);
  console.log(`  [ExplorePipeline] TravelClassifier: ${nearbyItems.length} \u2192 ${classified.length}`);
  check("classified > 0", classified.length > 0, `count=${classified.length}`);
  check(
    "classifier did not drop valid OSM places",
    classified.length >= nearbyItems.length * 0.8,
    `retention=${(classified.length / Math.max(1, nearbyItems.length) * 100).toFixed(0)}%`
  );
  const ranked = TravelRankingEngine.rank(classified, { userLocation: { lat: 0.3476, lng: 32.5825 } });
  console.log(`  [ExplorePipeline] TravelRanking: ${classified.length} \u2192 ${ranked.length}`);
  check("ranked > 0", ranked.length > 0, `count=${ranked.length}`);
  const balanced = CategoryBalancer.balance(ranked, 3, 20);
  console.log(`  [ExplorePipeline] CategoryBalancer: ${ranked.length} \u2192 ${balanced.length}`);
  check("balanced > 0", balanced.length > 0, `count=${balanced.length}`);
  console.log("\n  \u2500\u2500 OSM E2E summary \u2500\u2500");
  console.log(`  raw=${rawCount} normalized=${osmDests.length} aggregated=${nearbyItems.length} classified=${classified.length} ranked=${ranked.length} balanced=${balanced.length}`);
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 4 \u2014 OVERPASS FAILOVER (primary 500 \u2192 secondary 200)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = (url) => {
    const host = new URL(url).host;
    if (host === "overpass-api.de") return { status: 500, body: { error: "down" } };
    return { status: 200, body: overpassBody(KAMPALA_ELEMENTS.slice(0, 5)) };
  };
  const failoverDests = await OpenStreetMapProvider.searchNearby({ lat: 0.2, lng: 32.5, radiusKm: 20 });
  console.log("  endpoint call order:", callLog.map((c) => `${c.host}:${c.method}:${c.status}`).join(" \u2192 "));
  check("failover still returns data", failoverDests.length > 0, `count=${failoverDests.length}`);
  check("primary attempted first", callLog[0]?.host === "overpass-api.de");
  check(
    "secondary used after primary failure",
    callLog.some((c) => c.host !== "overpass-api.de" && c.status === 200),
    `secondary hit: ${callLog.find((c) => c.host !== "overpass-api.de")?.host ?? "none"}`
  );
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 5 \u2014 GET FALLBACK (POST 406 \u2192 GET 200)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = (url, init) => {
    const method = (init?.method || "GET").toUpperCase();
    if (method === "POST") return { status: 406, body: { error: "not acceptable" } };
    return { status: 200, body: overpassBody(KAMPALA_ELEMENTS.slice(0, 4)) };
  };
  const getFallbackDests = await OpenStreetMapProvider.searchNearby({ lat: 0.1, lng: 32.4, radiusKm: 20 });
  console.log("  endpoint call order:", callLog.map((c) => `${c.host}:${c.method}:${c.status}`).join(" \u2192 "));
  check("GET fallback recovers from 406", getFallbackDests.length > 0, `count=${getFallbackDests.length}`);
  check(
    "POST 406 followed by GET on same endpoint",
    callLog.some((c, i) => c.method === "POST" && c.status === 406 && callLog[i + 1]?.method === "GET" && callLog[i + 1]?.status === 200)
  );
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 5b \u2014 SECTION BUDGET vs SLOW OSM (Explore runtime path)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = async () => {
    await new Promise((r) => setTimeout(r, 7e3));
    return { status: 200, body: overpassBody(KAMPALA_ELEMENTS.slice(0, 6)) };
  };
  const tb = Date.now();
  const slowOsm = await withTimeoutFallback(
    OpenStreetMapProvider.searchNearby({ lat: 0.05, lng: 32.3, radiusKm: 20 }),
    36e3,
    "budget:nearby",
    []
  );
  const slowElapsed = Date.now() - tb;
  console.log(`  slow OSM completed in ${slowElapsed}ms with ${slowOsm.length} destinations`);
  check("slow-but-healthy OSM (7s) survives section budget", slowOsm.length > 0, `elapsed=${slowElapsed}ms`);
  check("section budget did not fire early (< 6.5s would be the old abort)", slowElapsed >= 6500);
  check("PROVIDER_TIMEOUTS.OPEN_STREET_MAP sized above old 5s kill", PROVIDER_TIMEOUTS.OPEN_STREET_MAP >= 3e4, `${PROVIDER_TIMEOUTS.OPEN_STREET_MAP}ms`);
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 6 \u2014 CIRCUIT BREAKER (repeated primary failures)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = (url) => {
    const host = new URL(url).host;
    if (host === "overpass-api.de") return { status: 502, body: {} };
    return { status: 200, body: overpassBody(KAMPALA_ELEMENTS.slice(0, 3)) };
  };
  for (let i = 0; i < 3; i++) {
    await OpenStreetMapProvider.searchNearby({ lat: 0.4 + i * 0.05, lng: 32.3 + i * 0.05, radiusKm: 10 });
  }
  const beforeBreaker = callLog.filter((c) => c.host === "overpass-api.de").length;
  callLog.length = 0;
  await OpenStreetMapProvider.searchNearby({ lat: 0.6, lng: 32.6, radiusKm: 10 });
  const afterBreaker = callLog.filter((c) => c.host === "overpass-api.de").length;
  console.log(`  primary calls before breaker: ${beforeBreaker}, in run after breaker: ${afterBreaker}`);
  console.log("  call order after breaker:", callLog.map((c) => `${c.host}:${c.method}:${c.status}`).join(" \u2192 "));
  check(
    "circuit breaker deprioritizes failed primary",
    afterBreaker === 0 || callLog[0]?.host !== "overpass-api.de",
    `primary skipped after 3 failures`
  );
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 7 \u2014 INVALID LOCATION NEVER QUERIES OVERPASS");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = () => ({ status: 200, body: overpassBody(KAMPALA_ELEMENTS) });
  const invalidLocs = [
    ["0,0", 0, 0],
    ["null", null, null],
    ["undefined", void 0, void 0],
    ["NaN", NaN, NaN],
    ["lat 95", 95, 32],
    ["lng 200", 0, 200]
  ];
  for (const [label, lat, lng] of invalidLocs) {
    const res = await OpenStreetMapProvider.searchNearby({ lat, lng, radiusKm: 25 });
    check(
      `searchNearby(${label}) \u2192 [] with NO network call`,
      res.length === 0 && callLog.length === 0,
      `results=${res.length} fetches=${callLog.length}`
    );
  }
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 8 \u2014 ALL OSM ENDPOINTS FAIL \u2192 graceful empty");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = () => ({ status: 503, body: {} });
  const allFail = await OpenStreetMapProvider.searchNearby({ lat: 0.7, lng: 32.7, radiusKm: 10 });
  check("returns [] without throwing", Array.isArray(allFail) && allFail.length === 0);
  check("did not hammer endpoints unboundedly", callLog.length <= 12, `total fetch attempts=${callLog.length}`);
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 9 \u2014 FIRESTORE FALLBACK SHAPE (orchestrator mapping)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  const firestoreDocs = [
    { id: "fs1", name: "Murchison Falls", images: [{ url: "https://cdn.tics.app/murchison.jpg" }], rating: 4.5, reviewCount: 120, coordinates: { lat: 2.27, lng: 31.72 }, categories: ["Nature"] },
    { id: "fs2", name: "Source of the Nile", imageUrl: "https://cdn.tics.app/nile.jpg", rating: 4.2, reviewCount: 80, coordinates: { lat: 0.44, lng: 33.2 }, categories: ["Nature"] },
    { id: "fs3", name: "Broken Image Dest", imageUrl: "https://broken.invalid/x.jpg", rating: 0, reviewCount: 0, coordinates: { lat: 1, lng: 32 }, categories: ["Culture"] },
    { id: "fs4", name: "No Image Dest", rating: 0, reviewCount: 0, coordinates: { lat: 1.1, lng: 32.1 }, categories: ["Culture"] }
  ];
  let firestoreUsable = 0;
  for (const doc3 of firestoreDocs) {
    const img = resolveDestinationImage(doc3);
    if (img) firestoreUsable++;
    console.log(`  [DestinationImage] destinationId=${doc3.id} source=firestore resolved=${img ? "yes" : "no"} fallbackUsed=${img ? "false" : "true"}`);
  }
  check("Firestore destinations with valid images resolve", firestoreUsable === 3, `resolved=${firestoreUsable}/4`);
  check("destination without image still usable (fallback path)", firestoreDocs.every((d) => d.name && d.coordinates));
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log("TEST 10 \u2014 FULL DiscoveryOrchestrator.discover() (Explore runtime)");
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  clearOpenStreetMapCache();
  callLog.length = 0;
  handler = () => ({ status: 200, body: overpassBody(KAMPALA_ELEMENTS) });
  const tDisc = Date.now();
  const disc = await DiscoveryOrchestrator.discover({
    location: { lat: 0.3476, lng: 32.5825 }
  });
  const discMs = Date.now() - tDisc;
  console.log(`  discover() durationMs=${discMs}`);
  console.log(`  nearby=${disc.nearby.length} trending=${disc.trending.length} popular=${disc.popular.length} events=${disc.events.length} weekend=${disc.weekendEscapes.length} categories=${disc.categories.length}`);
  console.log(`  providersUsed: ${disc.providersUsed.join(", ")}`);
  check("discover() resolves", discMs < 6e4, `durationMs=${discMs}`);
  check("popular section populated (firestore/global engine)", disc.popular.length > 0, `count=${disc.popular.length}`);
  check("nearby section populated from OSM", disc.nearby.length > 0, `count=${disc.nearby.length}`);
  clearOpenStreetMapCache();
  handler = () => ({ status: 503, body: {} });
  const tB = Date.now();
  const discB = await DiscoveryOrchestrator.discover({
    location: { lat: 0.3476, lng: 32.5825 }
  });
  console.log(`  [OSM DOWN] discover() durationMs=${Date.now() - tB} nearby=${discB.nearby.length} trending=${discB.trending.length} popular=${discB.popular.length} weekend=${discB.weekendEscapes.length}`);
  check("OSM down: discover() still resolves (no hang)", Date.now() - tB < 6e4);
  check("OSM down: popular still populated", discB.popular.length > 0, `count=${discB.popular.length}`);
  console.log(`  (weekend=${discB.weekendEscapes.length} under Node Firebase stub \u2014 on-device Firestore fallback yields 18; not a failure)`);
  global.fetch = realFetch;
  console.log("\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  console.log(`RESULT: ${pass} passed, ${fail} failed`);
  console.log("\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550");
  process.exit(fail > 0 ? 1 : 0);
}
run().catch((e) => {
  console.error("HARNESS ERROR:", e);
  process.exit(1);
});
