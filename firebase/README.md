# TICS Firebase Backend

This directory replaces the Laravel backend. It contains:

- `functions/`: Firebase Cloud Functions (scheduled monitors + alert/recommendation generation)
- `firestore.rules`: Firestore security rules
- `firestore.indexes.json`: Firestore indexes (if needed)

